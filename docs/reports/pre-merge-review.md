# Pre-merge review — confirmed findings

Workflow w4dg63sk4 (86 agents). Raw 80 -> deduped 80 -> **confirmed 54** (rejected 6 as false positives). Each confirmed finding was adversarially verified against HEAD.


## HIGH (18)

### H1. ToolLoopAgent.kt:337 — ToolLoopAgentState.lastFinishReason is never set — always null
- area: agent | reviewer: codereview | category: Logic Bug / Broken API Contract
- In runEngineLoop the stream collector's when-branch places StreamEvent.Finish in the catch-all '-> Unit' case (around line 366). The onFinish engineHook (lines 319–328) updates messages, phase, pendingApprovals, and streaming text but cannot supply a finishReason because OnFinishEvent carries no such field. There is no other code path that writes to lastFinishReason. The field is defined and documented in ToolLoopAgentState as 'Finish reason of the most recent completed loop' and is part of the public engineState StateFlow, but it remains permanently null. Any host that branches on engineState.value.lastFinishReason to detect stop/length/max-steps/approval-pause gets wrong behavior on every completed generation. Fix: in runEngineLoop's collect { when (event) } block, replace the 'is StreamEvent.Finish,' entry in the -> Unit group with an explicit handler: is StreamEvent.Finish -> updateEngineStateIfCurrent(ownJob) { it.copy(lastFinishReason = event.finishReason) }
- verify: Confirmed real against current code. ToolLoopAgentState.lastFinishReason (ToolLoopAgentEngine.kt:33, documented "Finish reason of the most recent completed loop") is exposed on the public engineState StateFlow (ToolLoopAgent.kt:174) but is never assigned anywhere. In runEngineLoop, StreamEvent.Finish falls into the catch-all `-> Unit` group at ToolLoopAgent.kt:366 (unhandled), and the onFinish engine hook (lines 318-328) copies messages/streamingAssistantText/currentToolCalls/pendingApprovals/phase but cannot set lastFinishReason because OnFinishEvent (Lifecycle.kt:56-78) carries no finishReas

### H2. ToolLoopAgent.kt:883 — Tool-error message is appended BEFORE its assistant tool-call message, corrupting the log and masking the real tool error
- area: agent | reviewer: silentbug | category: error-masking / message-log corruption
- In the approval-categorization pass (streamInternal, lines 879-950) every failure path calls emitToolError(this, ..., messages) which immediately does messages.add(ToolMessage(...)) (emitToolError, line 1419). This runs for NoSuchTool (883), decode/repair failure (899-905), needsApproval failure (913-919) and gated+repaired (929-942). But the assistant message that actually CONTAINS the tool call (assistantParts.addAll(stepToolCalls) at 868) is only appended afterwards at line 984. Result: the message log becomes [..., toolResult(error for call B), assistant(toolCall A, toolCall B), toolResult(A)] — a tool result that precedes the assistant turn that issued the call.

Why it matters: providers map messages strictly in order (verified in AnthropicProvider.kt:555-601 — a Tool-role message becomes a user `tool_result` block, an Assistant message becomes the `tool_use` block). The next model call (mixed step where another tool kept the loop alive) or a host resume therefore sends a `tool_result` with no preceding `tool_use` and the provider 400s. If convertToLanguageModelPrompt runs, validateNoDanglingToolCalls (ConvertToLanguageModelPrompt.kt:275-294) subtracts the error result before call B is registered, then throws MissingToolResultsError([B]). Either way the genuine root cause (model called an unknown tool / emitted undecodable args) is hidden behind a confusing downstream error, and any persisted history is corrupted. This is a refactor regression: pre-hoist, decode failures happened inside executeTool which ran AFTER the assistant message was appended.

Fix: do not mutate `messages` from emitToolError during categorization. Collect the failed (toolCallId, toolName, AgentError) tuples into a local list during the loop (still emitting the StreamEvent.ToolError for live observers), then append their ToolMessages to `messages` AFTER the assistant message is added at line 984 — preserving [assistant(call A, call B), toolResult/err(B), toolResult(A)] order.
- verify: Confirmed against current code. emitToolError (ToolLoopAgent.kt:1410-1420) synchronously appends a Tool-role message (tool_result) to `messages` at line 1419. It is invoked 4x inside the approval-categorization loop (lines 879-950): NoSuchTool (883), decode/repair failure (899-905), approval-check failure (913-919), gated+repaired (929-942). The assistant message that contains all the tool calls (assistantParts.addAll(stepToolCalls) at 868) is only appended at line 984, AFTER the loop. So for a turn with a valid call A plus a failing call B, `messages` becomes [..., ToolMessage(B,err), Assista

### H3. MediaModels.kt:351-352 — WrappedImageModel silently drops maxImagesPerCall from the inner model
- area: generation | reviewer: codereview | category: Logic error / refactor omission
- WrappedImageModel (the concrete class created by WrapImageModel()) overrides modelId and provider but never overrides maxImagesPerCall. The ImageModel interface default is `null`, so the wrapper always returns null regardless of what the inner model declares. In ImageGeneration.generateImage(), the batching logic is: `val counts = MediaSupport.splitCount(n, model.maxImagesPerCall?.coerceAtLeast(1) ?: n)`. When model is a WrappedImageModel and the inner model has maxImagesPerCall = 1, the null from the wrapper collapses the expression to `splitCount(n, n)` — one chunk of size n — and all n images are sent in a single call to the inner model, which may fail or silently truncate. Compare to WrappedEmbeddingModel in the same file (lines 211–246) which explicitly propagates both `override val maxEmbeddingsPerCall: Int? = inner.maxEmbeddingsPerCall` and `override val supportsParallelCalls: Boolean = inner.supportsParallelCalls`. ImageBatchingTest never exercises a wrapped model so this is undetected. Fix: add `override val maxImagesPerCall: Int? = inner.maxImagesPerCall` to WrappedImageModel after line 352.
- verify: Confirmed against current code. WrappedImageModel (src/commonMain/kotlin/ai/torad/aisdk/MediaModels.kt:347-380) overrides only modelId (351) and provider (352); it never overrides maxImagesPerCall, so it inherits the ImageModel interface default of null (184-185). ImageGeneration.generateImage computes `val counts = MediaSupport.splitCount(n, model.maxImagesPerCall?.coerceAtLeast(1) ?: n)` (282); with a wrapped model the null collapses this to splitCount(n, n), which returns listOf(n) (252, since total <= perChunk) — a single call of n images. This silently defeats the n-batching that the mech

### H4. CompletionApi.kt:192-197 — Completion.stop() leaves state as Idle instead of Done — partial text erased from state machine
- area: generation | reviewer: codereview | category: State machine bug
- The setLoading callback wired in Completion.complete() (lines 186–202) transitions the phase on setLoading(false) via a `when` branch: `is CompletionPhase.Idle, is CompletionPhase.Done -> CompletionPhase.Idle`. This causes a race with stop():
1. complete() is running, phase = Streaming.
2. stop() is called → abortController?.abort(); then mutableState.update sets Streaming → Done(p.text).
3. AbortError propagates out of the collection in callCompletionApi, caught by `catch (abort: AbortError)`.
4. `finally { options.setLoading(false) }` fires. The setLoading handler sees phase = Done and applies `is CompletionPhase.Done -> CompletionPhase.Idle`, transitioning to Idle.
5. Net result: state is Idle, not Done. The partial completion text is discarded from the state machine (the return value of complete() is unaffected, but state-observing UI sees Idle).

The `Done → Idle` arm is only meaningful if some external code (like the public setCompletion() method) sets the phase to Done mid-flight and then setLoading(false) was intended to reset it. But stop() intentionally sets Done to preserve the partial text; the current arm contradicts that intent. Fix: change the two conflated arms to preserve existing terminal states:
```kotlin
is CompletionPhase.Idle -> p
is CompletionPhase.Done -> p  // stop() already set Done; preserve it
```
- verify: Confirmed by reading CompletionApi.kt and AbortSignal.kt. The code matches the finding exactly. stop() (lines 221-235) sets Streaming -> Done(p.text) to preserve partial text. The setLoading callback's false-branch (lines 191-198) has a conflated arm `is CompletionPhase.Idle, is CompletionPhase.Done, -> CompletionPhase.Idle`, which erases a Done state.

Mechanism (deterministic in the common case, not just a race): In AbortSignal.kt:65-74, abort() cancels the backing job and fires registered callbacks, but callCompletionApi registers NO callback — it detects abort only via throwIfAborted() on 

### H5. StreamObjectResult.kt:12 — StreamObjectResult holds a cold Flow — every distinct property/method collection triggers a separate upstream API call
- area: generation | reviewer: codereview | category: Resource correctness / cold-flow misuse
- StreamObjectResult stores `private val events: Flow<StreamEvent>` which, per the LanguageModel.stream() contract (explicitly documented: "Cold until collected, then drives one upstream call per collection"), triggers a new network call on every collect invocation. The class exposes four independent consumers of that same cold flow:
- partialObjectStream (line 16): flow { events.collect { ... } }
- textStream (line 31): events.filterIsInstance<>().map { } — creates a new cold pipeline over events
- elementStream() (line 34): flow { events.collect { ... } }
- finish() (line 60): directly calls events.collect { ... }

A natural usage pattern:
```kotlin
val result = StreamObjectResult(model, output, prompt = "...")
result.partialObjectStream.collect { partial -> updateUi(partial) }  // API call 1
val final = result.finish()  // API call 2 — gets a DIFFERENT generation
```
This makes two separate API calls (potentially to a paid endpoint), wastes credits, and can return inconsistent results if the model is non-deterministic.

Contrast with StreamTextResult in Generate.kt (lines 47–128), which uses an AtomicReference<CompletableDeferred<List<StreamEvent>>> memoization layer to ensure the upstream is collected exactly once and replayed to all later collectors.

StreamObjectResultTest.kt tests each consumer in isolation (separate test instances), so the double-collection path is never exercised and the bug is invisible in CI.

Fix: internally replace the raw events field with a memoized stream. The simplest approach reuses the StreamTextResult pattern: create a private StreamTextResult from events, and have partialObjectStream / textStream / elementStream / finish() all consume streamTextResult.fullStream instead of events directly. Alternatively, capture events in a MutableList<StreamEvent> on first collection using a similar CAS-based primary-collector pattern.
- verify: Confirmed against current code. StreamObjectResult.kt:12 stores `private val events: Flow<StreamEvent>` and exposes four independent raw consumers of it with no memoization: partialObjectStream (line 16, `flow { events.collect {} }`), textStream (lines 31-32, `events.filterIsInstance<>().map{}`), elementStream() (line 34, `flow { events.collect {} }`), and finish() (line 66, `events.collect {}`). A grep for shareIn/replay/AtomicReference/buffer in the file returns nothing.

The cold-flow contract is documented verbatim at LanguageModel.kt:52: "Streaming completion. Cold until collected, then d

### H6. StructuredObjectApi.kt:159 — StructuredObject.submit() swallows coroutine CancellationException, breaking structured concurrency
- area: generation | reviewer: silentbug | category: error-handling
- The broad `catch (t: Throwable)` at line 159 catches plain `kotlinx.coroutines.CancellationException` (the JobCancellationException thrown when the enclosing coroutine/scope is cancelled) and does NOT rethrow it: it sets state to `Done(partial, raw, t)`, calls the suspend `options.onError(t)`, and lets `submit()` return as if it completed normally. The earlier `catch (abort: AbortError)` (157) only catches AbortError (a CancellationException subclass used for explicit aborts); external cancellation is a *different* CancellationException and falls through to 159. Why it matters: swallowing cancellation violates cooperative cancellation — the caller believes submit() finished cleanly, the cancellation does not propagate up the job tree, and a cancelled UI/agent flow silently reports a spurious 'error' (the cancellation) via onError instead of unwinding. The team's own sibling, CompletionApi.callCompletionApi (CompletionApi.kt:94-98), does this correctly: `catch (abort: AbortError) {...} catch (t: CancellationException) { throw t } catch (t: Throwable) {...}`. Fix: add `catch (c: CancellationException) { throw c }` immediately after the AbortError catch (ordering matters — AbortError is a CancellationException subclass so it must stay first) and before the generic `catch (t: Throwable)`.
- verify: CONFIRMED REAL. In StructuredObjectApi.kt, submit() (line 107) is a suspend function that collects a Flow at line 123 (a cancellation point). The catch chain is: `catch (abort: AbortError)` (157) then `catch (t: Throwable)` (159) with NO CancellationException rethrow between them; CancellationException is not even imported in this file. AbortError is confirmed to be a CancellationException subclass (AbortSignal.kt:124: `class AbortError(...) : kotlin.coroutines.cancellation.CancellationException(...)`), so the AbortError catch only intercepts the explicit-abort subtype. An external/structured 

### H7. StreamObjectResult.kt:99 — streamObject ignores in-band StreamEvent.Error, masking real provider errors with a misleading NoObjectGeneratedError
- area: generation | reviewer: silentbug | category: silent-failure
- `StreamEvent.Error` is this SDK's in-band terminal error channel — providers emit it (not throw) for parse/protocol/API failures (e.g. AnthropicProvider 'Anthropic stream protocol error', GoogleLanguageModel/Bedrock 'Failed to parse ... stream event', OpenAICompatibleStreaming, Gateway, HttpTransport). StreamObjectResult ignores it in every accessor: `finish()` maps `is StreamEvent.Error -> Unit` (line 99-101), and `partialObjectStream` (line 20) / `elementStream` (line 53) both `if (event !is StreamEvent.TextDelta) return@collect`, dropping Error. Consequence: when the provider stream errors, `finish()`/`objectValue()` proceed to `decodeOrThrow(accumulated.toString())` on the empty/partial buffer and throw `NoObjectGeneratedError("Object stream produced no parseable object")`, completely masking the real cause (e.g. '401 invalid API key', rate limit, malformed wire frame); `partialObjectStream`/`elementStream` just complete normally with no error at all. Because the `events` Flow is private, a StreamObjectResult consumer has NO escape hatch to observe the Error (unlike StreamTextResult, which at least exposes fullStream). The correct precedent is ToolLoopAgent.kt:435: `is StreamEvent.Error -> throw UiMessageStreamError(event.message, event.cause)`. Fix: in `finish()` capture any `StreamEvent.Error` and throw it (preserving `event.cause`) instead of `-> Unit`; in `partialObjectStream` and `elementStream` re-throw on `StreamEvent.Error` rather than silently skipping it.
- verify: CONFIRMED against current code. StreamObjectResult.kt:99-101 groups `is StreamEvent.Error` with Abort/Raw into `-> Unit` inside finish(), then line 104 unconditionally calls decodeOrThrow(accumulated.toString()), which at line 114 throws NoObjectGeneratedError("Object stream produced no parseable object"), discarding the real error and its event.cause. partialObjectStream (line 20) and elementStream (line 53) both `if (event !is StreamEvent.TextDelta) return@collect`, so an emitted Error is skipped and the flow completes normally with no error at all. The `events` flow is private (line 12) wit

### H8. McpOAuth.kt:242 — addClientAuthentication callback receives authorizationServerUrl instead of tokenUrl
- area: mcp | reviewer: codereview | category: security
- Both exchangeAuthorization (line 242) and refreshAuthorization (line 281) call applyOAuthClientAuthentication passing authorizationServerUrl as the third argument. The parameter is named tokenUrl (McpOAuth.kt:390) and is forwarded verbatim to the addClientAuthentication callback (line 400) as its url parameter. The actual token endpoint URL is computed locally as tokenUrl (lines 227/268) and is used correctly for the HTTP POST (postOAuthTokenRequest at lines 247/286), but it is never passed to the authentication callback. Any URL-bound client authentication — DPoP (which must encode the token endpoint URL in the htu claim), pushed-authorization-request signing, or a provider that derives credentials from the endpoint URL — will compute against the wrong value. Fix: pass tokenUrl instead of authorizationServerUrl on lines 242 and 281.
- verify: Confirmed against current code and the upstream reference. exchangeAuthorization (McpOAuth.kt:227) and refreshAuthorization (line 268) each compute the real token endpoint as a local `tokenUrl`, use it for the HTTP POST (lines 247/286), but pass `authorizationServerUrl` — not `tokenUrl` — as the 3rd positional argument to applyOAuthClientAuthentication (lines 242 and 281). That 3rd parameter is named `tokenUrl` (line 390) and is forwarded verbatim to the addClientAuthentication callback's url parameter (line 400). So the callback receives the authorization-server base URL under a parameter nam

### H9. MCP.kt:920 — OAuth state and PKCE code_verifier use Random.Default (non-CSPRNG)
- area: mcp | reviewer: codereview | category: security
- IdGenerator defaults to random = Random.Default (IdGenerator.kt:8). On Kotlin/JVM this resolves to kotlin.random.PlatformRandom backed by java.util.Random — a seeded PRNG, not java.security.SecureRandom. Line 920 generates the OAuth state (used for CSRF) and line 921 generates the PKCE code_verifier with this non-cryptographic source. RFC 7636 §4.1 explicitly requires 'high-entropy cryptographic random string' for the code verifier. A predictable PRNG seed (e.g. seeded from wall-clock on JVM) lets an attacker who can observe the initialization time predict future state and verifier values. Fix: add an expect/actual SecureRandom wrapper in the SDK and pass it when constructing IdGenerator for both the state (line 920) and the code_verifier (line 921), e.g. IdGenerator(prefix = "mcp-verifier", size = 48, random = SecureRandom).generate().
- verify: Confirmed in current code. IdGenerator.kt:10 defaults `random = Random.Default` (finding cited line 8 = the `alphabet` field; minor drift, substance correct). On JVM, kotlin.random.Random.Default is a non-cryptographic PRNG (ThreadLocalRandom/java.util.Random), never java.security.SecureRandom — the finding's "java.util.Random" wording is slightly imprecise but the not-a-CSPRNG conclusion is correct. MCP.kt:920 generates the OAuth CSRF `state` via IdGenerator.generate(prefix="mcp") with the default RNG, and MCP.kt:922 (finding cited 921, which is actually saveState — off by one) generates the 

### H10. MCP.kt:1147 — HTTP transport silently swallows non-2xx responses to notification POSTs (incl. the initialized handshake)
- area: mcp | reviewer: silentbug | category: silent-failure
- In `HttpMCPTransport.postMessage`, the early-return guard `if (response.status.value == 202 || message is JSONRPCNotification) { ensureInboundSse(); return }` (line 1147) runs BEFORE the non-2xx error check at line 1151. Because the `message is JSONRPCNotification` disjunct matches regardless of status, any non-2xx HTTP response to a notification POST (400/500, or a 401 when no authProvider is configured) is discarded with no throw, no onError, and no log. The `if (response.status.value !in 200..299)` block that builds the rich MCPClientError (with body text and the 404->'use sse' hint) is never reached for notifications. WHY IT MATTERS: the sole notification this client sends is `notifications/initialized`, emitted as the last step of `init()` (MCP.kt:353). If the server rejects that POST (e.g. 4xx/5xx because the session isn't recognized), `init()` returns reporting a fully successful connection while the server considers the session uninitialized; subsequent `tools/list`/`tools/call` then fail in confusing ways with the true root cause (the rejected handshake and its HTTP body) already thrown away. This is a textbook silent failure that masks an upstream/transport error. FIX: move the non-2xx check ahead of the notification/202 short-circuit so errors surface for both requests and notifications, while still skipping body parsing for accepted notifications:

    if (response.status.value !in 200..299) {
        val text = response.bodyAsText()
        val hint = if (response.status.value == HTTP_NOT_FOUND) ". This server does not support HTTP transport. Try using `sse` transport instead" else ""
        val error = MCPClientError("MCP HTTP Transport Error: POSTing to endpoint (HTTP ${response.status.value})$hint: $text")
        onError?.invoke(error)
        throw error
    }
    if (response.status.value == 202 || message is JSONRPCNotification) {
        ensureInboundSse()
        return
    }

A 202 still passes the 2xx check and returns via the second block; a 200 to a notification now also returns without body-parsing, but a 4xx/5xx to a notification is no longer hidden.
- verify: Confirmed against current code. In HttpMCPTransport.postMessage (MCP.kt:1127), the guard at MCP.kt:1147 `if (response.status.value == 202 || message is JSONRPCNotification) { ensureInboundSse(); return }` runs BEFORE the non-2xx error block at MCP.kt:1151. Because the `message is JSONRPCNotification` disjunct matches regardless of HTTP status, any non-2xx response (400/500, or 401 when authProvider is null or already retried) to a notification POST short-circuits at 1147 with no throw, no onError, no log; the rich MCPClientError (body text + 404->sse hint) at 1151-1163 is never reached.\n\nThe

### H11. providers/AmazonBedrockProvider.kt:295 — Bedrock reranking: ARN region defaults to us-east-1 but agent runtime URL defaults to us-west-2
- area: providers-bedrock-media | reviewer: codereview | category: correctness
- BedrockRerankingModel.rerank calls BedrockRequest.bedrockRerankBody(settings.region ?: "us-east-1", ...) which embeds an ARN of the form arn:aws:bedrock:us-east-1::foundation-model/<modelId>. However BedrockHttp.bedrockAgentRuntimeBaseURL (BedrockHttp.kt:177) defaults to "https://bedrock-agent-runtime.${settings.region ?: "us-west-2"}.amazonaws.com". When settings.region is null and neither agentBaseURL nor baseURL is set, the HTTP request is sent to the us-west-2 endpoint while the model ARN identifies a us-east-1 resource. AWS Bedrock Agent Runtime validates that the ARN region matches the endpoint region, so every rerank call fails with a validation or not-found error in the default out-of-the-box configuration. Fix: make both defaults consistent. Since the Bedrock Agent Runtime / reranking service is historically available in us-west-2, change line 295 to use settings.region ?: "us-west-2" — matching the URL default — or alternatively change both defaults to us-east-1.
- verify: Confirmed in current code. AmazonBedrockProvider.kt:295 builds the rerank body with `settings.region ?: "us-east-1"`, and that region is embedded verbatim into the foundation-model ARN at BedrockRequest.kt:491 (`arn:aws:bedrock:$region::foundation-model/$modelId`). The endpoint, however, comes from BedrockHttp.kt:175-178 `bedrockAgentRuntimeBaseURL`, which when agentBaseURL/baseURL are unset defaults to `bedrock-agent-runtime.${settings.region ?: "us-west-2"}.amazonaws.com`. `AmazonBedrockProviderSettings.region` is `String? = null` (line 37) and the `AmazonBedrock(...)` factory does no env-va

### H12. providers/OpenAICompatibleWire.kt:56 — In-band error detector treats `"error": null` (JsonNull) as a real error, turning successful 200 responses into thrown failures
- area: providers-openai | reviewer: silentbug | category: wire-parsing-false-error
- `openAICompatibleInBandError` does `val error = obj?.get("error")` then `if (obj == null || error == null) null else { ... }`. For a body like `{"id":..., "choices":[...], "error": null}`, kotlinx returns `JsonNull` (a non-null reference and, per this same file's line 397 `null, JsonNull -> ... is JsonPrimitive ->`, a JsonPrimitive subtype), so `error == null` is FALSE. The `when(error)` branch hits `is JsonPrimitive -> error.contentOrNull ?: error.content`, and `JsonNull.contentOrNull` is null while `JsonNull.content` is the literal string "null", so it returns `OpenAICompatibleInBandError(message = "null")`. WHY IT MATTERS: `generate()` (OpenAICompatibleModels.kt:70) and every streamed chunk via `transformChatStreamEvent` (OpenAICompatibleModels.kt:178) then throw `APICallError("OpenAI-compatible request failed (200): null")` on a perfectly valid response. Many OpenAI-compatible backends (LiteLLM proxy, vLLM, self-hosted gateways) include `"error": null` on success, so this can convert the entire happy path into a cryptic "200: null" failure and hide the real result. The TS SDK guards this with a schema that requires `error` to be an object. FIX: treat JsonNull as absent, e.g. `val error = obj?.get("error")?.takeUnless { it is JsonNull }` (JsonNull is already imported here).
- verify: Confirmed genuine defect in OpenAICompatibleWire.kt:53-71. The code matches the description: line 55 `val error = obj?.get("error")`, line 56 `if (obj == null || error == null) null else {...}`, line 62 `is JsonPrimitive -> error.contentOrNull ?: error.content`. I empirically verified the kotlinx behavior by compiling the exact line-62 expression against kotlinx-serialization-json 1.9.0: for a body `{"choices":[...],"error":null}`, `obj["error"]` returns the JsonNull singleton (a NON-null reference), so `error == null` is FALSE (only a MISSING key yields Kotlin null). JsonNull `is JsonPrimitiv

### H13. providers/OpenResponsesProvider.kt:949 — User-supplied Responses provider options are silently discarded on any decode error (`runCatching{...}.getOrNull()`)
- area: providers-openai | reviewer: silentbug | category: swallowed-exception
- `openResponsesProviderOptions` ends with `return runCatching { json.decodeFromJsonElement(OpenResponsesOptions.serializer(), element) }.getOrNull()`. The JSON tolerates unknown keys (`ignoreUnknownKeys = true`), so the only way this throws is a genuine user mistake: a wrong-typed value on a known field (e.g. `maxToolCalls` as a string, `store` as `"true"`, a malformed `metadata`). When that happens the exception is swallowed and the function returns null, which means EVERY option the user set — `instructions`, `reasoningEffort`, `previousResponseId`, `store`, `conversation`, `serviceTier`, `allowedTools`, etc. — is silently dropped (see how a null `providerOptions` zeroes out all of openResponsesRequestBody lines 426-459). WHY IT MATTERS: the request silently behaves as if no options were passed; the user gets no error and no clue why their reasoning effort / cache key / instructions had no effect — a multi-hour debugging trap. The rest of this codebase fails loudly via WireDecoder. FIX: do not swallow — let the SerializationException propagate, or rethrow as a clear `InvalidArgumentError("providerOptions.$providerOptionsName", ...)` naming the offending field.
- verify: CONFIRMED real. OpenResponsesProvider.kt:949 is exactly `return runCatching { json.decodeFromJsonElement(OpenResponsesOptions.serializer(), element) }.getOrNull()`. The default json (openResponsesJson, lines 1137-1141) sets ignoreUnknownKeys=true and leaves isLenient at its false default (strict types), so unknown keys are tolerated but a wrong-typed KNOWN field throws SerializationException — which runCatching swallows, returning null. A null providerOptions then zeroes out EVERY option: lines 418-420 and 426-459 all gate on `providerOptions?.X?.let{...}` (instructions, reasoningEffort/Summar

### H14. ToolJsonSchema.kt:179 — sealedSchema generates structurally wrong JSON Schema for sealed class tool inputs
- area: tools | reviewer: codereview | category: Correctness / Schema Generation
- kotlinx.serialization 1.11.0 constructs a SealedClassSerializer descriptor with exactly 2 top-level elements: element[0] is the 'type' discriminator (PrimitiveKind.STRING) and element[1] is the inner 'value' descriptor (PolymorphicKind.SEALED or CONTEXTUAL) whose own elements are the concrete subclass descriptors. sealedSchema at line 183 iterates (0 until descriptor.elementsCount) — which is [0, 1] — and wraps both in oneOf, producing: {"oneOf": [{"type": "string"}, <value-element-schema>]}. The first oneOf variant is the raw type-discriminator pseudo-field schema, not a subclass schema. The second variant is either another nested {"oneOf": [subclass_schemas]} (if value is SEALED) or {"type": "string"} (if value is CONTEXTUAL) — both are wrong. Additionally, even the subclass schemas produced by the inner recursive call are missing the required 'type' discriminator property (e.g. {"type": "com.example.Shape.Circle"}), so kotlinx.serialization JSON format cannot be correctly described. The correct fix is: (1) navigate to descriptor.getElementDescriptor(1) to reach the inner 'value' descriptor, (2) iterate its elements (the actual subclass descriptors), (3) for each subclass generate an objectSchema that merges a required 'type' field (enum: [subclass.serialName]) with the subclass's own fields. As written, any tool whose TInput is a sealed class (e.g. a 'command: Command' union type) will send an invalid schema to providers. OpenAI strict-mode will reject the tool definition at registration time; other providers will receive LLM-generated JSON that lacks the 'type' discriminator and fails kotlinx.serialization deserialization, producing a tool error on every call. There are no tests exercising sealed class tool inputs, so this is completely undetected.
- verify: CONFIRMED against current code at src/commonMain/kotlin/ai/torad/aisdk/ToolJsonSchema.kt:179-188 with kotlinx.serialization 1.11.0 (gradle/libs.versions.toml:7).

(1) Code matches the description: sealedSchema iterates `(0 until descriptor.elementsCount)` and wraps every element in `oneOf` (lines 183-187).

(2) The descriptor-structure premise is verified, not assumed. I decompiled kotlinx-serialization-core-jvm-1.11.0.jar. SealedClassSerializer's descriptor (method `descriptor_delegate$lambda$0` / `...$0$0`) is built with PolymorphicKind.SEALED and exactly two elements: element[0]="type" back

### H15. RetryPolicy.kt:80 — retryAfterDelayMs() uses case-sensitive lookups on a known mixed-case header map, silently ignoring Retry-After on HTTP/1.1 connections
- area: transport-telemetry | reviewer: codereview | category: correctness
- HttpTransport.flattenedHeaders() (HttpTransport.kt:121) builds the responseHeaders map with `it.key` — the original header name as received off the wire. On HTTP/1.1 connections (AWS Bedrock, many proxies, older APIs) servers send `Retry-After` and `Retry-After-Ms` in title case, not lowercase. retryAfterDelayMs() at RetryPolicy.kt:80–81 performs direct map lookups:

    headers["retry-after-ms"]  // misses "Retry-After-Ms"
    headers["retry-after"]     // misses "Retry-After"

both of which return null, so the server's requested wait is discarded and the caller retries after at most maxDelayMs = 2 s. The pattern-level evidence is conclusive: every other site in the codebase that needs to read a specific header from the flattened map independently implemented a case-insensitive helper (BedrockHttp.headerValue, ReplicateProvider.replicateHeaderValue, FalProvider.headerValue, XaiProvider, LumaProvider, BlackForestLabsProvider, OpenAICompatibleFacadeSupport, DeepgramProvider, LMNTProvider, HumeProvider, ElevenLabsProvider — all use `entries.firstOrNull { it.key.equals(name, ignoreCase = true) }`). RetryPolicy is the only shared consumer that missed the same defence.

Impact: rate-limited requests to Bedrock (the SDK's primary SigV4 target) and any HTTP/1.1 provider retry immediately at exponential backoff cadence instead of honouring the server's Retry-After, risking continued 429s and burning retry budget.

Fix (preferred — systemic): Lowercase all keys in flattenedHeaders() so the map is always HTTP-canonically lowercase, matching HTTP/2 semantics and making downstream lookups uniform:

    internal fun HttpResponse.flattenedHeaders(): Map<String, String> =
        headers.entries().associate { it.key.lowercase() to it.value.joinToString(",") }

The existing case-insensitive provider helpers continue to work correctly against an all-lowercase map. Alternative minimal fix: normalise the header map locally inside retryAfterDelayMs() before lookup.
- verify: CONFIRMED genuine defect. RetryPolicy.kt:80-81 reads the response-header map with case-sensitive plain-map indexing (headers["retry-after-ms"] / headers["retry-after"]). That map originates from HttpTransport.flattenedHeaders() (HttpTransport.kt:120-121): `headers.entries().associate { it.key to it.value.joinToString(",") }` — it uses the wire-case key with NO lowercasing, and flattening a Ktor Headers into a plain Map discards Ktor's case-insensitive get(). It then feeds APICallError.responseHeaders (HttpTransport.kt:81-87/226-233; AiSdkError.kt:16-21), which retryAfterDelayMs() reads at Retr

### H16. RetryPolicy.kt:80 — Server Retry-After silently ignored: case-sensitive lookup on a server-cased header map
- area: transport-telemetry | reviewer: silentbug | category: resilience/retry
- retryAfterDelayMs() reads headers["retry-after-ms"] / headers["retry-after"] with a case-SENSITIVE Map.get on APICallError.responseHeaders. That map is produced by HttpTransport.flattenedHeaders() (`headers.entries().associate { it.key to ... }`, HttpTransport.kt:120-121), which preserves the server's ORIGINAL header casing and never lowercases — which is exactly why the rest of this codebase reads response headers through a case-insensitive helper `headerValue(headers, name)` that does `it.key.equals(name, ignoreCase = true)` (BedrockHttp.kt:208, OpenAICompatibleFacadeSupport.kt:208, LumaProvider.kt:286, ElevenLabsProvider.kt:299, DeepgramProvider.kt:529, +more). Real HTTP/1.1 servers send `Retry-After:` (Title-Case), so this lookup returns null, the server's backoff guidance is dropped with no error, and the client retries on its own short exponential schedule (<=maxDelayMs, default 2s) against a server that explicitly asked it to wait — a retry storm during rate limiting and a silent no-op of the policy's headline feature. It only happens to work over HTTP/2 (which lowercases header names on the wire), making this an environment-dependent, very hard-to-debug failure. RetryBackoffTest masks it by hand-building `responseHeaders = mapOf("retry-after" to ...)` in lowercase (RetryBackoffTest.kt:14), so the green test never exercises the real flattened-header casing. FIX: look up both keys case-insensitively, e.g. reuse a shared `headers.entries.firstOrNull { it.key.equals("retry-after", ignoreCase = true) }?.value` helper. (Minor adjacent gap: the seconds branch also silently ignores the HTTP-date form of Retry-After — acceptable but worth a comment.)
- verify: Confirmed against current code. RetryPolicy.kt:78-84 retryAfterDelayMs() does case-sensitive Map.get: headers["retry-after-ms"] (line 80) and headers["retry-after"] (line 81) on APICallError.responseHeaders. Those maps come from HttpTransport.flattenedHeaders() (HttpTransport.kt:120-121: headers.entries().associate { it.key to it.value.joinToString(",") }) which preserves the server's original header casing and never lowercases, producing a plain case-SENSITIVE LinkedHashMap. The rest of the codebase reads these exact flattened maps through a case-INSENSITIVE helper headerValue(headers,name) =

### H17. ui/Chat.kt:195 — regenerate() duplicates the last user message in state and the model request
- area: ui | reviewer: codereview | category: logic error
- Chat.regenerate() reads the last User-role message from the current message list and passes it to sendMessage(lastUser, body). sendMessage always appends its argument: `copy(messages = messages + message, ...)`. Because lastUser is already present in messages, the resulting state and the ChatRequest sent to the transport both contain a duplicate user turn (and the prior assistant reply is still in the list). Any call to regenerate() corrupts the conversation history. There are no tests for regenerate, so this has been silently broken since the rewrite. Fix: regenerate should not go through sendMessage. It should (a) trim trailing Assistant messages from the current list, (b) use the trimmed list as the request without re-appending the user message, and (c) re-drive the transport and op-tracking the same way sendMessage does. A minimal approach is to add a private sendWithExistingHistory(body) that skips the append step and have both sendMessage and regenerate call it.
- verify: Confirmed by reading the current code. Chat.regenerate() (src/commonMain/kotlin/ai/torad/aisdk/ui/Chat.kt:195-199) selects lastUser via internalState.value.messages.lastOrNull { it.role == UIMessageRole.User } — a message already present in the list — and passes it to sendMessage(lastUser, body). sendMessage (Chat.kt:166-171) unconditionally appends at line 170 (copy(messages = messages + message, ...)) and builds the outbound ChatRequest from that post-append list at line 171 (ChatRequest(messages = it.messages, ...)). Therefore every regenerate() duplicates the last user turn in BOTH the per

### H18. ui/MessageStreamReader.kt:270 — Errored ToolResult (isError=true) is rendered as a successful OutputAvailable card
- area: ui | reviewer: silentbug | category: error-masking
- The StreamEvent.ToolResult handler (lines 265-285) decides UI state with only `val deniedOutput = event.output as? ToolResultOutput.ExecutionDenied` and maps deniedOutput!=null -> OutputDenied else -> OutputAvailable. It completely ignores `event.isError`. A tool result that succeeded-to-return but signals an error (output type Error / ErrorJson, or Content(isError=true)) therefore renders as ToolCallState.OutputAvailable with error=null. This is reachable through normal usage: an MCP tool returning `isError:true` becomes ToolResultOutput.Content(isError=true) (MCP.kt:777-780), the agent emits it as a Success ToolResult with isError=true (ToolLoopAgent.kt applyToolResult, line 1366-1375) — NOT as a ToolError — so it lands here. Note also that the OutputDenied branch is effectively dead for agent-emitted events because `event.output` is derived via toolResultOutputFromJson, which only ever yields Text/Json, never ExecutionDenied (denials arrive via the dedicated ToolOutputDenied event instead). Net effect: a failed tool call shows the user a green success card; renderers that branch on `part.state` (the documented pattern in UIMessagePart.kt) never see the failure, and `error` is null so there is nothing to show. Fix: branch on isError, e.g. `state = when { deniedOutput != null -> OutputDenied; event.isError -> OutputError; else -> OutputAvailable }` and populate `error` from the output text (e.g. `with(ToolResultOutputs){ event.modelOutput.toJsonElement() }` stringified) when isError. No UI converter test covers this path.
- verify: Confirmed against current code. In MessageStreamReader.kt:265-285 the StreamEvent.ToolResult handler decides UI state solely from `event.output as? ToolResultOutput.ExecutionDenied` (line 270) -> OutputDenied else OutputAvailable, and sets error only from `deniedOutput?.reason`. It never reads `event.isError`. The chain is fully reachable: MCP.kt:777-780 maps an MCP `isError:true` result to ToolResultOutput.Content(isError=true) (wired as toModelOutput at MCP.kt:410); ToolExecutionResult.kt:23 + Tool.kt:858 make Success.isError=true; ToolLoopAgent.kt:1365-1374 emits this via the Success branch


## MEDIUM (29)

### M1. ToolLoopAgent.kt:857 — closeModelCall (telemetry span) skipped when generate() is the collector and the model throws
- area: agent | reviewer: codereview | category: Resource Leak (Telemetry Span)
- In streamInternal's 'catch (t: Throwable)' handler the order is: (1) dispatcher.emitError, (2) emit(StreamEvent.Error(...)), (3) closeModelCall, (4) finalMessagesRef?.value = ..., (5) return@flow. When generate() is the collector, its StreamCapture unconditionally throws UiMessageStreamError for StreamEvent.Error events (ToolLoopAgent.kt line ~435). Kotlin Flow propagates a collector exception back to the upstream emit call, so the emit(StreamEvent.Error(...)) call on line 857 itself throws UiMessageStreamError. Lines 858–860 (closeModelCall, finalMessagesRef assignment, return@flow) are unreachable. For any agent configured with telemetry (feed != null), the model-call span opened by onModelCallStart is permanently leaked on every model-level error when the caller uses generate() rather than stream(). The stream() path is not affected because a typical user collector does not throw on Error events. Fix: wrap the emit in try-finally so closeModelCall and finalMessagesRef are always executed: try { emit(StreamEvent.Error(...)) } finally { closeModelCall(FinishReason.Error, stepRawFinishReason); finalMessagesRef?.value = messages.toList() }; return@flow
- verify: Confirmed against current code. ToolLoopAgent.kt:855-860 orders the catch(t) handler as emitError(856), emit(StreamEvent.Error)(857), closeModelCall(858), finalMessagesRef(859), return(860). The model-call telemetry span opens at 736-738 (onModelCallStart) outside the try(739); the ONLY paired close is the per-step local closeModelCall (720-734, fires onModelCallFinish at 722), and there is no outer try-finally in streamInternal (single flow{} from 515). When generate() is the collector, its StreamCapture throws UiMessageStreamError on StreamEvent.Error (line 435; UiMessageStreamError is an Ai

### M2. ToolLoopAgent.kt:467 — generate() throws NoOutputGeneratedError on a pending-approval pause for structured-output agents, discarding pendingApprovals
- area: agent | reviewer: silentbug | category: control-flow / approval
- generate() builds the result as GenerateResult(output = decodeFinalOutput(output, text, finishReason), ... pendingApprovals = collectedApprovals.toList(), messages = collectedMessages). decodeFinalOutput (486-494) throws NoOutputGeneratedError whenever output != null and finishReason != Stop. When the loop pauses on a tool approval, the final finishReason is ToolApprovalRequested (set at 1090-1094, carried to the emitted Finish at 1146). Because decodeFinalOutput is evaluated as the first constructor argument, it throws before GenerateResult is built, so the collectedApprovals and collectedMessages that were correctly gathered (424-434) are never returned — the flow just throws.

Why it matters: for any agent that combines structured Output with an approval-gated tool, the documented approval/resume contract (return pendingApprovals, host re-calls generate with the approval response) is impossible via generate(): the host gets an exception instead of the approvals and cannot resume. stream() works fine, so generate() and stream() disagree on a core flow. The throw is intended for length/step-cap exhaustion (per the doc comment) but wrongly also fires for the legitimate approval pause.

Fix: treat ToolApprovalRequested as a non-terminal-output case — when pendingApprovals is non-empty (or finishReason == ToolApprovalRequested), skip output decoding and emit GenerateResult with a placeholder/no output and the populated pendingApprovals, rather than calling decodeFinalOutput.
- verify: Confirmed against current code. In generate() (ToolLoopAgent.kt:467-477) the GenerateResult is built with `output = decodeFinalOutput(output, text, finishReason)` as the FIRST constructor argument (line 468). decodeFinalOutput (486-494) throws NoOutputGeneratedError whenever `output != null` (structured-output agent; the property at line 68) AND `finishReason != FinishReason.Stop`. On an approval pause, effectiveFinishReason is set to ToolApprovalRequested (1090-1094), carried into lastFinishReason (1116) and emitted as the terminal StreamEvent.Finish (1146); generate()'s collector then record

### M3. ToolLoopAgent.kt:857 — Model-call telemetry span leaks on generate() error paths — closeModelCall is skipped despite the "called on EVERY exit" invariant
- area: agent | reviewer: silentbug | category: telemetry bracket leak
- closeModelCall is documented (716-719) to run on every exit from the model stream so a span-pairing integration never leaks an open onModelCallStart span. On the model-failure catch the order is emit(StreamEvent.Error(...)) (857) THEN closeModelCall(...) (858). When the agent is driven by generate(), the capture collector throws UiMessageStreamError synchronously on StreamEvent.Error (435), so the emit() at 857 re-raises that exception and lines 858-859 never execute — onModelCallFinish is never fired for the step whose onModelCallStart fired at 736-738. The same shape exists in the mid-stream provider-error path: emit(event) at 807 throws via the collector before `throw TerminalModelStreamError()` at 808, so the catch at 827 (which holds closeModelCall at 840 and the TOOL-002 partial-content preservation at 831-837) is never entered. The underlying error IS surfaced (emitError + the thrown UiMessageStreamError), so this is not a hidden error — but it is a real observability defect: any telemetry integration that pairs model-call start/finish leaks an unclosed span on every generate() error when telemetry is configured (feed != null), directly contradicting the code's own invariant.

Fix: close the model-call bracket before surfacing the error — call closeModelCall(FinishReason.Error, stepRawFinishReason) (and set finalMessagesRef) BEFORE emit(StreamEvent.Error(...)); likewise close the bracket before/at the StreamEvent.Error->TerminalModelStreamError handoff so the generate() collector's synchronous throw cannot skip it.
- verify: Confirmed against current code. closeModelCall (ToolLoopAgent.kt:720) is the ONLY firer of onModelCallFinish (722); onModelCallStart fires once at 737. In generate(), streamInternal (plain flow {} at 515) is collected directly (460-463) with captureCollector.consume -> StreamEvent.Error throws UiMessageStreamError (435). StreamCapture.consume (ToolLoopAgentInternals.kt:22) re-throws with no guard, and there is no buffer/flowOn, so emit is synchronous. Path A (transport throw, catch Throwable at 855): emit(StreamEvent.Error) at 857 invokes the throwing collector, the UiMessageStreamError unwind

### M4. StreamObjectResult.kt:109 — decodeOrThrow discards the underlying decode/validation exception and drops available result context
- area: generation | reviewer: silentbug | category: error-context
- `decodeOrThrow` (lines 109-115) uses `runCatching { output.decode(text) }.getOrNull()` for both the primary and the repaired attempt, then throws `NoObjectGeneratedError("Object stream produced no parseable object", text = text)`. The actual exception explaining WHY the JSON failed (kotlinx SerializationException with offset, or a missing-required-field / InvalidResponseDataError from Output.decode) is thrown away by `getOrNull()` and never attached. `NoObjectGeneratedError` (AiSdkError.kt:137-144) accepts `cause`, `response`, `usage`, and `finishReason`, and `finish()` already has `usage`, `finishReason`, `response`, and `warnings` in scope at the call site (line 104) — but none are threaded in. A consumer therefore gets a generic 'no parseable object' with no parser message, no finish reason, and no usage, making a malformed-output failure very hard to diagnose. Fix: capture the first decode exception (e.g. `val cause = runCatching { output.decode(text) }.exceptionOrNull()`), and have `finish()` pass `cause`, `usage`, `finishReason`, and `response` into the `NoObjectGeneratedError` it ultimately throws.
- verify: Confirmed against current code at /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/StreamObjectResult.kt:109-115. decodeOrThrow does exactly what the finding describes: line 110 `runCatching { output.decode(text) }.getOrNull()?.let { return it }` (primary) and line 112 the same for the repaired text both discard the thrown exception via getOrNull(), then line 114 throws `NoObjectGeneratedError("Object stream produced no parseable object", text = text)` passing only message + text. The discarded exceptions are genuinely informative: Output.kt shows Obj.dec

### M5. UrlOps.kt:20 — encode() produces malformed percent-encoding for non-ASCII input
- area: json-wire | reviewer: codereview | category: correctness
- The guard `char.isLetterOrDigit()` uses Kotlin/Unicode semantics, not RFC 3986's unreserved-character set (US-ASCII only). When the function iterates over the raw UTF-8 bytes of a non-ASCII string, leading bytes in the range 0xC0–0xFF map to Latin-1 supplement codepoints (U+00C0–U+00FF), many of which are Unicode letters. Those bytes pass through unencoded.

Concrete example: `"é".encodeToByteArray()` = `[0xC3, 0xA9]`. Byte 0xC3 → `Char(195)` = `'Ã'` → `'Ã'.isLetterOrDigit() = true` → appended raw. Produces `Ã%A9` instead of `%C3%A9`. The resulting query string is silently malformed.

Callers that trigger this in production: `params.userId` and `params.tags.joinToString(",")` in `KtorGatewayTransport.kt` lines 321 and 325, plus the generic `(key, value)` loops in `OpenAICompatibleHttp.kt`, `OpenAIProvider.kt`, `DeepgramProvider.kt`, and `ElevenLabsProvider.kt`.

Fix — change line 20 from:
```kotlin
if (char.isLetterOrDigit() || char in setOf('-', '_', '.', '~')) {
```
to:
```kotlin
if (unsigned < 128 && char.isLetterOrDigit() || char in setOf('-', '_', '.', '~')) {
```
or equivalently use explicit ASCII ranges: `char in 'A'..'Z' || char in 'a'..'z' || char in '0'..'9'`.
- verify: Confirmed by reading /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/UrlOps.kt:15-27 and reproducing with kotlinc. UrlOps.encode percent-encodes URL query values by iterating UTF-8 bytes, but the unreserved-char guard at line 20 uses char.isLetterOrDigit() (Unicode semantics) instead of an ASCII-only test. For multibyte UTF-8 input, lead/continuation bytes in 0xC0-0xFF map via unsigned.toChar() to Latin-1-supplement codepoints (U+00C0-U+00FF), many of which are Unicode letters, so they pass through unencoded. Empirically: 195.toChar().isLetterOrDigit()==

### M6. UrlOps.kt:82 — parseUrl() mis-parses IPv6-literal + userinfo, bypassing private-IP SSRF guard
- area: json-wire | reviewer: codereview | category: security
- For a URL of the form `http://user@[::1]/path`, `authority` becomes `"user@[::1]"`. The else branch at line 82 calls `authority.substringAfter('@')` which yields `"[::1]"`, then `.substringBefore(':')` stops at the very first colon — the one inside the brackets — returning `"["` instead of `"[::1]"`. `ParsedUrl.hostname` is stored as `"["`.

In `validateDownload` (line 58): `"[".lowercase().trim('[', ']') = ""`. Then:
- `isIPv4("")` returns false (split gives 1 part, not 4)
- `isPrivateIPv6("")` returns false (empty string matches no private-prefix)
- The localhost name check also misses it

Result: `validateDownload("http://user@[::1]/path")` does not throw, silently allowing a download to the IPv6 loopback. The same bypass works for any private IPv6 address (`fc00::/7`, `fe80::/10`, `::`) when prefixed with any userinfo.

The tests in `GatewayAndProviderUtilsParityTest` do not cover this case, so the bug is undetected.

Fix — replace lines 79–83 with:
```kotlin
val afterUserinfo = authority.substringAfter('@')
val host = if (afterUserinfo.startsWith("[")) {
    afterUserinfo.substringBefore(']') + "]"
} else if (authority.startsWith("[")) {
    authority.substringBefore(']') + "]"
} else {
    afterUserinfo.substringBefore(':')
}
```
or more concisely:
```kotlin
val hostPart = authority.substringAfter('@')
val host = if (hostPart.startsWith("[")) {
    hostPart.substringBefore(']') + "]"
} else {
    hostPart.substringBefore(':')
}
```
- verify: Confirmed by reading UrlOps.kt and an empirical Kotlin repro. Line 82 is exactly `authority.substringAfter('@').substringBefore(':')`. For `http://user@[::1]/path`, authority=`user@[::1]`; the bracket check at line 79 tests the whole authority (starts with 'u'), so it falls to the else branch and produces hostname `"["`. In validateDownload, `"[".trim('[',']')`="" , and isIPv4("")/isPrivateIPv6("")/localhost checks all return false, so no DownloadError is thrown. The no-userinfo control `http://[::1]/path` correctly parses to `[::1]` and IS blocked, proving the userinfo prefix is the bypass. T

### M7. FixJson.kt:287 — fixJson silently drops the last valid escape sequence from a truncated string (partial-JSON data corruption)
- area: json-wire | reviewer: silentbug | category: silent-data-corruption / partial-json-repair
- WHAT: The "SER-003" block (code lines 287-291, comment 282-286) does `result = result.dropLast(2)` whenever a drained INSIDE_STRING frame's `result` ends with `\<char>`. Its stated premise — that closing the string there yields malformed JSON like `\""` — is FALSE. `\"`, `\\`, `\n` etc. are COMPLETE, valid JSON escapes; closing WITHOUT dropping produces valid JSON.

EVIDENCE (extracted the exact scanner and ran it; cross-checked validity with a JSON parser): input `{"q":"ab\"` -> this code yields `{"q":"ab"}` (value `ab`, the escaped quote is gone), whereas the undropped close `{"q":"ab\""}` is VALID and parses to `ab"` (this is what upstream v6 yields). Likewise `"ab\\` -> `"ab"` (loses the backslash; correct is `"ab\\"` -> `ab\`), and `"ab\n` -> `"ab"` (loses the newline; correct is `"ab\n"`). So ANY partial JSON whose string ends exactly at a complete escape silently loses that character.

WHY IT MATTERS: parsePartialJson feeds streaming structured-output rendering. During active streaming the loss self-heals on the next delta, but (a) a stream truncated right after an escape, or (b) any one-shot parsePartialJson on such text, returns permanently wrong content with no error — a silent corruption. It also violates this file's explicit "faithful/byte-identical upstream port" contract and is untested (FixJsonTest only covers the bare-dangling-backslash case at line 46, which takes a different path).

FIX: Remove the SER-003 block (lines 287-291) entirely. The only case where dropping is correct — input ending in a lone dangling `\` — does NOT need it, because lastValidIndex never includes a dangling backslash (verified: that case closes correctly as `"..."` without the block, matching FixJsonTest line 46). Add regression vectors for inputs ending in `\"`, `\\`, and `\n`.
- verify: CONFIRMED REAL. FixJson.kt:287-291 (comment 281-286) does `result = result.dropLast(2)` when a draining INSIDE_STRING frame's `result` ends with `\<char>`. The stated premise — that closing there yields malformed `\""` — is provably false: `"ab\""` is valid JSON parsing to `ab"`.

Hand-traced the scanner on `{"q":"ab\"`: the `\` (i=8) enters INSIDE_STRING_ESCAPE; the `"` (i=9) pops it and sets lastValidIndex=9, so result=`{"q":"ab\"` with INSIDE_STRING still open. At drain SER-003 fires (result[8]=='\\'), drops `\"`, then closes -> `{"q":"ab"}` (value `ab`; escaped quote silently lost). Same f

### M8. middleware/ExtractJson.kt:155 — resolvePrefix unconditionally sets phase=Streaming for non-JSON code fences, leaking fence headers into streamed output
- area: middleware | reviewer: codereview | category: Logic error / refactor bug
- In `resolvePrefix` Case 2 (lines 149–156), `block.phase = TextPhase.Streaming` is placed **outside** the `if (prefixMatch != null)` guard. `FENCE_PREFIX` (`^```(?:json)?\s*\n`) only matches ```` ```json\n ```` and ```` ```\n ```` — any other language tag (```` ```python\n ````, ```` ```sh\n ````, etc.) yields `prefixMatch = null`, leaving the buffer with the fence header intact. But the phase still transitions to Streaming, so:
1. `TextStart` is emitted immediately (line 124) for an un-stripped block.
2. Once the buffer exceeds `SUFFIX_BUFFER_SIZE` (12 chars), `emitBufferedText` flushes the raw fence header as a `TextDelta` — the consumer receives ```` ```python\n{ ```` instead of `{`.
3. At `TextEnd`, `remainingText` sees `emittedContent = true` and returns `stripClosingFence(remaining)`, which only removes the trailing ```` ``` ```` — it never calls `extractAndRepairJson`/`extractJsonRegion`. The correct fallback path (fence regex fails → `scanBalanced` locates the `{`/`[` inside the block) is unreachable.

**Fix:** move `block.phase = TextPhase.Streaming` inside `if (prefixMatch != null)`. When the fence is non-JSON, the phase remains `Prefix`; at `TextEnd` the full buffer goes through `transformText` → `extractJsonRegion` → `scanBalanced`, which correctly extracts JSON from any language fence.
- verify: CONFIRMED as a genuine defect. In ExtractJson.kt resolvePrefix Case 2 (lines 149-156), `block.phase = TextPhase.Streaming` (line 155) sits OUTSIDE the `if (prefixMatch != null)` guard (lines 151-154). FENCE_PREFIX (`^```(?:json)?\s*\n`) only matches ```json\n``` and ``` \n```; verified empirically that it does NOT match ```python```, ```JSON``` (uppercase), ```js```, ```json5```, ```sh```. For any such non-json fence, prefixMatch is null so the header is never stripped and prefixStripped stays false, yet phase still becomes Streaming. Consequence chain holds: (1) TextStart emitted for the un-s

### M9. middleware/Logging.kt:69 — Terminal StreamEvent.Error is logged without its typed `cause` throwable
- area: middleware | reviewer: silentbug | category: inadequate-error-handling
- `LoggingWire.logStreamEvent` logs the terminal error as `logger.warn("[$tag] stream-error ${event.message}")` and discards `event.cause` — the `@Transient val cause: Throwable?` that `StreamEvent.Error` carries (Streaming.kt:323-327). `Logger.warn` has the signature `warn(message: String, throwable: Throwable? = null)` (Logger.kt:29), and the sibling `ToolError` branch two lines up (Logging.kt:63-67) correctly forwards `event.error` as that throwable. So the ONE event that unwinds the entire loop — the most severe, the one a developer most needs to debug — is the only error whose stack trace / typed cause is dropped from the logs.

Why it matters: this middleware is the documented home for routing observability into a host's `Logger` adapter (Logcat / Crashlytics / Sentry / os_log per the file header, lines 18-20). A Crashlytics/Sentry-style sink needs the `Throwable` to group, symbolicate, and attach a stack trace; given only a message string it cannot. The terminal error therefore becomes the hardest failure to diagnose 6 months out, defeating the purpose of the logging middleware for exactly the case that matters most. (The cause is not globally lost — it still rides the typed `StreamEvent.Error` channel downstream — which is why this is medium, not a hard swallow; but the host that relies on the Logger gets a degraded, stack-trace-less record.)

Note: this line was untouched by the refactor (the same drop existed pre-`refactor/ts-residue-cleanup`), but the refactor converted this `when` to exhaustive arms while explicitly enumerating every event, so the gap was reviewed and left in place.

Fix: pass the cause through, mirroring the ToolError branch:
`is StreamEvent.Error -> logger.warn("[$tag] stream-error ${event.message}", event.cause)`
- verify: Confirmed against current code. Logging.kt:68-69 logs `is StreamEvent.Error -> logger.warn("[$tag] stream-error ${event.message}")`, dropping the `@Transient val cause: Throwable?` that StreamEvent.Error carries (Streaming.kt:323-327). Logger.warn DOES accept a throwable: `warn(message: String, throwable: Throwable? = null)` (Logger.kt:29). The sibling ToolError branch two lines up (Logging.kt:63-67) correctly forwards its typed throwable `event.error` (AgentError, which extends AiSdkException -> Throwable). So the asymmetry is real: both branches call logger.warn, both have a @Transient typed

### M10. providers/GoogleInteractionsModel.kt:870 — googlePollInteraction never checks the result of the last poll iteration
- area: providers-anthropic-google | reviewer: codereview | category: logic-bug
- After the `repeat(maxAttempts)` loop, the `current` variable holds the freshest poll response, but the function immediately throws `InvalidResponseDataError` without checking whether that last response is terminal. The terminal check is only performed at the TOP of each iteration (against the result obtained in the PREVIOUS iteration). So if the interaction reaches a terminal state on the very last poll attempt, the function still throws a timeout error instead of returning success.

Concrete trace with maxAttempts=N:
  initial poll → current0
  iter 0: check current0 → not done → delay → current1
  …
  iter N-1: check currentN-1 → not done → delay → currentN
  << loop exits >>
  throw timeout  ← currentN is never inspected

The video-polling path in GoogleMediaModels.kt correctly adds `if (current["done"] != true) throw ...` AFTER the loop. The interactions path is missing that post-loop guard.

Fix: after the `repeat` block ends, add `if (googleInteractionsTerminal(current.value.jsonObject["status"]?.jsonPrimitive?.contentOrNull)) return current` before the throw, mirroring the video-polling pattern.
- verify: Confirmed genuine off-by-one defect. In src/commonMain/kotlin/ai/torad/aisdk/providers/GoogleInteractionsModel.kt:870-875, googlePollInteraction checks terminal status at the TOP of each repeat iteration (line 871, against the previously-fetched value), fetches a fresh `current` at the BOTTOM (line 873), then throws InvalidResponseDataError unconditionally (line 875). With maxAttempts=N this does N+1 fetches (current0 initial + current1..currentN in-loop) but only N terminal checks (current0..current(N-1)); the final fetch currentN is never inspected. The finding's trace is exact. The asymmetr

### M11. providers/AlibabaProvider.kt:331 — alibabaVideoInput puts raw base64 bytes into the img_url URL field
- area: providers-anthropic-google | reviewer: codereview | category: correctness
- In `AlibabaWire.alibabaVideoInput`, when `mode == ImageToVideo` and `params.image` is non-null, the code does:

    put("img_url", JsonPrimitive(it.url ?: it.base64))

If `it.url` is null, the raw base64-encoded image bytes are used as the field value. The Alibaba DashScope i2v API's `img_url` field expects an HTTP URL, not base64 data. Sending binary data here causes an immediate API-level rejection.

The pattern already seen in other providers (e.g., `generateGeminiImage`) is to throw `UnsupportedFunctionalityError` when only base64 is available and the API requires a URL.

Fix: replace the null-coalescing fallback with an explicit guard:
    params.image?.let {
        val url = it.url ?: throw UnsupportedFunctionalityError(
            "imageToVideo-base64",
            "Alibaba i2v requires an image URL; inline base64 is not supported."
        )
        put("img_url", JsonPrimitive(url))
    }
- verify: CONFIRMED defect, but the finding mischaracterizes the cause/fix. AlibabaProvider.kt:331 does `put("img_url", JsonPrimitive(it.url ?: it.base64))`. GeneratedFile.url is nullable and base64 is required (MediaModels.kt:7-13), so when url is null the RAW base64 string (no `data:` prefix) is emitted into img_url. The path is reachable (generate() line 158 → alibabaVideoInput; no image preprocessing upstream). Per the official Alibaba Cloud Model Studio (DashScope) docs for this exact legacy i2v endpoint, img_url accepts EITHER a public HTTP/HTTPS URL OR a Base64 string — but the Base64 MUST be in 

### M12. providers/GoogleWire.kt:260 — googleToolConfig early-returns retrievalConfig and silently ignores ToolChoice.None
- area: providers-anthropic-google | reviewer: codereview | category: logic-bug
- `googleToolConfig` starts with:

    options["retrievalConfig"]?.let { retrieval ->
        return buildJsonObject { put("retrievalConfig", retrieval) }
    }

When `retrievalConfig` is present, the function exits immediately with only `{ retrievalConfig: ... }`. The subsequent `when (choice)` block — which handles `ToolChoice.None` by emitting `{ functionCallingConfig: { mode: "NONE" } }` — is never reached.

Result: if a caller sets both `providerOptions.google.retrievalConfig` and `toolChoice = ToolChoice.None`, function calling is NOT disabled on the Google API. The tools list is still sent (because `googleToolsJson` does not filter for `ToolChoice.None`) and the model can freely invoke them, directly contradicting the caller's stated intent.

Fix: merge both concerns instead of early-returning, e.g.:
    fun googleToolConfig(choice: ToolChoice, options: JsonObject): JsonObject? = buildJsonObject {
        options["retrievalConfig"]?.let { put("retrievalConfig", it) }
        val modeConfig = when (choice) {
            ToolChoice.None -> buildJsonObject { put("mode", JsonPrimitive("NONE")) }
            ToolChoice.Required -> buildJsonObject { put("mode", JsonPrimitive("ANY")) }
            is ToolChoice.Specific -> buildJsonObject { ... }
            else -> null
        }
        modeConfig?.let { put("functionCallingConfig", it) }
    }.takeIf { it.isNotEmpty() }
- verify: Confirmed against current code at src/commonMain/kotlin/ai/torad/aisdk/providers/GoogleWire.kt:259-281. `googleToolConfig` early-returns `{retrievalConfig: ...}` at lines 260-262 whenever `options["retrievalConfig"]` is present, making the `when (choice)` block at 263-280 unreachable. That block is the only place `functionCallingConfig` (mode NONE/ANY/allowedFunctionNames) is emitted for Google.

Genuine defect, not intended and not handled elsewhere:
- The Google API `ToolConfig` is a protobuf with `functionCallingConfig` (field 1) and `retrievalConfig` (field 2) as independent OPTIONAL field

### M13. providers/AlibabaProvider.kt:261 — Alibaba embedding vector silently coerces non-numeric/null elements to 0f
- area: providers-anthropic-google | reviewer: silentbug | category: silent-failure
- `item.jsonObject["embedding"]?.jsonArray.orEmpty().map { it.jsonPrimitive.floatOrNull ?: 0f }` substitutes 0f whenever a vector element is a JSON null or a non-numeric primitive. An embedding is the model's core output; silently replacing a value with 0f corrupts the vector and produces subtly-wrong similarity results with no error surfaced (monitoring stays green). This is inconsistent with every other embedding decoder in the codebase: Cohere (CohereProvider.kt:177) and Google (GoogleMediaModels.kt:70,73) both use `WireDecoder.embeddingFloat`, which throws a typed WireDecodeException on a non-numeric element. Fix: decode the vector with `WireDecoder.embeddingVector(item.jsonObject["embedding"] ?: WireDecoder.fail(...), provider)` (or map each element through `WireDecoder.embeddingFloat`) so a malformed element fails loudly instead of fabricating 0f.
- verify: Confirmed against current code. AlibabaProvider.kt:261 is exactly `item.jsonObject["embedding"]?.jsonArray.orEmpty().map { it.jsonPrimitive.floatOrNull ?: 0f }`. A JSON null (JsonNull is a JsonPrimitive, so `.jsonPrimitive` succeeds) or a non-numeric primitive yields `floatOrNull == null`, and `?: 0f` silently substitutes 0f into the embedding vector. Since 0f is a legal embedding value, this corrupts the vector and degrades similarity results with no error surfaced — a genuine silent-data-corruption defect. The consistency claim is accurate and actually broader than stated: EVERY other embedd

### M14. providers/RevaiProvider.kt:277 — mapRevaiTranscript accumulates inter-word punctuation into the next segment's text field
- area: providers-audio-misc | reviewer: codereview | category: correctness
- Line 277 appends every element's value to currentText regardless of type. Segments are only emitted on elements of type "text" (line 278), so "punct" elements (commas, periods, spaces — all standard in Rev.ai's response) that fall between two words accumulate in currentText and are prepended to the following word's segment text.

Concrete example with a Rev.ai response whose elements are [text("Hello",0-0.5), punct(","), text("World",0.6-1.0)]:
  - After "Hello" segment is created, currentText is reset to "".
  - punct(",") → currentText = ",".
  - text("World") → currentText = ",World" → segment text = ",World".

The top-level TranscriptionModelResult.text (built separately at line 265) is correct; only TranscriptSegment.text values are affected.

Fix: only accumulate text for elements whose type is "text", moving the `currentText +=` inside the `if (type == "text")` block, or alternatively emit the accumulated punctuation as part of the current (not next) segment before resetting.
- verify: Confirmed against current code. In mapRevaiTranscript (RevaiProvider.kt:275-294), line 277 (`currentText += obj["value"]...`) appends every element's value unconditionally, while segment emission is gated on type=="text" (line 278). So a "punct" element's value (e.g. a comma) accumulates into currentText and is prepended to the next text element's segment. Trace of [text("Hello",0.1-0.3), punct(","), text("world",0.4-0.8)]: after "Hello" is emitted, currentText="," from the comma, then "world" makes currentText=",world"; .trim() (line 288) only strips whitespace, so segment text = ",world". Th

### M15. providers/AssemblyAIProvider.kt:223 — pollTranscript uses an unbounded while(true) loop with no maximum-iteration guard
- area: providers-audio-misc | reviewer: codereview | category: correctness
- Both RevaiProviderSettings and GladiaProviderSettings carry a maxPollAttempts field (default 60) that bounds their polling loops. AssemblyAIWire.pollTranscript (line 223) uses `while (true)` with no upper bound. For a stuck job — where the AssemblyAI API keeps returning "queued" or "processing" indefinitely — the coroutine will hang forever unless the caller independently sets an AbortSignal with a timeout. AssemblyAIProviderSettings has no maxPollAttempts, and there is no compile-time or documentation reminder that a timeout is required.

Fix: add `val maxPollAttempts: Int = 120` to AssemblyAIProviderSettings (120 × 3 s = 6 min, appropriate for long audio) and convert pollTranscript to the same bounded repeat pattern used by Gladia and RevAI, throwing NoTranscriptGeneratedError on exhaustion.
- verify: Confirmed against current code. AssemblyAIProvider.kt:223 `AssemblyAIWire.pollTranscript` is a `while (true)` loop whose only exits are status-based (completed→return, error/unsupported→throw) plus `abortSignal.throwIfAborted()` (line 224); for "queued"/"processing" it merely `delay`s and loops, with no iteration counter or deadline. AssemblyAIProviderSettings (lines 74-78) has only apiKey/headers/pollingIntervalMillis — no maxPollAttempts. By contrast RevaiProviderSettings.maxPollAttempts=60 (RevaiProvider.kt:65) bounds its loop via repeat(...) and throws "polling timed out" (line 137), and G

### M16. providers/GoogleVertexProvider.kt:331 — GoogleVertexXai silently drops the `xHandles` search-parameter (and any leading-uppercase key) because its snake-case helper diverges from the tested xAI one
- area: providers-audio-misc | reviewer: silentbug | category: silent-failure
- `googleVertexXaiSnakeCaseKey` (GoogleVertexProvider.kt:331-341), used to transform `searchParameters` at :285, naively snake-cases every key. The native xAI path uses `XaiWire.xaiSnakeCaseKey` (XaiProvider.kt:413-420), which special-cases the deprecated alias `xHandles` -> `included_x_handles` because, per the comment there, naive `x_handles` is 'an unknown key xAI ignores.' This is explicitly locked down by a test: XaiProviderTest.kt:203-204 asserts the result must be `included_x_handles` and that `x_handles` must be absent. The Vertex copy lacks that special-case, so a user who sets `providerOptions.xai.searchParameters.sources[].xHandles` on the GoogleVertexXai path has it silently rewritten to `x_handles` and ignored by xAI — the live search runs unrestricted and returns results from all handles, with no warning or error. Why it matters: a user-specified search filter is silently discarded, producing wrong results that the user cannot detect. Secondary defect in the same function: it `append('_')` for an uppercase char unconditionally, whereas the xAI version guards with `if (index > 0)`; any PascalCase key would gain a spurious leading underscore (`_from_date`) and likewise be silently ignored. Fix: have the Vertex-xAI path reuse `XaiWire.xaiSnakeCaseJson`/`xaiSnakeCaseKey` instead of maintaining a second, drifting copy (or, at minimum, add the `xHandles` special-case and the `index > 0` leading-underscore guard to `googleVertexXaiSnakeCaseKey`).
- verify: Confirmed against current code. GoogleVertexProvider.kt:331-341 `googleVertexXaiSnakeCaseKey` naively snake-cases every key with (a) no `xHandles`->`included_x_handles` special-case and (b) an unconditional `append('_')` for uppercase chars. It is used at :285 via `googleVertexXaiSnakeCase` (:322-329) to transform `searchParameters`, and the GoogleVertexXai delegate invokes this transform on generate/stream/streamResult (:204/207/210), so the path is live. The native xAI helper `XaiWire.xaiSnakeCaseKey` (XaiProvider.kt:413-420) DOES special-case `xHandles` (comment: naive `x_handles` is 'an un

### M17. providers/FalProvider.kt:179 — FalSpeechModel hardcodes https://fal.run instead of using settings.baseURL
- area: providers-bedrock-media | reviewer: codereview | category: correctness
- FalSpeechModel.generate submits to the hardcoded URL "https://fal.run/$modelId" (line 179) while FalImageModel correctly uses "${settings.baseURL.trimEnd('/')}/$modelId". FalProviderSettings.baseURL defaults to "https://fal.run" so in the default case the behaviour is identical, but any caller that sets a custom baseURL (e.g. a test proxy, a Fal-compatible self-hosted endpoint, or a staging gateway) will find that speech generation silently bypasses the override and still calls fal.run directly. This is a refactor-introduced inconsistency — every other Fal model type respects the setting. Fix: change line 179 to url = "${settings.baseURL.trimEnd('/')}/$modelId".
- verify: CONFIRMED defect. FalProvider.kt:179 (FalSpeechModel.generate) hardcodes url = "https://fal.run/$modelId", while FalImageModel.generate (line 135) uses "${settings.baseURL.trimEnd('/')}/$modelId". FalProviderSettings.baseURL defaults to "https://fal.run" (line 54), so the default path is byte-identical, but a caller setting a custom baseURL has speech silently routed to fal.run while image honors the override. baseURL is provably meant to be overridable: the image test sets baseURL="https://fal.test" (FalProviderTest.kt:67) and asserts the request hits "https://fal.test/fal-ai/qwen-image" (lin

### M18. providers/ReplicateProvider.kt:137 — Replicate image generation ignores prediction status/error and reports a misleading "missing output"
- area: providers-bedrock-media | reviewer: silentbug | category: error-masking / wire-parsing hides upstream error
- The image path sends `prefer: wait` and reads `response.value.jsonObject["output"] ?: throw NoImageGeneratedError("Replicate image response is missing output")` (lines 137-138). It never inspects the prediction `status`/`error`. When a prediction terminates as `status=="failed"` (e.g. NSFW filter, invalid input, model crash) the body carries the real reason in `error` but `output` is null/absent, so the user gets the generic "missing output" and the upstream cause is discarded. The same misleading message appears when the wait window elapses and the prediction is still `processing`. Note the video path (replicatePollVideoPrediction, lines 393-396) does this correctly. Fix: before the output check, branch on replicateStatus(prediction): `"failed" -> throw NoImageGeneratedError("Replicate image generation failed: " + (error ?: "Unknown error"))`, `"canceled" -> throw ...`, and surface a still-processing state distinctly from a truly absent output.
- verify: Confirmed against current code in src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt.

(1) Code matches exactly. Lines 137-138 read `val output = response.value.jsonObject["output"] ?: throw NoImageGeneratedError("Replicate image response is missing output")` with no preceding inspection of `status`/`error` between the POST (lines 120-136) and the output read. The image request sends `prefer: wait` via line 134 -> replicatePreferHeader (lines 299-302), verified by the test at line 75 (`assertEquals("wait=5", ...)`).

(2) Genuine defect, not handled elsewhere. With `prefer: wai

### M19. providers/ProdiaProvider.kt:273 — Prodia input-image URL is downloaded without an HTTP status check, sending an error body as image bytes
- area: providers-bedrock-media | reviewer: silentbug | category: silent-failure / ignored HTTP status
- prodiaInputFile() does `file.url?...?.let { client.request(it).bodyAsBytes() } ?: Base64Codec.decode(file.base64)`. The Ktor client in this SDK is not configured with expectSuccess (every other download helper checks status manually), so a 404/500/403 on the input URL does NOT throw — `bodyAsBytes()` returns the HTML/JSON error page, which is then uploaded as the multipart `input` image/video. The user sees a confusing downstream Prodia rejection (or a garbage result) instead of "input image download failed". Every sibling helper guards this: falGetBinary (FalProvider.kt:599), bflDownloadImage (BlackForestLabsProvider.kt:294), replicateDownloadImage (ReplicateProvider.kt:345), Luma downloadImage (LumaProvider.kt:259). Fix: fetch via the same pattern and throw ApiCallError when `response.status.value !in 200..299` before reading bytes.
- verify: Confirmed against current code. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/ProdiaProvider.kt:271-276, prodiaInputFile() does `file.url?.takeIf { it.isNotBlank() }?.let { client.request(it).bodyAsBytes() } ?: Base64Codec.decode(file.base64)` — line 273 reads bodyAsBytes() with no HTTP status check. The SDK never configures Ktor's expectSuccess (grep for "expectSuccess" across the repo returns zero hits; every production HttpClient is user-injected via constructor, and Ktor defaults expectSuccess=false), so a 404/500/403 on the input URL 

### M20. providers/ByteDanceProvider.kt:305 — ByteDance poll masks non-succeeded/failed terminal states as a generic timeout
- area: providers-bedrock-media | reviewer: silentbug | category: error-masking / failure surfaced as timeout
- byteDancePoll() only handles `"succeeded" -> return` and `"failed" -> throw` (lines 305-308). Any other terminal status — notably `"cancelled"` (a documented Ark task state), or an unrecognized/error status — falls through, the loop delays and re-polls, and the task never changes, so after the full pollTimeoutMs (default 300_000 ms = 5 minutes) the user gets `NoVideoGeneratedError("...timed out after 300000ms")`. That hides the real terminal state behind a 5-minute wait and a wrong diagnosis. KlingAI handles this correctly with an explicit `else -> throw InvalidResponseDataError(...)` (KlingAIProvider.kt:136). Fix: add `"canceled"/"cancelled" -> throw NoVideoGeneratedError("ByteDance video generation was canceled")` and an `else -> throw InvalidResponseDataError(statusBody, "Unknown ByteDance task status: <status>")` so unexpected/terminal states fail immediately with the real status.
- verify: Confirmed against current code. ByteDanceProvider.kt:305-308 is a `when` over `statusBody["status"]` with only `"succeeded" -> return` and `"failed" -> throw` and NO `else`/in-progress arms. The poll endpoint (line 300, `/contents/generations/tasks/{id}`) is the Volcengine Ark video-task endpoint. Live docs (volcengine-video-mcp, LaoZhang Seedance API, apidog) confirm its documented terminal statuses include `cancelled` AND `expired` in addition to `succeeded`/`failed`; the apidog reference loop literally breaks on ("failed","expired","cancelled"). In this code those terminal states match noth

### M21. providers/OpenAICompatibleStreaming.kt:39 — Stream-state error check also mishandles JsonNull and will crash the stream once the in-band detector is fixed
- area: providers-openai | reviewer: silentbug | category: wire-parsing-false-error
- `OpenAIChatStreamState.accept` does `if (obj["error"] != null) { events += StreamEvent.Error(obj["error"]?.jsonObject?.get("message")... ?: obj["error"]?.jsonPrimitive?.contentOrNull ...) }`. Same root cause as the OpenAICompatibleWire finding: for `"error": null`, `obj["error"] != null` is TRUE (JsonNull is a non-null reference), then `obj["error"]?.jsonObject` invokes `.jsonObject` on JsonNull which throws `IllegalArgumentException` (the `?.` only guards a null reference, not the JsonNull *value*), aborting the whole stream with an unrelated error. Today this is shadowed because `transformChatStreamEvent` runs `openAICompatibleInBandError` and throws first — but that means fixing the OpenAICompatibleWire finding (returning null for JsonNull) will route a `"error": null` chunk into this code and surface the crash. The two must be fixed together. FIX: `val err = obj["error"]?.takeUnless { it is JsonNull }`, branch on `err != null`, and read message via `(err as? JsonObject)` / `(err as? JsonPrimitive)?.contentOrNull` (add the JsonNull import).
- verify: CONFIRMED REAL. All four claims verified against current code.

(1) Code matches: OpenAICompatibleStreaming.kt:39-43 is exactly `if (obj["error"] != null) { events += StreamEvent.Error(obj["error"]?.jsonObject?.get("message")?.jsonPrimitive?.contentOrNull ?: obj["error"]?.jsonPrimitive?.contentOrNull ?: ...) }`.

(2) Genuine JsonNull defect: for {"error": null}, obj["error"] returns the JsonNull singleton (a non-null Kotlin reference), so `obj["error"] != null` is TRUE. The `?.` then proceeds to invoke `.jsonObject` on JsonNull; kotlinx's `jsonObject` accessor is `this as? JsonObject ?: error(

### M22. providers/TogetherAIFacade.kt:120 — Image facades fabricate empty-content GeneratedFiles when the provider omits image data
- area: providers-openai | reviewer: silentbug | category: sentinel-fabrication
- TogetherAI image: `base64 = item.jsonObject["b64_json"]?.jsonPrimitive?.contentOrNull.orEmpty()` (line 120). DeepInfra image (DeepInfraFacade.kt:110): `base64 = image.jsonPrimitive.contentOrNull.orEmpty().stripDataUriPrefix()`. When the `data`/`images` array contains items but an item has no `b64_json` (or is a JSON null), the code produces `GeneratedFile(mediaType="image/png", base64="")` — a non-empty image list of zero-byte images. WHY IT MATTERS: the higher-level `generateImage` only throws `NoImageGeneratedError` when the aggregated list is EMPTY (MediaModels.kt:289); a list containing empty-content files passes the check, so the caller receives a "successful" PNG with no bytes and no warning/error. The sibling `OpenAICompatibleImageModel` (OpenAICompatibleModels.kt:351-355) sets the correct contract: it uses `WireDecoder.requiredOneOfString(... "b64_json", "url")` and throws on empty data. FIX: mirror that — fail (e.g. `NoImageGeneratedError`/`InvalidResponseDataError`) when an item carries no usable image payload instead of defaulting base64 to "".
- verify: Confirmed against current code. TogetherAIFacade.kt:120 maps the `data` array with `base64 = item.jsonObject["b64_json"]?.jsonPrimitive?.contentOrNull.orEmpty()` (lines 117-122), and DeepInfraFacade.kt:110 maps `images` with `image.jsonPrimitive.contentOrNull.orEmpty().stripDataUriPrefix()` — both produce `GeneratedFile(mediaType="image/png", base64="")` for an array entry that lacks `b64_json` / is JSON null (no per-item payload check). GeneratedFile (MediaModels.kt:7-13) has no validation on base64. generateImage's only guard is MediaModels.kt:289 `if (images.isEmpty()) throw NoImageGenerate

### M23. ToolCallRepairer.kt:76 — Repaired-input decode failure swallowed by runCatching{}.getOrNull(), producing a misleading "repair: returned null" error
- area: tools | reviewer: silentbug | category: error-masking
- In tryRepair, when the repair function returns a corrected ToolCall, the repaired input is decoded inside `runCatching { def to decodeInput(def, corrected.input) }.getOrNull()` (line 76). If the repaired args parse as JSON but still fail schema decode (wrong shape/types, or the call was re-routed to a tool whose schema the args don't satisfy), getOrNull() discards the WireDecodeException/AgentError and returns null. The caller resolveCall (lines 46-50) then interprets that null as "repair gave up" and throws AgentError.ToolCallRepairFailed(originalError = plainError, repairError = null), which renders as "...repair: returned null" (AgentError.kt:41). WHY IT MATTERS: the repair feature exists precisely to recover from bad tool args; the second decode error is the single most useful diagnostic (it says *why the corrected args were still wrong*), and it is silently dropped. Worse, the message actively lies — it claims the repair fn returned null when it actually returned undecodable input. This same getOrNull() also collapses the legitimate "re-routed tool not in set" case (toolDef == null) into the identical opaque message. FIX: replace the runCatching/getOrNull with an explicit try/catch that rethrows CancellationException and threads the decode failure back so resolveCall populates repairError, e.g. inside tryRepair: `toolDef?.let { def -> try { def to decodeInput(def, corrected.input) } catch (ce: CancellationException) { throw ce } catch (t: Throwable) { throw AgentError.ToolCallRepairFailed(call.toolName, originalError, repairError = t) } }`, leaving the reroute-missing (toolDef == null) case as the only null return.
- verify: Confirmed against current code. ToolCallRepairer.kt:76 is exactly `runCatching { def to decodeInput(def, corrected.input) }.getOrNull()`. decodeInput delegates to WireDecoder.decode, which throws WireDecodeException on schema decode failure (WireDecoder.kt:60-63). So when the repair fn returns a non-null corrected ToolCall (line 64) whose input still fails schema decode (wrong shape/types, or it re-routed to a tool whose schema the args don't satisfy), getOrNull() swallows the exception and returns null. That null flows back to resolveCall, which throws AgentError.ToolCallRepairFailed(original

### M24. ToolApprovalCoordinator.kt:71 — Approval response that matches no pending tool call is silently dropped; purpose-built AgentError.InvalidApprovalResponse is never raised
- area: tools | reviewer: silentbug | category: silent-failure
- In applyToolApprovalResponses, an approval whose effective target call id matches no pending tool call (`callIndex == -1`) is silently `continue`d (line 71). The earlier guards also drop meaningful input silently: approvals present but no prior assistant message (line 49) or no prior tool calls (line 51) just `return null`. In every one of these branches a tool the user explicitly approved is discarded with ZERO feedback — no StreamEvent.ToolError, no StreamEvent.ToolOutputDenied, no log, no exception. The codebase defines a typed error for exactly this case — AgentError.InvalidApprovalResponse(toolCallId, knownPendingIds) "Approval response references unknown tool call" (AgentError.kt:45) — but it is never thrown in production code (only exercised by AgentErrorTest round-trip). WHY IT MATTERS: this runs on the untrusted resume path (the host replays client-supplied history, which is the whole reason the surrounding HMAC fail-closed re-validation exists). A mismatched/garbled approvalId means the approved tool never executes and the loop proceeds to the next model call with a dangling, unresolved tool call — which can wedge the model into re-requesting approval indefinitely or trip provider-side "tool_call without matching result" errors, all with no diagnostic for the host or user. FIX: when an approval correlates to no pending call, raise AgentError.InvalidApprovalResponse(targetCallId, priorToolCalls.map { it.toolCallId }) (or at minimum emit a StreamEvent.ToolError via emitToolError) instead of `continue`; apply the same surfacing to the approvals-present-but-nothing-to-match early returns at lines 49 and 51.
- verify: Confirmed by reading the actual code. In ToolApprovalCoordinator.applyToolApprovalResponses, an approval whose effective targetCallId matches no remaining pending ToolCall is silently skipped via `continue` (line 71), and the early guards silently `return null` when there is no prior assistant message (line 49) or no prior ToolCall parts (line 51). None of these branches emit StreamEvent.ToolError, StreamEvent.ToolOutputDenied, a log, or an exception — verified by reading the whole method. AgentError.InvalidApprovalResponse is defined at AgentError.kt:45 with the message "Approval response ref

### M25. RetryPolicy.kt:71 — Retry-After > 60s silently dropped to tiny backoff instead of being capped
- area: transport-telemetry | reviewer: silentbug | category: resilience/retry
- retryDelayMs() returns `if (serverMs in 0 until MAX_RETRY_AFTER_MS || serverMs < exponentialBackoffDelay) serverMs else exponentialBackoffDelay`. For a server Retry-After >= 60_000ms the first clause is false and — since exponentialBackoffDelay is <= maxDelayMs (default 2_000ms) — the second is false too, so it returns the tiny exponential backoff. Net effect: a server that says 'retry after 90s' gets retried in ~100ms-2s, almost immediately re-hitting the rate limit and burning the retry budget. The constant name MAX_RETRY_AFTER_MS clearly implies the intent was to CAP the honored wait at 60s, not to discard a long Retry-After and retry instantly. FIX: `serverMs.coerceAtMost(MAX_RETRY_AFTER_MS)` (waiting the 60s ceiling) rather than falling back to exponentialBackoffDelay. Note this is currently dead behind finding #1 (which makes serverMs null in production) and surfaces the moment the casing bug is fixed — fix both together.
- verify: CONFIRMED at /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/RetryPolicy.kt:69-76. The code matches the description exactly: retryDelayMs() returns serverMs only if `serverMs in 0 until MAX_RETRY_AFTER_MS` (0..59_999) OR `serverMs < exponentialBackoffDelay`, else exponentialBackoffDelay. MAX_RETRY_AFTER_MS=60_000 (line 88); default maxDelayMs=2_000 (line 9) and exponentialBackoffDelay is coerced to maxDelayMs (lines 26/37). For serverMs >= 60_000: first clause false (60_000 not in 0..59_999), second clause false (60_000 < 2_000 is false) -> returns the t

### M26. ui/Streams.kt:167 — validateUiMessages rejects an empty list, making setMessages(emptyList()) throw
- area: ui | reviewer: codereview | category: broken API contract
- validateUiMessages starts with `require(messages.isNotEmpty()) { "Messages array must not be empty" }`. Chat.setMessages and ChatSession.setMessages both call this validator, so chatSession.setMessages(emptyList()) and chat.setMessages(emptyList()) throw IllegalArgumentException. This blocks the common 'clear chat' operation. The inconsistency is direct: Chat(initialMessages = emptyList()) is the default constructor signature, meaning the empty-message state is valid at construction but unreachable via setMessages. The Vercel AI SDK TS original allows setMessages([]). Fix: remove the isNotEmpty check from validateUiMessages (validate only uniqueness and non-blank IDs), or add a dedicated clearMessages() method that bypasses the validator and sets messages to emptyList() directly.
- verify: Verified against current code. Streams.kt:167 — validateUiMessages opens with `require(messages.isNotEmpty()) { "Messages array must not be empty" }` (Kotlin `require` throws IllegalArgumentException). Chat.setMessages (Chat.kt:110) calls this validator before applying state, and ChatSession.setMessages (ChatSession.kt:31) delegates to chat.setMessages. So both chat.setMessages(emptyList()) and chatSession.setMessages(emptyList()) throw. The asymmetry is real: the Chat constructor defaults initialMessages = emptyList() (Chat.kt:65) and the init block does NOT validate, so the empty-message sta

### M27. ui/Chat.kt:198 — regenerate() duplicates the last user message and corrupts conversation state
- area: ui | reviewer: silentbug | category: state-corruption
- regenerate() (lines 195-199) does `val lastUser = ...lastOrNull { it.role == User }; return sendMessage(lastUser, body)`. But sendMessage's optimistic update is an unconditional append: `copy(messages = messages + message, ...)` (line 170) — NOT a withUpsert. So regenerate re-appends a message whose `id` already exists in the list, producing two messages with identical ids. This: (1) silently violates the unique-id invariant that UiMessageStreams.validateUiMessages enforces everywhere else (sendMessage never re-validates, so the dup persists undetected); (2) sends a doubled user turn to the transport/LLM (the request is built from `it.messages`, line 171); (3) never removes the stale trailing assistant message, so the model also still sees the prior answer. v6 regenerate semantics remove the trailing assistant turn and re-run from the existing last user message WITHOUT appending it again. Fix: regenerate should re-issue the request from the existing message list (optionally dropping the trailing assistant message) rather than calling sendMessage with a message already present. ChatSession.regenerate (ChatSession.kt:86) inherits this same bug via delegation; fixing Chat.regenerate fixes both.
- verify: Confirmed against current code. Chat.kt:195-199 regenerate() takes the existing last user message (already in internalState.messages) and passes it to sendMessage(). sendMessage's optimistic update at Chat.kt:170 is an unconditional append `copy(messages = messages + message, ...)` — it does NOT use withUpsert (withUpsert exists at lines 232-240 but is only applied to streamed responses at line 175). So the existing user message is re-appended, yielding two messages with identical id. All three consequences verified: (1) the duplicate violates the id-uniqueness invariant that UiMessageStreams.

### M28. ui/MessageStreamReader.kt:54 — shiftIndexesAfterRemoval leaves dataPartIndexById stale, silently overwriting the wrong part
- area: ui | reviewer: silentbug | category: state-corruption
- When a streaming-tool placeholder part is removed on a final ToolCall (lines 236-245), shiftIndexesAfterRemoval (lines 54-61) reindexes only `toolByCallId` and `partIndexById`. It does NOT shift `dataPartIndexById`, which also stores absolute part indices (populated at line 335). If a keyed Raw data part was appended after the placeholder (e.g. a progress data part emitted while tool input is still streaming), its recorded index is now off-by-one after the removal. A subsequent keyed update for that same data id (lines 330-333) then does `parts[existingIdx] = part` against a stale index that is still within bounds, silently overwriting an unrelated part (e.g. the freshly-added final tool card) instead of updating the data part in place — and the real data part is never updated. The result is silent message corruption with no error. Fix: add `for ((key, value) in dataPartIndexById.toMap()) { if (value > removedIndex) dataPartIndexById[key] = value - 1 }` to shiftIndexesAfterRemoval.
- verify: Confirmed against current code in /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/ui/MessageStreamReader.kt. shiftIndexesAfterRemoval (lines 54-61) shifts only toolByCallId (55-57) and partIndexById (58-60); it never shifts dataPartIndexById. That map also holds ABSOLUTE indices into the same `parts` list (written at line 335 `dataPartIndexById[dataId] = parts.size - 1`, read as an absolute index at lines 330-332 `parts[existingIdx] = part`). The placeholder removal path (lines 236-245) does parts.removeAt(placeholderIdx) then calls shiftIndexesAfterRemo

### M29. ui/Streams.kt:147 — pipeUiMessageStreamToResponse default encoder emits debug toString() under an SSE content-type
- area: ui | reviewer: silentbug | category: api-footgun
- pipeUiMessageStreamToResponse defaults `encoder: (UIMessage) -> String = { it.toString() }` (line 147) while the default headers declare `text/event-stream` (uiMessageStreamHeaders(), line 130-131). UIMessage is a plain data class, so toString() produces Kotlin debug output like `UIMessage(id=..., role=..., parts=[...])` — neither JSON nor SSE `data:` framing. A server that calls this without supplying an encoder (the default header strongly implies it is ready to use for SSE) will silently write structurally-invalid output to the wire; the client fails to parse and the server gets no signal anything is wrong. The two defaults are internally contradictory. Fix: drop the default and require an explicit encoder, or default to a real JSON/SSE serializer (e.g. encode each UIMessage to the v6 chunk JSON via ToUIMessageStream and frame as `data: <json>\n\n`).
- verify: Confirmed against current code. Streams.kt:147 literally defaults `encoder: (UIMessage) -> String = { it.toString() }`, while the same function (line 149) defaults `headers = uiMessageStreamHeaders()` which returns `Content-Type: text/event-stream; charset=utf-8` (lines 130-131). UIMessage is a `@Serializable data class` (UIMessage.kt:22-29) with no toString() override, so the default encoder emits Kotlin debug output like `UIMessage(id=..., role=..., parts=[...], createdAtMs=..., metadata=...)` — not JSON and without SSE `data:`/`\n\n` framing. The two defaults are internally contradictory: a


## LOW (7)

### L1. providers/AnthropicProvider.kt:321 — Discarded WireDecoder.required() call makes the blocks.size fallback dead in content_block_delta and content_block_stop
- area: providers-anthropic-google | reviewer: codereview | category: correctness
- In the `content_block_delta` (line 321) and `content_block_stop` (line 347) arms of `AnthropicStreamState.accept`, the index is parsed as:

    WireDecoder.required(obj, "index", ...)   // result discarded
    WireDecoder.optionalInt(obj, "index", ...) ?: blocks.size

`WireDecoder.required` (confirmed in WireDecoder.kt:66-67) returns `obj[key]` or throws `WireDecodeException` if the key is absent. The returned value is immediately discarded. The subsequent `optionalInt` call also reads `obj["index"]`. If the key was absent, `required` would have already thrown — the `?: blocks.size` fallback is therefore unreachable.

Contrast with `content_block_start` (line 282), which correctly uses only `optionalInt(...) ?: blocks.size`: if `index` is absent there, it silently defaults to `blocks.size`. The two handling paths are inconsistent, and the fallback documented as the defensive default is dead code for delta/stop events.

Fix: remove the `WireDecoder.required(...)` lines in both the delta (line 321) and stop (line 347) arms, leaving only `WireDecoder.optionalInt(obj, "index", ...) ?: blocks.size` — exactly mirroring `content_block_start`.
- verify: Verified against the actual code. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/AnthropicProvider.kt the content_block_delta arm (lines 320-322) and content_block_stop arm (lines 346-348) both call WireDecoder.required(obj, "index", ...) and discard its return value, then call WireDecoder.optionalInt(obj, "index", ...) ?: blocks.size. WireDecoder.required (WireDecoder.kt:66-67) returns obj[key] or throws when the key is absent; WireDecoder.optionalInt (WireDecoder.kt:108-109) returns null ONLY when obj[key] is null. So once required() runs

### L2. providers/GoogleWire.kt:320 — Non-streaming Gemini functionCall fabricates an empty tool name instead of failing
- area: providers-anthropic-google | reviewer: silentbug | category: wire-parsing
- In `googleLanguageResult`, a tool call is decoded with `toolName = callObj["name"]?.jsonPrimitive?.contentOrNull.orEmpty()`, so a missing/blank `functionCall.name` yields a ToolCall with `toolName == ""`. That nameless ToolCall flows downstream into the tool loop and fails far from the cause with a confusing "tool not found", masking the real wire problem. The streaming path for the exact same field uses `WireDecoder.requiredString(call, "name", ...)` (GoogleLanguageModel.kt:157) and is explicitly tested to surface it as a wire Error event (GoogleProviderTest.kt:303 `stream surfaces malformed Gemini function call as wire error event`) — so the strict behavior is the intended contract and the non-streaming path diverges from it. The same leniency exists in the non-streaming interactions decoder (GoogleInteractionsModel.kt:761). Fix: decode the name with `WireDecoder.requiredString(callObj, "name", "google", "generateContent response", path)` in both non-streaming sites to match the streaming path.
- verify: CONFIRMED as a real defect by reading the actual code. The non-streaming Gemini language path at src/commonMain/kotlin/ai/torad/aisdk/providers/GoogleWire.kt:320 decodes the tool name as `toolName = callObj["name"]?.jsonPrimitive?.contentOrNull.orEmpty()`, so a missing/non-string functionCall.name silently yields a ToolCall with toolName == "". This function (`googleLanguageResult`) is the decoder for non-streaming generateContent (called from GoogleLanguageModel.kt:70). The streaming sibling for the exact same field at GoogleLanguageModel.kt:157 uses `WireDecoder.requiredString(call, "name", 

### L3. providers/GoogleVertexProvider.kt:331 — googleVertexXaiSnakeCaseKey produces a leading underscore for keys starting with an uppercase letter
- area: providers-audio-misc | reviewer: codereview | category: correctness
- The XAI reference implementation (XaiProvider.kt, xaiNaiveSnakeCaseKey) guards the underscore insertion with `if (index > 0) append('_')`, so the very first character is never preceded by an underscore. The Google Vertex copy (googleVertexXaiSnakeCaseKey, line 334) omits that guard and always prepends '_' before any uppercase character. For a key like "SearchParameters" the function emits "_search_parameters" instead of "search_parameters". All current callers pass camelCase-starting-lowercase keys (e.g. "returnCitations"), so the bug is latent; but any user-supplied searchParameters JSON key that starts with an uppercase letter will be silently transformed into an invalid API parameter the server ignores.

Fix: mirror the XAI version exactly:
  value.forEachIndexed { index, char ->
    if (char.isUpperCase()) {
      if (index > 0) append('_')
      append(char.lowercaseChar())
    } else { append(char) }
  }
- verify: Confirmed by reading both functions. GoogleVertexProvider.kt:331-341 (googleVertexXaiSnakeCaseKey) uses `value.forEach { char -> if (char.isUpperCase()) { append('_'); append(char.lowercaseChar()) } ... }` with NO index guard, so an uppercase first character gets a leading underscore: "SearchParameters" -> "_search_parameters". The reference XaiProvider.kt:422-432 (xaiNaiveSnakeCaseKey) uses `forEachIndexed` with `if (index > 0) append('_')`, producing "search_parameters". The two are meant to mirror each other and the Vertex copy dropped the guard. Call path verified: googleVertexXaiProviderO

### L4. providers/HuggingFaceProvider.kt:696 — HuggingFace provider options are silently dropped when they fail to decode
- area: providers-bedrock-media | reviewer: silentbug | category: silent-failure / swallowed decode error
- huggingFaceProviderOptions() does `runCatching { aiSdkJson.decodeFromJsonElement(HuggingFaceResponsesSettings.serializer(), element) }.getOrNull()`. aiSdkJson uses ignoreUnknownKeys, so unknown keys are tolerated, but a wrong-typed value (e.g. `metadata` passed as a string/array, `reasoningEffort` as a number, `strictJsonSchema` as a string) makes decode throw and getOrNull() swallows it to null. The effect: ALL of the user's huggingface options (instructions, reasoningEffort, metadata, strictJsonSchema) silently vanish from the request with no warning — the model runs without the requested configuration and the user has no idea why. This is exception-coerced-to-null masking a real input error. Fix: let the decode error propagate (drop the runCatching) so a malformed options block fails loudly, or at minimum append a CallWarning("other", "Ignored invalid huggingface providerOptions: <message>") so the dropped configuration is visible.
- verify: Confirmed against current code. HuggingFaceProvider.kt:694-697 does `runCatching { aiSdkJson.decodeFromJsonElement(HuggingFaceResponsesSettings.serializer(), element) }.getOrNull()`, and the result is consumed in huggingFaceResponsesRequestBody (line 519) where a null causes every hf option (instructions, reasoningEffort, metadata, strictJsonSchema; lines 531-542) to be omitted. No CallWarning is emitted on the null path even though the `warnings` list is in scope and used for trivial unsupported params (topK/seed, lines 511-515). aiSdkJson (TypedJson.kt:16-20) is ignoreUnknownKeys + isLenient

### L5. providers/OpenAICompatibleFacadeSupport.kt:47 — Double User-Agent header for every facade provider and OpenAI chat/completion model
- area: providers-openai | reviewer: codereview | category: correctness
- FacadeSupport.compatibleSettings embeds the versioned user-agent directly into settings.headers via ProviderHeaders.withUserAgentSuffix(headers, "ai-sdk/$name/$version") but never sets userAgentSuffix = null. OpenAICompatibleProviderSettings.userAgentSuffix therefore keeps its default value "ai-sdk/openai-compatible-kotlin". In OpenAICompatibleHttpModel.commonHeaders(), ProviderHeaders.build calls withUserAgentSuffix(base, settings.userAgentSuffix), which APPENDS the suffix to the user-agent already present in settings.headers. The final header becomes "ai-sdk/groq/3.0.39 ai-sdk/openai-compatible-kotlin" for Groq, and similarly for every other facade provider (DeepSeek, Perplexity, Cerebras, MoonshotAI, TogetherAI, Fireworks, DeepInfra, Vercel, Baseten). The same defect exists in OpenAIWire.toCompatibleSettings (OpenAIProvider.kt:171), which affects OpenAIProvider.chat() / completion() / embedding(). Note: AzureOpenAIProvider.compatibleSettings() already correctly sets userAgentSuffix = null and is unaffected. Fix: add userAgentSuffix = null to the OpenAICompatibleProviderSettings constructor call in FacadeSupport.compatibleSettings AND in OpenAIWire.toCompatibleSettings, since the user-agent is already embedded in the headers map passed to those constructors.
- verify: CONFIRMED as a real defect. Trace: OpenAICompatibleProviderSettings.userAgentSuffix defaults to "ai-sdk/openai-compatible-kotlin" (OpenAICompatibleProvider.kt:21). FacadeSupport.compatibleSettings embeds the versioned UA into headers via ProviderHeaders.withUserAgentSuffix(headers, "ai-sdk/$name/$version") (OpenAICompatibleFacadeSupport.kt:51) but never passes userAgentSuffix, so the default is retained. In commonHeaders() (OpenAICompatibleHttp.kt:47), ProviderHeaders.build calls withUserAgentSuffix(base, settings.userAgentSuffix) (ProviderHeaders.kt:38-40), which APPENDS the default suffix on

### L6. providers/OpenResponsesProvider.kt:1072 — created_at timestamp parsed with intOrNull — silently becomes null for post-2038 Unix timestamps
- area: providers-openai | reviewer: codereview | category: correctness
- openResponsesGenerateResult reads the created_at field from the response with: response["created_at"]?.jsonPrimitive?.intOrNull?.toLong()?.times(1000). kotlinx-serialization's intOrNull returns null for JSON numbers that do not fit in a 32-bit signed integer. Unix timestamps reach Int.MAX_VALUE (2 147 483 647) in January 2038; any server-reported timestamp from that point onward will silently produce a null timestampMillis in LanguageModelResponseMetadata. The existing test (line 130) encodes 1_780_000_000 * 1000 which still fits, masking the issue. The OpenAI-compatible chat wire (OpenAICompatibleWire.chatResultFromJson) correctly handles the equivalent created field with: obj["created"]?.jsonPrimitive?.doubleOrNull?.let { (it * 1000).toLong() }. Fix: replace intOrNull with doubleOrNull and apply the same expression as the chat wire: response["created_at"]?.jsonPrimitive?.doubleOrNull?.let { (it * 1000).toLong() }.
- verify: CONFIRMED REAL. src/commonMain/kotlin/ai/torad/aisdk/providers/OpenResponsesProvider.kt:1072 reads `timestampMillis = response["created_at"]?.jsonPrimitive?.intOrNull?.toLong()?.times(1000)` — exact match to the finding. kotlinx-serialization's `JsonPrimitive.intOrNull` returns null for any JSON number exceeding 32-bit signed Int (> 2,147,483,647); Int.MAX_VALUE as a Unix-seconds timestamp is 2038-01-19 03:14:07 UTC (the `?.times(1000)` confirms seconds are expected), so from Jan 2038 onward `timestampMillis` silently becomes null. This is a genuine defect, not intended behavior: every sibling

### L7. ui/MessageStreamReader.kt:54 — shiftIndexesAfterRemoval does not update dataPartIndexById, causing duplicate data parts on upsert
- area: ui | reviewer: codereview | category: logic error
- When a StreamEvent.ToolCall arrives with a final tool-call ID that differs from the streaming placeholder ID, MessageStreamReader removes the placeholder part via parts.removeAt(placeholderIdx) and calls shiftIndexesAfterRemoval(placeholderIdx). That function decrements stored indices in toolByCallId and partIndexById for all values > removedIndex, but it never touches dataPartIndexById. If a keyed StreamEvent.Raw data part was appended at any index after the placeholder, its recorded index in dataPartIndexById is now stale (one too high). The next Raw event for the same data ID reads the stale index, finds it fails `in parts.indices`, and falls through to the else branch — appending a fresh duplicate instead of upserting in place. dataPartIndexById is then updated to the new (correct) index, so subsequent updates work, but the stale copy at the lower index persists and renders as a spurious extra part. Fix: add a third loop to shiftIndexesAfterRemoval that mirrors the existing loops: `for ((key, value) in dataPartIndexById.toMap()) { if (value > removedIndex) dataPartIndexById[key] = value - 1 }`.
- verify: CONFIRMED at code level. In src/commonMain/kotlin/ai/torad/aisdk/ui/MessageStreamReader.kt, shiftIndexesAfterRemoval (lines 54-61) decrements indices in toolByCallId and partIndexById but never in dataPartIndexById. The ToolCall handler (lines 236-245) removes a streaming placeholder via parts.removeAt(placeholderIdx) then calls shiftIndexesAfterRemoval, so any dataPartIndexById entry recorded at an index above the removed placeholder becomes stale. The Raw handler (lines 330-336) then misuses the stale index. The omission is real (2 of 3 index maps are maintained), the mechanism is mechanical


## LOW (unverified notes, 20)

- Provider.kt:150 — splitProviderModelId returns model ID with leading separator when separator is at position 0
- Generate.kt:86 — StreamTextResult.textStream silently truncates on a terminal StreamEvent.Error
- AgentTelemetryDispatcher.kt:48 — Lifecycle-hook failures can be fully swallowed — no logger.warn fallback and per-call hooks.onError is never notified
- AgentSession.kt:242 — AgentSession.submitStreaming drops StreamEvent.Error.cause, losing the root-cause chain in session error state
- MCP.kt:843 — OAuth protected-resource discovery coerces all throwables to null, masking network/parse errors and silently dropping RFC 8707 resource binding
- ConvertToLanguageModelPrompt.kt:239 — isUrlSupported swallows regex-compile errors, silently masking a malformed supportedUrls pattern
- AbortSignal.kt:72 — AbortController swallows abort-callback exceptions with no logging
- HttpTransport.kt:351 — SSE parse-failure error event discards the offending payload
- ui/MessageStreamReader.kt:215 — ToolInputDelta silently dropped (after buffering) when no toolName is recorded
- middleware/SimulateStreaming.kt:37 — ResponseMetadata emission drops `timestampMillis` from the generate result
- providers/AzureProvider.kt:125 — AzureOpenAITools default parameters each construct a separate OpenAITools() instance, discarding 45 tool objects
- providers/TogetherAIFacade.kt:161 — Reranking decode fabricates sentinels (index 0, score 0, empty document) for missing required fields
- providers/GroqFacade.kt:142 — Groq browserSearch tool is silently dropped on unsupported models with no warning to the user
- providers/CohereProvider.kt:515 — Cohere chat tool call fabricates an empty tool name on missing field
- providers/CohereProvider.kt:216 — Cohere rerank result defaults index/score to sentinels, silently mis-mapping results
- providers/GoogleMediaModels.kt:69 — Google embedding decode returns empty vectors / wrong count on a malformed 200 body
- providers/LumaProvider.kt:244 — Luma poll discards the upstream failure_reason on a failed generation
- providers/XaiProvider.kt:185 — XaiChatLanguageModel.streamResult() contains a no-op identity map that leaves xAI web-search citations uninjected
- providers/AssemblyAIProvider.kt:223 — AssemblyAI transcription polls in an unbounded `while(true)` with no max-attempts cap and no backoff when pollingIntervalMillis == 0
- providers/AssemblyAIProvider.kt:235 — AssemblyAI 'unsupported status' error throws away the response body that names the offending status