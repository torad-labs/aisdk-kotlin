{
  "id": "I2",
  "written_scope": "Implements the repaired Replicate video-only item in four files. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt, it preserves the exact prior I2 production delta: private Starting/Processing/Succeeded/Failed/Canceled/Unknown(raw) states, one parse per received prediction, polling only Starting/Processing, output exposure only for Succeeded, and immediate malformed/unknown rejection. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/ReplicateProviderTest.kt, it preserves the transition/request-count matrix and adds exactly one processing→queued poll-path regression test that includes premature output in both responses, requires the exact InvalidResponseDataError message, and requires exactly the create URL followed by one poll URL. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/ReplicateDefensiveParsingTest.kt and ReplicateProviderTest.kt, it byte-preservingly wraps the four overlong JSON fixtures. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/architecture-budget.json, it changes only the ReplicateProvider.kt ceiling from 739 to the checker-measured 772. Explicit non-goals: no change to Replicate image handling, timeout arithmetic, poll-URL policy, output-shape policy, public declarations, ABI dumps, CHANGELOG.md, INTERFACE_CONTRACT.md, provider/model/MIME identifiers, or the staged pre-campaign baseline.",
  "recommendation": "LAND_WIDENED",
  "premise_check": "\"Use the exact I2 artifact as the semantic base; do not redesign or edit its production state machine\" is sound: the pinned production scope is 1 file and the defensible scope is the same 1 file; the repaired production diff SHA-256 is 49c4c52bcba9594825385e29136c233ca20917e3597bf9bde409f73b4e54a428, exactly equal to the plan sandbox's production diff hash. \"Wrap the JSON fixtures currently at artifact lines 55, 63, and 71 ... and the canceled fixture currently at artifact line 305\" is sound: the pinned formatting scope is 4 expressions and the defensible scope is exactly 4; a byte comparison reported identical=True for all 4, and the final Detekt run has 0 findings. \"Use adjacent raw-string concatenation at the \\\"urls\\\": boundary ... do not insert payload whitespace and do not use trimIndent\" is sound: the pinned technique is 4 adjacent concatenations and the defensible technique is the same 4; the before/after fixture lengths and SHA-256 values were identical. \"Add exactly one permanent request-path regression test for processing on create followed by unknown queued on the first GET\" is sound: the pinned new-test count is 1 and the defensible count is 1; ReplicateProviderTest moved from 7 prior-artifact tests to 8 repaired tests, and the new test fails the baseline then passes the repair. \"Include output fields in both responses ... pollIntervalMs=0 and pollTimeoutMs=1; assert the exact error and exact two-URL request sequence\" is sound: the pinned assertion set is 2 outputs, 2 option values, 1 exact message, and 1 exact two-element URL list; the defensible set is identical. \"Do not add separate boolean/null/object/array tests\" is sound: pinned extra-test count is 0 and defensible count is 0; a no-file Kotlin probe ran the shared predicate for number, boolean, null, object, and array and printed rejected=true for all 5 shapes. \"Change only Replicate 739 to the measured 772\" is sound: pinned budget-key count is 1 and defensible count is 1; the budget diff contains exactly that one key, and the direct checker reports success. \"Do not split or compress the production implementation merely to evade the budget\" is sound: pinned production implementation is the preserved 1-file I2 diff and defensible implementation is identical; its diff hash did not move. \"Return the complete unstaged binary Git patch, including the original three-file artifact ... and architecture-budget.json\" is a necessary widening of the original three-file item: original pinned file count was 3, while the mechanically defensible repaired count is 4 because the repository gate tracks ReplicateProvider.kt; no fifth file is changed. \"Verification order is the exact item gate, direct Detekt, wave check, then campaign-close ci-gate\" is sound: pinned gate count is 4 and defensible count is 4; all four were rerun in that order after mutation restoration and passed. \"Do not alter any line of the artifact's production delta\" is sound: pinned and defensible altered production-delta line count beyond the prior artifact are both 0, proven by equal production diff hashes. \"Do not change the four fixture payload bytes or their existing assertions\" is sound: pinned and defensible changed-payload-byte counts are both 0, and all existing assertions remain. \"Do not alter any architecture budget except Replicate 739→772\" is sound: pinned and defensible changed-budget-entry counts are both 1. \"Do not touch API dumps, CHANGELOG.md, INTERFACE_CONTRACT.md, public declarations, identifiers, MIME values, model IDs, staged baseline, or Google generateId corrections\" is sound: pinned and defensible deltas in each forbidden category are 0; the unstaged name list is exactly the four scoped files and the cached baseline hash remains 63fad51fdf65d1f04d068d1db82a90bf04c814187e0da95a7f411ddc32b1e762.",
  "artifact": "diff --git a/architecture-budget.json b/architecture-budget.json\nindex f9fcff1..45009d0 100644\n--- a/architecture-budget.json\n+++ b/architecture-budget.json\n@@ -43,7 +43,7 @@\n     \"src/commonMain/kotlin/ai/torad/aisdk/providers/GladiaProvider.kt\": 766,\n     \"src/commonMain/kotlin/ai/torad/aisdk/providers/KlingAIProvider.kt\": 683,\n     \"src/commonMain/kotlin/ai/torad/aisdk/providers/LiteRTLanguageModel.kt\": 1237,\n-    \"src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt\": 739,\n+    \"src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt\": 772,\n     \"src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt\": 637,\n     \"src/commonTest/kotlin/ai/torad/aisdk/CohereProviderTest.kt\": 599,\n     \"src/commonTest/kotlin/ai/torad/aisdk/ParallelToolExecutionTest.kt\": 537,\ndiff --git a/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt b/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt\nindex fac8d2f..fe0180f 100644\n--- a/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt\n+++ b/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt\n@@ -655,6 +655,7 @@ private class ReplicateVideoModel(\n         abortSignal: AbortSignal,\n     ): JsonObject {\n         var prediction = initialPrediction\n+        var state = replicatePredictionState(prediction)\n         val pollIntervalMs = (options[\"pollIntervalMs\"] as? JsonPrimitive)?.contentOrNull?.toLongOrNull()\n             ?: DEFAULT_REPLICATE_VIDEO_POLL_INTERVAL_MS\n         val pollTimeoutMs = (options[\"pollTimeoutMs\"] as? JsonPrimitive)?.contentOrNull?.toLongOrNull()\n@@ -665,35 +666,69 @@ private class ReplicateVideoModel(\n             .toInt()\n             .coerceAtLeast(1)\n         var attempts = 0\n-        while (replicateStatus(prediction) in setOf(\"starting\", \"processing\")) {\n-            if (attempts >= maxPollAttempts) {\n-                throw NoVideoGeneratedError(\"Video generation timed out after ${pollTimeoutMs}ms\")\n-            }\n-            if (pollIntervalMs > 0) delay(pollIntervalMs)\n-            abortSignal.throwIfAborted()\n-            val pollUrl = ((JsonAccess.obj(prediction, \"urls\"))?.get(\"get\") as? JsonPrimitive)?.contentOrNull\n-                ?: throw InvalidResponseDataError(null, \"Replicate prediction response is missing urls.get\")\n-            prediction = settings.replicateGetJson(\n-                client = client,\n-                url = pollUrl,\n-                headers = settings.replicateHeaders(),\n-                abortSignal = abortSignal,\n-            ).value.jsonObject\n-            attempts++\n-        }\n-        when (replicateStatus(prediction)) {\n-            \"failed\" -> {\n-                val detail = (prediction[\"error\"] as? JsonPrimitive)?.contentOrNull ?: \"Unknown error\"\n-                throw NoVideoGeneratedError(\"Video generation failed: $detail\")\n+        while (true) {\n+            when (state) {\n+                ReplicatePredictionState.Starting,\n+                ReplicatePredictionState.Processing,\n+                -> {\n+                    if (attempts >= maxPollAttempts) {\n+                        throw NoVideoGeneratedError(\"Video generation timed out after ${pollTimeoutMs}ms\")\n+                    }\n+                    if (pollIntervalMs > 0) delay(pollIntervalMs)\n+                    abortSignal.throwIfAborted()\n+                    val pollUrl = ((JsonAccess.obj(prediction, \"urls\"))?.get(\"get\") as? JsonPrimitive)?.contentOrNull\n+                        ?: throw InvalidResponseDataError(null, \"Replicate prediction response is missing urls.get\")\n+                    prediction = settings.replicateGetJson(\n+                        client = client,\n+                        url = pollUrl,\n+                        headers = settings.replicateHeaders(),\n+                        abortSignal = abortSignal,\n+                    ).value.jsonObject\n+                    state = replicatePredictionState(prediction)\n+                    attempts++\n+                }\n+                ReplicatePredictionState.Succeeded -> return prediction\n+                ReplicatePredictionState.Failed -> {\n+                    val detail = (prediction[\"error\"] as? JsonPrimitive)?.contentOrNull ?: \"Unknown error\"\n+                    throw NoVideoGeneratedError(\"Video generation failed: $detail\")\n+                }\n+                ReplicatePredictionState.Canceled -> {\n+                    throw NoVideoGeneratedError(\"Video generation was canceled\")\n+                }\n+                is ReplicatePredictionState.Unknown -> {\n+                    throw InvalidResponseDataError(\n+                        JsonPrimitive(state.raw),\n+                        \"Replicate prediction response has unknown status: ${state.raw}\",\n+                    )\n+                }\n             }\n-            \"canceled\" -> throw NoVideoGeneratedError(\"Video generation was canceled\")\n         }\n-        return prediction\n     }\n \n-    private fun replicateStatus(prediction: JsonObject): String =\n-        (prediction[\"status\"] as? JsonPrimitive)?.contentOrNull\n+    private fun replicatePredictionState(prediction: JsonObject): ReplicatePredictionState {\n+        val status = prediction[\"status\"]\n             ?: throw InvalidResponseDataError(null, \"Replicate prediction response is missing status\")\n+        if (status !is JsonPrimitive || !status.isString) {\n+            throw InvalidResponseDataError(status, \"Replicate prediction response status is not a string\")\n+        }\n+        return when (val raw = status.content) {\n+            \"starting\" -> ReplicatePredictionState.Starting\n+            \"processing\" -> ReplicatePredictionState.Processing\n+            \"succeeded\" -> ReplicatePredictionState.Succeeded\n+            \"failed\" -> ReplicatePredictionState.Failed\n+            \"canceled\" -> ReplicatePredictionState.Canceled\n+            else -> ReplicatePredictionState.Unknown(raw)\n+        }\n+    }\n+\n+    private sealed interface ReplicatePredictionState {\n+        data object Starting : ReplicatePredictionState\n+        data object Processing : ReplicatePredictionState\n+        data object Succeeded : ReplicatePredictionState\n+        data object Failed : ReplicatePredictionState\n+        data object Canceled : ReplicatePredictionState\n+        data class Unknown(val raw: String) : ReplicatePredictionState\n+    }\n \n     private fun replicateVideoProviderMetadata(prediction: JsonObject, videoUrl: String): JsonElement = buildJsonObject {\n         put(\ndiff --git a/src/commonTest/kotlin/ai/torad/aisdk/ReplicateDefensiveParsingTest.kt b/src/commonTest/kotlin/ai/torad/aisdk/ReplicateDefensiveParsingTest.kt\nindex eb7a769..539daf3 100644\n--- a/src/commonTest/kotlin/ai/torad/aisdk/ReplicateDefensiveParsingTest.kt\n+++ b/src/commonTest/kotlin/ai/torad/aisdk/ReplicateDefensiveParsingTest.kt\n@@ -9,7 +9,9 @@ import io.ktor.http.HttpStatusCode\n import io.ktor.http.headersOf\n import kotlinx.coroutines.test.runTest\n import kotlin.test.Test\n+import kotlin.test.assertEquals\n import kotlin.test.assertFails\n+import kotlin.test.assertFailsWith\n import kotlin.test.assertTrue\n \n class ReplicateDefensiveParsingTest {\n@@ -46,4 +48,57 @@ class ReplicateDefensiveParsingTest {\n             \"the structured Replicate error is built, not an IllegalArgumentException from jsonPrimitive\",\n         )\n     }\n+\n+    @Test\n+    fun `video generate rejects missing status without polling`() = runTest {\n+        assertInvalidVideoStatus(\n+            response = \"\"\"{\"output\":\"https://cdn.example/video.mp4\",\"\"\" +\n+                \"\"\"\"urls\":{\"get\":\"https://api.replicate.com/v1/predictions/pred1\"}}\"\"\",\n+            expectedMessage = \"Replicate prediction response is missing status\",\n+        )\n+    }\n+\n+    @Test\n+    fun `video generate rejects non-string status without polling`() = runTest {\n+        assertInvalidVideoStatus(\n+            response = \"\"\"{\"status\":42,\"output\":\"https://cdn.example/video.mp4\",\"\"\" +\n+                \"\"\"\"urls\":{\"get\":\"https://api.replicate.com/v1/predictions/pred1\"}}\"\"\",\n+            expectedMessage = \"Replicate prediction response status is not a string\",\n+        )\n+    }\n+\n+    @Test\n+    fun `video generate rejects unknown status without consuming output`() = runTest {\n+        assertInvalidVideoStatus(\n+            response = \"\"\"{\"status\":\"queued\",\"output\":\"https://cdn.example/video.mp4\",\"\"\" +\n+                \"\"\"\"urls\":{\"get\":\"https://api.replicate.com/v1/predictions/pred1\"}}\"\"\",\n+            expectedMessage = \"Replicate prediction response has unknown status: queued\",\n+        )\n+    }\n+\n+    private suspend fun assertInvalidVideoStatus(response: String, expectedMessage: String) {\n+        var requestCount = 0\n+        val client = HttpClient(\n+            MockEngine {\n+                requestCount++\n+                respond(\n+                    content = response,\n+                    status = HttpStatusCode.OK,\n+                    headers = headersOf(HttpHeaders.ContentType, \"application/json\"),\n+                )\n+            },\n+        )\n+        val model = Replicate(client).video(ModelId(\"minimax/video-01\"))\n+\n+        val error = assertFailsWith<InvalidResponseDataError> {\n+            model.generate(\n+                VideoGenerationParams {\n+                    prompt(\"x\")\n+                },\n+            )\n+        }\n+\n+        assertEquals(expectedMessage, error.message)\n+        assertEquals(1, requestCount)\n+    }\n }\ndiff --git a/src/commonTest/kotlin/ai/torad/aisdk/ReplicateProviderTest.kt b/src/commonTest/kotlin/ai/torad/aisdk/ReplicateProviderTest.kt\nindex 1abe14f..f105e56 100644\n--- a/src/commonTest/kotlin/ai/torad/aisdk/ReplicateProviderTest.kt\n+++ b/src/commonTest/kotlin/ai/torad/aisdk/ReplicateProviderTest.kt\n@@ -208,10 +208,118 @@ class ReplicateProviderTest {\n             metadata?.get(\"videos\")?.jsonArray?.single()?.jsonObject?.get(\"url\")?.jsonPrimitive?.contentOrNull,\n         )\n         assertEquals(1.25, metadata?.get(\"metrics\")?.jsonObject?.get(\"predict_time\")?.jsonPrimitive?.doubleOrNull)\n+        assertEquals(2, fixture.calls.size)\n     }\n \n     @Test\n-    fun `video model extracts url from array-shaped output`() = runTest {\n+    fun `video model polls starting and processing exactly once per transition`() = runTest {\n+        val createUrl = \"https://api.replicate.com/v1/models/minimax/video-01/predictions\"\n+        val firstPollUrl = \"https://api.replicate.com/v1/predictions/pred1\"\n+        val secondPollUrl = \"https://api.replicate.com/v1/predictions/pred2\"\n+        val fixture = TestServer.createTestServer(\n+            mutableMapOf(\n+                createUrl to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\n+                            \"\"\"{\"id\":\"pred1\",\"status\":\"starting\",\"urls\":{\"get\":\"$firstPollUrl\"}}\"\"\",\n+                        ),\n+                    ),\n+                ),\n+                firstPollUrl to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\n+                            \"\"\"{\"id\":\"pred1\",\"status\":\"processing\",\"urls\":{\"get\":\"$secondPollUrl\"}}\"\"\",\n+                        ),\n+                    ),\n+                ),\n+                secondPollUrl to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\n+                            \"\"\"{\"id\":\"pred1\",\"status\":\"succeeded\",\"output\":\"https://cdn.example/video.mp4\"}\"\"\",\n+                        ),\n+                    ),\n+                ),\n+            ),\n+        )\n+        fixture.server.start()\n+        val model = Replicate(fixture.httpClient()).video(ModelId(\"minimax/video-01\"))\n+\n+        val result = model.generate(\n+            VideoGenerationParams {\n+                prompt(\"camera push in\")\n+                providerOptions(\n+                    ProviderOptions.Raw(\n+                        JsonObject(\n+                            mapOf(\n+                                \"replicate\" to buildJsonObject {\n+                                    put(\"pollIntervalMs\", JsonPrimitive(0))\n+                                    put(\"pollTimeoutMs\", JsonPrimitive(2))\n+                                },\n+                            ),\n+                        ),\n+                    ),\n+                )\n+            },\n+        )\n+\n+        assertEquals(\"https://cdn.example/video.mp4\", result.videos.single().url)\n+        assertEquals(listOf(createUrl, firstPollUrl, secondPollUrl), fixture.calls.map { it.requestUrl })\n+    }\n+\n+    @Test\n+    fun `video model rejects unknown status returned by poll without exposing output`() = runTest {\n+        val createUrl = \"https://api.replicate.com/v1/models/minimax/video-01/predictions\"\n+        val pollUrl = \"https://api.replicate.com/v1/predictions/pred1\"\n+        val fixture = TestServer.createTestServer(\n+            mutableMapOf(\n+                createUrl to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\n+                            \"\"\"{\"id\":\"pred1\",\"status\":\"processing\",\"\"\" +\n+                                \"\"\"\"output\":\"https://cdn.example/premature.mp4\",\"\"\" +\n+                                \"\"\"\"urls\":{\"get\":\"$pollUrl\"}}\"\"\",\n+                        ),\n+                    ),\n+                ),\n+                pollUrl to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\n+                            \"\"\"{\"id\":\"pred1\",\"status\":\"queued\",\"\"\" +\n+                                \"\"\"\"output\":\"https://cdn.example/unknown.mp4\"}\"\"\",\n+                        ),\n+                    ),\n+                ),\n+            ),\n+        )\n+        fixture.server.start()\n+        val model = Replicate(fixture.httpClient()).video(ModelId(\"minimax/video-01\"))\n+\n+        val error = assertFailsWith<InvalidResponseDataError> {\n+            model.generate(\n+                VideoGenerationParams {\n+                    prompt(\"x\")\n+                    providerOptions(\n+                        ProviderOptions.Raw(\n+                            JsonObject(\n+                                mapOf(\n+                                    \"replicate\" to buildJsonObject {\n+                                        put(\"pollIntervalMs\", JsonPrimitive(0))\n+                                        put(\"pollTimeoutMs\", JsonPrimitive(1))\n+                                    },\n+                                ),\n+                            ),\n+                        ),\n+                    )\n+                },\n+            )\n+        }\n+\n+        assertEquals(\"Replicate prediction response has unknown status: queued\", error.message)\n+        assertEquals(listOf(createUrl, pollUrl), fixture.calls.map { it.requestUrl })\n+    }\n+\n+    @Test\n+    fun `video model extracts url from array-shaped output without polling`() = runTest {\n         val fixture = TestServer.createTestServer(\n             mutableMapOf(\n                 \"https://api.replicate.com/v1/models/minimax/video-01/predictions\" to UrlHandler(\n@@ -236,10 +344,40 @@ class ReplicateProviderTest {\n         )\n \n         assertEquals(\"https://cdn.example/array.mp4\", result.videos.single().url)\n+        assertEquals(1, fixture.calls.size)\n+    }\n+\n+    @Test\n+    fun `video model reports canceled predictions without polling`() = runTest {\n+        val fixture = TestServer.createTestServer(\n+            mutableMapOf(\n+                \"https://api.replicate.com/v1/models/minimax/video-01/predictions\" to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\n+                            \"\"\"{\"id\":\"pred1\",\"status\":\"canceled\",\"output\":null,\"\"\" +\n+                                \"\"\"\"urls\":{\"get\":\"https://api.replicate.com/v1/predictions/pred1\"}}\"\"\",\n+                        ),\n+                    ),\n+                ),\n+            ),\n+        )\n+        fixture.server.start()\n+        val model = Replicate(fixture.httpClient()).video(ModelId(\"minimax/video-01\"))\n+\n+        val error = assertFailsWith<NoVideoGeneratedError> {\n+            model.generate(\n+                VideoGenerationParams {\n+                    prompt(\"x\")\n+                },\n+            )\n+        }\n+\n+        assertEquals(\"Video generation was canceled\", error.message)\n+        assertEquals(1, fixture.calls.size)\n     }\n \n     @Test\n-    fun `video model reports failed predictions and unsupported families`() = runTest {\n+    fun `video model reports failed predictions without polling and rejects unsupported families`() = runTest {\n         val fixture = TestServer.createTestServer(\n             mutableMapOf(\n                 \"https://api.replicate.com/v1/models/minimax/video-01/predictions\" to UrlHandler(\n@@ -254,13 +392,15 @@ class ReplicateProviderTest {\n         fixture.server.start()\n         val provider = Replicate(fixture.httpClient(), ReplicateProviderSettings { apiToken(\"token\") })\n \n-        assertFailsWith<AiSdkException> {\n+        val error = assertFailsWith<NoVideoGeneratedError> {\n             provider.video(ModelId(\"minimax/video-01\")).generate(\n                 VideoGenerationParams {\n                     prompt(\"x\")\n                 }\n             )\n         }\n+        assertEquals(\"Video generation failed: bad prompt\", error.message)\n+        assertEquals(1, fixture.calls.size)\n         assertFailsWith<NoSuchModelError> { provider.languageModel(\"model\") }\n         assertFailsWith<NoSuchModelError> { provider.embeddingModel(\"embed\") }\n     }\n",
  "integration_point": "Save artifact verbatim as /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I2.patch, then run `git -C /home/marcos/Documents/dev/opensource/aisdk-kotlin apply --check /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I2.patch && git -C /home/marcos/Documents/dev/opensource/aisdk-kotlin apply /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I2.patch`. Changed files: /home/marcos/Documents/dev/opensource/aisdk-kotlin/architecture-budget.json; /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt; /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/ReplicateProviderTest.kt; /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/ReplicateDefensiveParsingTest.kt. From /home/marcos/Documents/dev/opensource/aisdk-kotlin run, in order: `./gradlew jvmTest --tests '*Replicate*'`; `./gradlew detekt`; `./gradlew check`; `bash .claude/hooks/rules/ci-gate.sh`. Direct budget check: `node tools/check-architecture-budget.mjs`. The returned patch SHA-256 is f5827a0bf215028e25ff639fe0dd9785c6a1953b64bd05bbba0d16d6f996c3cc.",
  "bad_example": "Replicate video request-path context: POST `https://api.replicate.com/v1/models/minimax/video-01/predictions` returns `{\"id\":\"pred1\",\"status\":\"processing\",\"output\":\"https://cdn.example/premature.mp4\",\"urls\":{\"get\":\"https://api.replicate.com/v1/predictions/pred1\"}}`; the first GET returns `{\"id\":\"pred1\",\"status\":\"queued\",\"output\":\"https://cdn.example/unknown.mp4\"}`. This must throw `InvalidResponseDataError` with message exactly `Replicate prediction response has unknown status: queued` after exactly those two requests and must expose neither output.",
  "good_example": "Replicate video request-path context: POST returns `{\"status\":\"processing\",\"urls\":{\"get\":\"https://api.replicate.com/v1/predictions/pred1\"}}`; the first GET returns `{\"status\":\"succeeded\",\"output\":\"https://cdn.example/video.mp4\"}`. This must return that URL in exactly two requests and must not be caught as invalid.",
  "verification": {
    "gate_result": "Adjudication 1 quoted: \"The artifact is not landable because four newly added JSON fixture lines violate the 120-character Detekt limit, causing both `detekt` and the wave landing `check` gate to fail with seven weighted issues.\" Ruling accepted as VALID. I split exactly the three defensive fixtures and one canceled fixture with adjacent raw-string concatenation, preserving payload bytes. Proof after final restoration: `/home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I2/gradlew -p /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I2 detekt` exited 0 with `BUILD SUCCESSFUL in 4s`; the final `.../gradlew -p ... check` exited 0 with `BUILD SUCCESSFUL in 738ms`. A deliberate reversion of one wrap made Detekt fail at ReplicateDefensiveParsingTest.kt:55 with both `MaximumLineLength` and `MaxLineLength`, proving the green is caused by the repair.\n\nAdjudication 2 quoted: \"The artifact omits the mechanically required architecture file-size budget update: `ReplicateProvider.kt` grew past its tracked limit, so the campaign-close CI gate fails even if the narrow item tests pass.\" Ruling accepted as VALID. I changed only the Replicate entry from 739 to 772. Proof: `node /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I2/tools/check-architecture-budget.mjs` exited 0 with `architecture file-size budget OK: 55 large files tracked`; `bash /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I2/.claude/hooks/rules/ci-gate.sh` exited 0 and ended `architecture gate: PASS`. A deliberate 772→771 mutation made the direct checker fail with `ReplicateProvider.kt: 772 > 771`.\n\nFinal ordered gates after every mutation was restored: exact item gate `.../gradlew -p ... jvmTest --tests '*Replicate*'` → `BUILD SUCCESSFUL in 1s`; direct Detekt → `BUILD SUCCESSFUL in 4s`; wave `check` → `BUILD SUCCESSFUL in 738ms`; campaign-close CI gate → `architecture gate: PASS`. No skip flag or bypass environment variable was used.",
    "ran_it": "Executed the production Kotlin through JVM, Android host, Linux Native, metadata/ABI, Detekt, and the repository CI gate. The exact Replicate JVM XML aggregation printed: `ReplicateDefensiveParsingTest: tests=4 failures=0 errors=0 skipped=0`; `ReplicateProviderTest: tests=8 failures=0 errors=0 skipped=0`; `aggregate: files=2 tests=12 failures=0 errors=0 skipped=0`. Full `./gradlew check` ran 39 actionable tasks in the first landing pass and completed successfully; the final restored-state pass also completed successfully.",
    "positive_case": "Red before production repair: with the new permanent processing→queued poll test present against the staged baseline production, `.../gradlew -p ... jvmTest --tests '*rejects unknown status returned by poll*'` exited 1: `1 test completed, 1 failed`, `AssertionError at ReplicateProviderTest.kt:270`, `BUILD FAILED in 11s`; the baseline had returned the queued response's output instead of throwing. Green after applying the preserved I2 production delta: the same command exited 0 with `BUILD SUCCESSFUL in 2s`. The permanent assertions require message exactly `Replicate prediction response has unknown status: queued` and URLs exactly `[createUrl, pollUrl]`, proving exactly two requests and no output exposure.",
    "negative_case": "Compliant known states stayed accepted: initial succeeded array output completes in 1 request; processing→succeeded completes in 2; starting→processing→succeeded completes in 3. Failed and canceled remain terminal in 1 request with exact messages. The final exact gate ran all 12 Replicate tests with 0 failures, including unchanged Replicate image behavior.",
    "compliant_probe": "Ran the 12-test Replicate JVM matrix: image structured-error and image generation paths; initial succeeded scalar/array output; processing→succeeded; starting→processing→succeeded; failed; canceled; missing status; numeric status; initial unknown string; and polled unknown string. Request-count assertions cover 1/2/3-request paths. Separately, the exact non-string guard was executed for `42`, `true`, `null`, `{\"x\":1}`, and `[1]`; it printed `rejected=true` for JsonLiteral(number), JsonLiteral(boolean), JsonNull, JsonObject, and JsonArray. This proves the planner's `PROVEN_SAFE` disposition without adding redundant permanent cases.",
    "cannot_fail_probe": "Five independent breakages were applied temporarily and restored. (1) Changed the Unknown branch to `return prediction`; `jvmTest --tests '*unknown status*'` went red with `2 tests completed, 2 failed`: both initial-unknown and polled-unknown tests failed. (2) Removed `|| !status.isString`; `jvmTest --tests '*non-string status*'` went red with `1 test completed, 1 failed`, a `ComparisonFailure`, because numeric 42 was misclassified as an unknown string. (3) Mapped wire `processing` to Succeeded; the two transition filters ran 3 tests and produced 2 failures, both `NoVideoGeneratedError`, for processing paths. (4) Rejoined one byte-preserving fixture onto the original overlong line; `./gradlew detekt` failed with exactly 2 weighted issues at ReplicateDefensiveParsingTest.kt:55: `MaximumLineLength` and `MaxLineLength`. (5) Lowered the repaired budget from 772 to 771; `node tools/check-architecture-budget.mjs` failed with `ReplicateProvider.kt: 772 > 771`. After restoring all five, the production diff hash again matched the preserved artifact, the exact item gate, Detekt, check, direct budget checker, and CI gate all passed.",
    "expected_delta": "Required instrument, measured before→after with ast-grep against the staged baseline and repaired worktree: `replicateStatus($P)` call sites 2→0; `replicatePredictionState($P)` call sites 0→2, located at repaired ReplicateProvider.kt:658 (initial response) and :687 (each fetched response); whole-file raw `prediction[\"status\"]` reads 2→2, correctly unchanged because the image read remains and the video raw read moves from the old helper into the new single parser. This matches the prediction: one parser invocation per received video prediction without changing the independent image branch. The architecture instrument moved its tracked ceiling 739→772, and the checker measured the repaired file at exactly 772. The formatting instrument moved the four named overlong fixtures from 7 weighted Detekt issues in the adjudication to 0; all four payload comparisons reported identical=True. The permanent poll-unknown seam moved from 0→1 test and baseline red→repair green. These are the predicted deltas; none is an unmoved false landing.",
    "real_occurrences": "/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt:658 and :687 are the two parse points; :708-730 is the private parser/state domain. /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/ReplicateProviderTest.kt:270 anchors the permanent polled-unknown request-path regression. /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/ReplicateDefensiveParsingTest.kt:52-103 contains initial missing/non-string/unknown rejection coverage. /home/marcos/Documents/dev/opensource/aisdk-kotlin/architecture-budget.json:46 records the exact 772 ceiling."
  },
  "duplication_check": "The exact existing owner was private `replicateStatus(JsonObject): String` in /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt. The artifact replaces it rather than adding a parallel source of truth: ast-grep measured `replicateStatus` calls 2→0 and `replicatePredictionState` calls 0→2. There is one raw video status parser, and its six private variants explicitly map all five known provider strings plus Unknown(raw). The independent image status read is deliberately not reused because the item freezes image handling as output-authoritative; whole-file raw status reads correctly remain 2→2. No public type or generic poller/PollState is added.",
  "known_soft_spots": "The weakest retained joint is that permanent non-string coverage names only numeric status; boolean, JSON null, object, and array are not individually persisted. This is deliberate under the plan, and the exact shared predicate was dynamically executed for all five shapes with rejected=true, but a future refactor splitting that predicate would warrant expanding permanent cases. Timeout arithmetic and malformed `urls.get` handling remain intentionally outside this repair; ast-grep measured the `ceil(...).toInt().coerceAtLeast(1)` expression, `attempts >= maxPollAttempts` branch, and missing-urls.get throw at baseline=1/repaired=1 each, so this patch neither improves nor regresses those policies. HTTP request counts do not directly count in-memory property reads; the structural proof is therefore separate: exactly two `replicatePredictionState` calls, one at initial receipt and one immediately after each GET assignment, with zero legacy helper calls. Adjacent raw-string concatenation depends on Kotlin's ordinary string `+` semantics; compile/test and four before/after SHA-256 comparisons prove the current bytes, but future formatter rewrites should preserve the no-whitespace boundary. Finally, 772 is intentionally an exact measured budget rather than headroom: any future legitimate production growth must be separately reviewed and remeasured."
}
