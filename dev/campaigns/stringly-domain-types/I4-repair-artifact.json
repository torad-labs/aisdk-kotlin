{
  "id": "I4",
  "written_scope": "Complete corrected I4 artifact over the staged `/home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/wave-I4-baseline.patch`. In `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt`, the private Fireworks poll path now validates the top-level payload once as `JsonObject`, extracts only a genuine JSON-string status from that local object, preserves `Pending`/`Ready`/`Error`/`Failed` behavior, and terminates unknown strings with `InvalidResponseDataError`. `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt` exercises the real Ktor test-server path for transitions, credentials, malformed `Ready.result.sample`, unknown strings, all five non-object top-level JSON categories, and all non-string status-member categories. `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsPollingStatusTest.kt` is preserved byte-for-byte from the prior complete I4 artifact so the returned patch remains independently landable over the staged baseline. Non-goals: no Black Forest Labs production change; no shared poller, status enum/sealed hierarchy, retry/backoff/abort/origin-parser change; no public symbol, ABI dump, CHANGELOG, or INTERFACE_CONTRACT delta; no commit.",
  "recommendation": "LAND",
  "premise_check": "SOUND. Quoted constraint: “immediately after the poll HTTP response is obtained, safely cast `response.value` exactly once to a local `JsonObject`.” The artifact has one structural hit for `response.value as? JsonObject` and zero for `response.value.jsonObject` in the poll method. Quoted constraint: “If that cast fails, throw `InvalidResponseDataError` immediately with the original `response.value` as `data` and the exact existing malformed-status diagnostic.” The final 11-row malformed table passes and asserts the exact payload, diagnostic, one poll, two total calls, and zero downloads for every row. Quoted constraint: “absent, JSON-null, numeric, boolean, array, or object-valued status members” must use the same typed error. The table includes all six member shapes; the five pinned non-object top-level shapes (`[]`, `null`, `\"payload\"`, `123`, `true`) are also included. Quoted constraint: “derive `result` and `sample` from the same validated local object.” `Ready` reads through `JsonAccess.obj(responseObject, \"result\")` and accepts only a string sample. Quoted constraint: “Leave the `Pending`, `Ready`, `Error`/`Failed`, and unknown branches otherwise byte-for-byte equivalent in behavior.” The exact selected gate passed 17/17 tests; request-count, POST-body, download, error-class, and credential assertions all remain green. Quoted constraint: “Do not alter `BlackForestLabsPollingStatusTest.kt`.” Relative to the prior I4 artifact it is byte-identical (`git` blob/index `186a4f3`); it is present in this complete patch only because the return artifact must apply directly over the staged pre-wave baseline. Quoted constraint: “Do not change Fireworks retry limits, interval/backoff arithmetic, request URL/method/body, abort behavior, image download behavior, or status policy.” The only production hunk is the classifier at lines 395–434; constants, delay arithmetic, request construction, abort calls, and download/origin helpers have no artifact hunk. Quoted constraint: “Do not change any Black Forest Labs production behavior.” `git diff --quiet -- src/commonMain/kotlin/ai/torad/aisdk/providers/BlackForestLabsProvider.kt` succeeded. Quoted constraint: “Do not add … public symbol … ABI dump delta, `CHANGELOG.md`, or `INTERFACE_CONTRACT.md` change.” The artifact has zero public data classes and `git diff --quiet` over both ABI dumps and both contract documents printed `ARTIFACT_PUBLIC_CONTRACT_DELTA=none`. Quoted constraint: “preserve the staged baseline.” The cached binary diff remained byte-for-byte equal to the baseline patch (SHA-256 `2e818c0ad5494abb8e251b2d4852b9055fa3061b833b296c5ba8ae7b8b216c93`). Measured pinned complete scope: 3 files. Measured defensible complete scope: the same 3 files, 343 insertions and 3 deletions; repair-specific behavior is confined to Fireworks production plus its test, while the BFL test is preserved context. The two additional status-member rows (boolean and array) are not a scope widening: the repair specification explicitly names those member types and the high-bar instruction requires closing the neighboring input class.",
  "artifact": "diff --git a/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt b/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt\nindex 9214079..5745c5b 100644\n--- a/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt\n+++ b/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt\n@@ -392,11 +392,25 @@ public class FireworksImageModel(\n                     abortSignal = abortSignal,\n                 )\n             }\n-            val status = (response.value.jsonObject[\"status\"] as? JsonPrimitive)?.contentOrNull\n+            val responseObject = response.value as? JsonObject\n+                ?: throw InvalidResponseDataError(\n+                    response.value,\n+                    \"Fireworks poll response is missing a string status\",\n+                )\n+            val status = (responseObject[\"status\"] as? JsonPrimitive)\n+                ?.takeIf { it.isString }\n+                ?.contentOrNull\n+                ?: throw InvalidResponseDataError(\n+                    response.value,\n+                    \"Fireworks poll response is missing a string status\",\n+                )\n             when (status) {\n+                \"Pending\" -> Unit\n                 \"Ready\" -> {\n-                    val sample = (JsonAccess.obj(response.value.jsonObject, \"result\"))?.get(\"sample\")\n-                    return (sample as? JsonPrimitive)?.contentOrNull\n+                    val sample = (JsonAccess.obj(responseObject, \"result\"))?.get(\"sample\")\n+                    return (sample as? JsonPrimitive)\n+                        ?.takeIf { it.isString }\n+                        ?.contentOrNull\n                         ?: throw InvalidResponseDataError(\n                             response.value,\n                             \"Fireworks poll response is Ready but missing result.sample\",\n@@ -406,6 +420,10 @@ public class FireworksImageModel(\n                     message = \"Fireworks image generation failed with status: $status\",\n                     url = pollUrl,\n                 )\n+                else -> throw InvalidResponseDataError(\n+                    response.value,\n+                    \"Fireworks poll response has unknown status: '$status'\",\n+                )\n             }\n             if (attempt < FIREWORKS_MAX_POLL_ATTEMPTS - 1) {\n                 delay(pollDelayMillis)\ndiff --git a/src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsPollingStatusTest.kt b/src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsPollingStatusTest.kt\nnew file mode 100644\nindex 0000000..186a4f3\n--- /dev/null\n+++ b/src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsPollingStatusTest.kt\n@@ -0,0 +1,110 @@\n+package ai.torad.aisdk\n+\n+import ai.torad.aisdk.providers.BlackForestLabs\n+import ai.torad.aisdk.providers.BlackForestLabsProviderSettings\n+import kotlinx.coroutines.test.runTest\n+import kotlinx.serialization.json.Json\n+import kotlinx.serialization.json.JsonElement\n+import kotlinx.serialization.json.JsonPrimitive\n+import kotlinx.serialization.json.buildJsonObject\n+import kotlin.test.Test\n+import kotlin.test.assertEquals\n+import kotlin.test.assertFailsWith\n+import kotlin.test.assertTrue\n+\n+class BlackForestLabsPollingStatusTest {\n+    @Test\n+    fun `named terminal statuses fail after one poll`() = runTest {\n+        val statuses = listOf(\n+            \"Error\",\n+            \"Failed\",\n+            \"Content Moderated\",\n+            \"Request Moderated\",\n+            \"Task not found\",\n+        )\n+\n+        for (status in statuses) {\n+            val fixture = bflFixture(\n+                buildJsonObject { put(\"status\", JsonPrimitive(status)) },\n+            )\n+\n+            val error = assertFailsWith<NoImageGeneratedError> {\n+                fixture.model.generateOne()\n+            }\n+\n+            assertTrue(error.message.orEmpty().contains(status))\n+            assertEquals(1, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+            assertEquals(2, fixture.server.calls.size)\n+        }\n+    }\n+\n+    @Test\n+    fun `missing status fails as malformed after one poll`() = runTest {\n+        val fixture = bflFixture(Json.parseToJsonElement(\"{}\"))\n+\n+        val error = assertFailsWith<InvalidResponseDataError> {\n+            fixture.model.generateOne()\n+        }\n+\n+        assertTrue(error.message.orEmpty().contains(\"Missing status\"))\n+        assertEquals(1, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+        assertEquals(2, fixture.server.calls.size)\n+    }\n+\n+    @Test\n+    fun `unknown string remains retryable until timeout`() = runTest {\n+        val fixture = bflFixture(\n+            buildJsonObject { put(\"status\", JsonPrimitive(\"Queued\")) },\n+        )\n+\n+        val error = assertFailsWith<NoImageGeneratedError> {\n+            fixture.model.generateOne()\n+        }\n+\n+        assertTrue(error.message.orEmpty().contains(\"timed out\"))\n+        assertEquals(3, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+        assertEquals(4, fixture.server.calls.size)\n+    }\n+\n+    private fun bflFixture(pollResponse: JsonElement): BflFixture {\n+        val server = TestServer.createTestServer(\n+            mutableMapOf(\n+                SUBMIT_URL to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\n+                            \"\"\"{\"id\":\"req1\",\"polling_url\":\"$POLL_BASE_URL\"}\"\"\",\n+                        ),\n+                    ),\n+                ),\n+                POLL_URL to UrlHandler(UrlResponse.JsonValue(pollResponse)),\n+            ),\n+        )\n+        server.server.start()\n+        val model = BlackForestLabs(\n+            server.httpClient(),\n+            BlackForestLabsProviderSettings {\n+                apiKey(\"key\")\n+                baseURL(BASE_URL)\n+                pollIntervalMillis(1)\n+                pollTimeoutMillis(3)\n+            },\n+        ).image(ModelId(MODEL_ID))\n+        return BflFixture(server, model)\n+    }\n+\n+    private suspend fun ImageModel.generateOne(): ImageModelResult =\n+        generate(ImageGenerationParams { prompt(\"x\") })\n+\n+    private data class BflFixture(\n+        val server: CreatedTestServer,\n+        val model: ImageModel,\n+    )\n+\n+    private companion object {\n+        const val BASE_URL = \"https://bfl.test/v1\"\n+        const val MODEL_ID = \"flux-pro-1.1\"\n+        const val SUBMIT_URL = \"$BASE_URL/$MODEL_ID\"\n+        const val POLL_BASE_URL = \"$BASE_URL/poll\"\n+        const val POLL_URL = \"$POLL_BASE_URL?id=req1\"\n+    }\n+}\ndiff --git a/src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt b/src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt\nnew file mode 100644\nindex 0000000..d104132\n--- /dev/null\n+++ b/src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt\n@@ -0,0 +1,212 @@\n+package ai.torad.aisdk\n+\n+import ai.torad.aisdk.providers.Fireworks\n+import ai.torad.aisdk.providers.FireworksProviderSettings\n+import io.ktor.http.HttpHeaders\n+import kotlinx.coroutines.test.runTest\n+import kotlinx.serialization.json.Json\n+import kotlinx.serialization.json.JsonElement\n+import kotlinx.serialization.json.JsonPrimitive\n+import kotlinx.serialization.json.buildJsonObject\n+import kotlinx.serialization.json.contentOrNull\n+import kotlinx.serialization.json.jsonObject\n+import kotlinx.serialization.json.jsonPrimitive\n+import kotlin.test.Test\n+import kotlin.test.assertEquals\n+import kotlin.test.assertFailsWith\n+import kotlin.test.assertNull\n+import kotlin.test.assertTrue\n+\n+class FireworksPollingStatusTest {\n+    @Test\n+    fun `Pending retries and Ready returns its validated sample`() = runTest {\n+        val fixture = fireworksFixture(\n+            listOf(\n+                buildJsonObject { put(\"status\", JsonPrimitive(\"Pending\")) },\n+                readyResponse(SAME_ORIGIN_IMAGE_URL),\n+            ),\n+        )\n+\n+        val result = fixture.model.generateOne()\n+\n+        assertEquals(Base64Codec.encode(IMAGE_BYTES), result.images.single().base64)\n+        val polls = fixture.server.calls.filter { it.requestUrl == POLL_URL }\n+        assertEquals(2, polls.size)\n+        assertTrue(polls.all { it.requestMethod == \"POST\" })\n+        assertTrue(\n+            polls.all {\n+                it.requestBodyJson.jsonObject[\"id\"]?.jsonPrimitive?.contentOrNull == \"req1\"\n+            },\n+        )\n+\n+        val download = fixture.server.calls.single { it.requestUrl == SAME_ORIGIN_IMAGE_URL }\n+        assertEquals(\"Bearer key\", download.requestHeaders.headerValue(HttpHeaders.Authorization))\n+        assertEquals(\"secondary\", download.requestHeaders.headerValue(\"x-api-key\"))\n+    }\n+\n+    @Test\n+    fun `Ready does not send credentials to a cross-origin sample`() = runTest {\n+        val fixture = fireworksFixture(\n+            pollResponses = listOf(readyResponse(CROSS_ORIGIN_IMAGE_URL)),\n+            imageUrl = CROSS_ORIGIN_IMAGE_URL,\n+        )\n+\n+        fixture.model.generateOne()\n+\n+        assertEquals(1, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+        val download = fixture.server.calls.single { it.requestUrl == CROSS_ORIGIN_IMAGE_URL }\n+        assertNull(download.requestHeaders.headerValue(HttpHeaders.Authorization))\n+        assertNull(download.requestHeaders.headerValue(\"x-api-key\"))\n+    }\n+\n+    @Test\n+    fun `Ready requires a string result sample without another poll`() = runTest {\n+        val responses = listOf(\n+            buildJsonObject {\n+                put(\"status\", JsonPrimitive(\"Ready\"))\n+                put(\"result\", buildJsonObject {})\n+            },\n+            buildJsonObject {\n+                put(\"status\", JsonPrimitive(\"Ready\"))\n+                put(\"result\", buildJsonObject { put(\"sample\", JsonPrimitive(42)) })\n+            },\n+        )\n+\n+        for (response in responses) {\n+            val fixture = fireworksFixture(listOf(response))\n+\n+            val error = assertFailsWith<InvalidResponseDataError> {\n+                fixture.model.generateOne()\n+            }\n+\n+            assertTrue(error.message.orEmpty().contains(\"missing result.sample\"))\n+            assertEquals(1, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+            assertEquals(2, fixture.server.calls.size)\n+            assertEquals(0, fixture.server.calls.count { it.requestUrl == SAME_ORIGIN_IMAGE_URL })\n+        }\n+    }\n+\n+    @Test\n+    fun `Error and Failed preserve immediate API call errors`() = runTest {\n+        for (status in listOf(\"Error\", \"Failed\")) {\n+            val fixture = fireworksFixture(\n+                listOf(buildJsonObject { put(\"status\", JsonPrimitive(status)) }),\n+            )\n+\n+            val error = assertFailsWith<APICallError> {\n+                fixture.model.generateOne()\n+            }\n+\n+            assertEquals(POLL_URL, error.url)\n+            assertTrue(error.message.orEmpty().contains(status))\n+            assertEquals(1, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+            assertEquals(2, fixture.server.calls.size)\n+        }\n+    }\n+\n+    @Test\n+    fun `missing null non-string and non-object statuses fail as malformed after one poll`() = runTest {\n+        val responses = listOf(\n+            Json.parseToJsonElement(\"{}\"),\n+            Json.parseToJsonElement(\"\"\"{\"status\":null}\"\"\"),\n+            buildJsonObject { put(\"status\", JsonPrimitive(123)) },\n+            Json.parseToJsonElement(\"\"\"{\"status\":true}\"\"\"),\n+            Json.parseToJsonElement(\"\"\"{\"status\":[]}\"\"\"),\n+            buildJsonObject { put(\"status\", buildJsonObject { put(\"phase\", JsonPrimitive(\"Pending\")) }) },\n+            Json.parseToJsonElement(\"[]\"),\n+            Json.parseToJsonElement(\"null\"),\n+            Json.parseToJsonElement(\"\\\"payload\\\"\"),\n+            Json.parseToJsonElement(\"123\"),\n+            Json.parseToJsonElement(\"true\"),\n+        )\n+\n+        for (response in responses) {\n+            val fixture = fireworksFixture(listOf(response))\n+\n+            val error = assertFailsWith<InvalidResponseDataError> {\n+                fixture.model.generateOne()\n+            }\n+\n+            assertTrue(error.message.orEmpty().contains(\"Fireworks poll response is missing a string status\"))\n+            assertEquals(response, error.data)\n+            assertEquals(1, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+            assertEquals(2, fixture.server.calls.size)\n+            assertEquals(0, fixture.server.calls.count { it.requestUrl == SAME_ORIGIN_IMAGE_URL })\n+        }\n+    }\n+\n+    @Test\n+    fun `unknown and blank string statuses preserve the raw value without retrying`() = runTest {\n+        for (status in listOf(\"Queued\", \"\", \" \")) {\n+            val response = buildJsonObject { put(\"status\", JsonPrimitive(status)) }\n+            val fixture = fireworksFixture(listOf(response))\n+\n+            val error = assertFailsWith<InvalidResponseDataError> {\n+                fixture.model.generateOne()\n+            }\n+\n+            assertTrue(error.message.orEmpty().contains(\"unknown status: '$status'\"))\n+            assertEquals(response, error.data)\n+            assertEquals(1, fixture.server.calls.count { it.requestUrl == POLL_URL })\n+            assertEquals(2, fixture.server.calls.size)\n+        }\n+    }\n+\n+    private fun fireworksFixture(\n+        pollResponses: List<JsonElement>,\n+        imageUrl: String = SAME_ORIGIN_IMAGE_URL,\n+    ): FireworksFixture {\n+        var pollResponseIndex = 0\n+        val server = TestServer.createTestServer(\n+            mutableMapOf(\n+                SUBMIT_URL to UrlHandler(\n+                    UrlResponse.JsonValue(Json.parseToJsonElement(\"\"\"{\"request_id\":\"req1\"}\"\"\")),\n+                ),\n+                POLL_URL to UrlHandler {\n+                    val response = pollResponses.getOrElse(pollResponseIndex) { pollResponses.last() }\n+                    pollResponseIndex++\n+                    UrlResponse.JsonValue(response)\n+                },\n+                imageUrl to UrlHandler(\n+                    UrlResponse.Binary(IMAGE_BYTES, headers = mapOf(HttpHeaders.ContentType to \"image/png\")),\n+                ),\n+            ),\n+        )\n+        server.server.start()\n+        val model = Fireworks(\n+            server.httpClient(),\n+            FireworksProviderSettings {\n+                apiKey(\"key\")\n+                baseURL(BASE_URL)\n+                headers(mapOf(\"X-Api-Key\" to \"secondary\"))\n+            },\n+        ).image(ModelId(MODEL_ID))\n+        return FireworksFixture(server, model)\n+    }\n+\n+    private suspend fun ImageModel.generateOne(): ImageModelResult =\n+        generate(ImageGenerationParams { prompt(\"x\") })\n+\n+    private fun readyResponse(imageUrl: String): JsonElement = buildJsonObject {\n+        put(\"status\", JsonPrimitive(\"Ready\"))\n+        put(\"result\", buildJsonObject { put(\"sample\", JsonPrimitive(imageUrl)) })\n+    }\n+\n+    private fun Map<String, String>.headerValue(name: String): String? =\n+        entries.firstOrNull { it.key.equals(name, ignoreCase = true) }?.value\n+\n+    private data class FireworksFixture(\n+        val server: CreatedTestServer,\n+        val model: ImageModel,\n+    )\n+\n+    private companion object {\n+        const val BASE_URL = \"https://fireworks.test\"\n+        const val MODEL_ID = \"accounts/fireworks/models/flux-kontext-pro\"\n+        const val SUBMIT_URL = \"$BASE_URL/workflows/$MODEL_ID\"\n+        const val POLL_URL = \"$SUBMIT_URL/get_result\"\n+        const val SAME_ORIGIN_IMAGE_URL = \"$BASE_URL/image.png\"\n+        const val CROSS_ORIGIN_IMAGE_URL = \"https://cdn.example/image.png\"\n+        val IMAGE_BYTES = byteArrayOf(1, 2, 3)\n+    }\n+}\n",
  "integration_point": "Save `artifact` verbatim as `/home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I4.patch`. Apply over the already-present combined baseline with `git -C /home/marcos/Documents/dev/opensource/aisdk-kotlin apply --whitespace=error-all /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I4.patch`. Changed files: `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt`; `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt`; `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsPollingStatusTest.kt`. Verify from `/home/marcos/Documents/dev/opensource/aisdk-kotlin` with `./gradlew --no-daemon --max-workers=2 jvmTest --tests '*Fireworks*' --tests '*BlackForestLabs*' --rerun-tasks`.",
  "bad_example": "Enclosing HTTP flow: submit returns `{\"request_id\":\"req1\"}` and the first `POLL_URL` response is the valid JSON value `[]` (neighboring cases: top-level `null`, `\"payload\"`, `123`, or `true`). The old direct `.jsonObject` accessor throws `IllegalArgumentException`; the repaired classifier must instead throw `InvalidResponseDataError` after exactly that one poll, retain the original `JsonElement` as `data`, make two total HTTP calls, and make no image download.",
  "good_example": "Enclosing HTTP flow: submit returns `{\"request_id\":\"req1\"}`; poll 1 is `{\"status\":\"Pending\"}`; poll 2 is `{\"status\":\"Ready\",\"result\":{\"sample\":\"https://fireworks.test/image.png\"}}`. This remains compliant: two POST polls with body `{\"id\":\"req1\"}`, then one same-origin image download carrying the configured credentials.",
  "verification": {
    "gate_result": "Final exact item gate run from the repair clone: `/home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/i4-opus-1-repair-I4/gradlew -p /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/i4-opus-1-repair-I4 --no-daemon --max-workers=2 jvmTest --tests '*Fireworks*' --tests '*BlackForestLabs*' --rerun-tasks`. Exact terminal summary: `BUILD SUCCESSFUL in 36s`, `7 actionable tasks: 7 executed`. XML aggregation printed `FINAL_I4_JUNIT_TOTALS tests=17 failures=0 errors=0 skipped=0` (Fireworks polling 6, BFL polling 3, and 8 selected existing tests).",
    "ran_it": "Executed the production Ktor path, not builder equality. The final malformed focused command returned `BUILD SUCCESSFUL in 35s`; its single table test iterated 11 payloads and, on each passing iteration, asserted `polls=1`, `total HTTP calls=2`, `downloads=0`, exact `error.data`, and the exact diagnostic. The final full selected run printed 17 tests, 0 failures, 0 errors, 0 skipped. The staged-baseline integrity check printed `CACHED_BASELINE_EXACT=yes`; structural checks printed `SAFE_POLL_CASTS=1`, `DIRECT_POLL_JSONOBJECT_ACCESS=0`, and `PUBLIC_DATA_CLASSES_IN_ARTIFACT_FILES=0`.",
    "positive_case": "Adjudication 1, quoted: “Reviewer r1-p1: a valid top-level JSON array reaches `response.value.jsonObject` before the artifact's status validation, so `IllegalArgumentException` escapes instead of the promised `InvalidResponseDataError`.” Ruling accepted as VALID. I replaced the direct accessor with the one safe `JsonObject` cast and added all five non-object top-level categories to the permanent HTTP-path table. Proof command: `.../gradlew -p ... --no-daemon --max-workers=2 jvmTest --tests '*FireworksPollingStatusTest*missing*' --rerun-tasks`; the final repaired run was `BUILD SUCCESSFUL in 35s`. Adjudication 2, quoted: “Reviewer r1-p2: the same pre-validation `.jsonObject` accessor lets a top-level array bypass the typed malformed-response error and violates the written one-poll failure contract.” Ruling accepted as VALID. The same permanent test asserts one poll, two total calls, no download, original data, and exact diagnostic for `[]`, `null`, string, number, and boolean; the final focused command passed. Both objections are closed as an input class, not only for `[]`.",
    "negative_case": "After restoring the safe guard, the malformed focused command passed (`BUILD SUCCESSFUL in 35s`). After restoring the unknown-status terminal throw, `... --tests '*FireworksPollingStatusTest*unknown*' --rerun-tasks` passed (`BUILD SUCCESSFUL in 36s`). The compliant `Pending`→`Ready` path also passed in the final gate and retains two POST polls, request id `req1`, successful download, and same-origin credentials; cross-origin credentials remain absent.",
    "compliant_probe": "Final permanent probes exercised: object payload with missing status; null, number, boolean, array, and object-valued status members; top-level array, null, string, number, and boolean payloads; `Pending`→`Ready`; immediate `Ready`; missing/numeric `result.sample`; `Error`; `Failed`; unknown `Queued`; empty and whitespace strings; same-origin credential forwarding; cross-origin credential stripping. The BFL suite retained five named terminal values, missing status, and retryable unknown `Queued`. All selected probes passed in the 17-test gate.",
    "cannot_fail_probe": "Representative production mutation 1 replaced the final safe top-level cast with the former `response.value.jsonObject`. Without changing the final test, `... --tests '*FireworksPollingStatusTest*missing*' --rerun-tasks` produced exactly `1 test completed, 1 failed`; XML said expected `InvalidResponseDataError` but got `IllegalArgumentException: Element class kotlinx.serialization.json.JsonArray is not a JsonObject`. Restoration returned `BUILD SUCCESSFUL in 35s`. Independent mutation 2 replaced only the unknown-status throw with `else -> Unit`. `... --tests '*FireworksPollingStatusTest*unknown*' --rerun-tasks` produced exactly one failed test caused by `APICallError: Fireworks image generation timed out after polling`; restoration returned `BUILD SUCCESSFUL in 36s`.",
    "expected_delta": "Repair instrument: exception type plus poll/download counts for valid non-object poll JSON. BEFORE-equivalent mutation (former direct accessor): top-level array reached poll 1 and escaped as `IllegalArgumentException`; the focused test failed. AFTER: all five non-object top-level categories and all six malformed object-member categories produce `InvalidResponseDataError`, exactly 1 poll, 2 total HTTP calls, and 0 downloads; the focused test passes. Secondary status-policy instrument: unknown-string mutation changed the terminal result to eventual timeout `APICallError`; restored code returns immediate `InvalidResponseDataError` and the passing test asserts exactly 1 poll. These deltas match the prediction. BFL production was predicted not to move and `BFL_PRODUCTION_ARTIFACT_DELTA=none`.",
    "real_occurrences": "/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt:395-434 is the single corrected Fireworks poll classifier. /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt:108-136 is the malformed-payload table and one-poll contract; lines 139-153 lock unknown/blank terminality. /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/BlackForestLabsProvider.kt:621-649 remains the separate unchanged BFL policy owner."
  },
  "duplication_check": "Checked the existing owner in `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/MediaPollRobustnessTest.kt:285-325`: it covers Fireworks `Pending` backoff through a final `Ready`, but not malformed top-level JSON, status wire types, unknown terminality, sample validation, or credentials. The new Fireworks test therefore owns the missing transition/shape contract rather than duplicating the backoff test. The preserved BFL test covers the complete named-terminal/missing/unknown request-count policy not fully pinned by the existing provider tests. Production remains provider-local; no shared poller/classifier was added.",
  "known_soft_spots": "1) Unknown Fireworks strings remain machine-readable through the original `JsonObject` in `InvalidResponseDataError.data`, but there is no dedicated public status field; adding one is an explicitly forbidden public-API follow-up. 2) `Ready.result.sample` rejects non-string primitives and the permanent numeric case proves one-poll/no-download behavior, but other non-string sample shapes are governed by the same cast rather than individually tabled. 3) Same-origin/cross-origin tests cover ordinary HTTPS URLs; explicit ports, IPv6, userinfo, and normalization edges in the pre-existing manual origin parser remain untested. This artifact has no hunk in `sameOrigin` or `headersForImageDownload`, so that is a separate origin-parser proposal. 4) BFL's intentionally open unknown policy is pinned with `Queued`; blank and non-string BFL statuses are not exhaustively tabled. The preserved BFL production file has no unstaged delta. 5) This repair did not run an all-`Pending` 240-attempt timeout under the resource law. The artifact leaves all three Fireworks polling constants, the repeat bound, delay arithmetic, and timeout throw untouched; the pinned plan records a prior focused backoff-to-final-fetch proof. 6) The 11 malformed inputs are rows inside one JUnit method: a future failure in an early row can mask later rows until the first regression is fixed, although the current green run necessarily traverses and asserts every row."
}
