{
  "id": "I1",
  "written_scope": "Repairs Rev.ai transcription-state handling in exactly two private/item-scoped files. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt, submission and poll responses are each decoded once into a private immutable RevaiJobSnapshot with explicit Transcribed/Failed/InProgress mappings and Unknown(raw); initial Failed precedes id validation, id validation precedes malformed-status rejection, only InProgress enters the provider-local pollRevaiJob helper, normalized attempts retain the one-GET floor, delay occurs only between nonterminal attempts, a terminal poll exits immediately, abort is checked before each status GET and immediately before the sole transcript GET, the complete provider response remains on InvalidResponseDataError, and terminal language comes from the terminal snapshot. The pre-existing one-use revaiOptions helper is inlined without changing its expression so the added polling member does not exceed detekt's class-function threshold. In /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt, 15 HTTP-path tests now cover valid-id and no-id initial Failed states, direct Transcribed, polled Failed, exact attempt bounds, malformed initial precedence, missing/numeric/unknown initial statuses, complete malformed-response retention, four malformed polled shapes, delayed InProgress→InProgress→Transcribed behavior, and maxPollAttempts 0/-1. Explicit non-goals: no public API, ABI dump, CHANGELOG.md, INTERFACE_CONTRACT.md, provider option/config wire behavior, transcript mapping, header/user-agent behavior, error type/message, generic poller/PollState, public data class, MIME/discriminator/identifier, or Google interactions change.",
  "recommendation": "LAND",
  "premise_check": "SOUND. Constraint \"Production repair, exactly one provider-local extraction ... Add a private member pollRevaiJob\": pinned scope was 1 private helper in 1 production file; defensible and actual scope is exactly 1 helper in that file. The one-use revaiOptions helper was mechanically inlined with the identical expression to hold RevaiTranscriptionModel at detekt's 11-function threshold; this is part of making the exact extraction landable, not a second abstraction. Constraint \"decode the submit response once ... Failed before id; then require id; then reject Unknown; only InProgress calls pollRevaiJob; initial Transcribed goes directly to transcript\": pinned state pool was 3 known Rev.ai transcription wire strings plus 1 Unknown carrier; defensible and actual pool is the same 3+Unknown, with 1 decoder call at submit and 1 per poll. Constraint \"pollRevaiJob ... attempts = maxPollAttempts.coerceAtLeast(1) ... abort-check, one GET, one decode, terminal return/failure/unknown rejection, between-attempt delay, timeout\": pinned control surface was 4 state outcomes, 1 normalized bound, 1 delay site, and 1 timeout exit; defensible and actual surface is exactly those counts. Constraint \"keep the abort check immediately before the single transcript GET and derive result language from the terminal snapshot\": pinned and defensible scope is 1 pre-fetch abort check, 1 transcript GET, and 1 terminal language source; actual matches. Constraint \"Leave RevaiJobSnapshot, RevaiJobState, and RevaiJobSnapshotDecoder private ... three explicit wire mappings and Unknown(raw) ... value = value\": pinned and defensible representation is 3 private declarations, 3 explicit strings, 1 raw unknown carrier, and 1 full-response field; actual matches. Constraint \"Test repair ... keep existing cases and add valid-id Failed, response retention, four malformed polls, positive-delay terminality, and nonpositive floor\": pinned final focused inventory was 15 RevaiProviderTest methods plus 1 RevaiDefensiveParsingTest; defensible and measured inventory is exactly 15+1, with 16/16 passing. Constraint \"Touch only item-scoped files\": pinned allowance was 2 files; defensible and actual unstaged artifact is 2 files (1 production, 1 test), 372 insertions and 47 deletions. Constraint \"no public API, ABI, changelog, interface-contract, generic poller, or option/config expansion\": pinned and defensible change count is 0 for each category; checkKotlinAbi passed across JVM/Linux/Apple and the unstaged diff contains no API/doc/config file.",
  "artifact": "diff --git a/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt b/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt\nindex 2ccfec4..6c1f9c6 100644\n--- a/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt\n+++ b/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt\n@@ -24,7 +24,6 @@ import kotlinx.serialization.json.JsonPrimitive\n import kotlinx.serialization.json.buildJsonObject\n import kotlinx.serialization.json.contentOrNull\n import kotlinx.serialization.json.floatOrNull\n-import kotlinx.serialization.json.jsonObject\n \n public const val REVAI_VERSION: String = \"2.0.33\"\n \n@@ -410,37 +409,21 @@ private class RevaiTranscriptionModel(\n             params = params,\n             headers = settings.revaiHeaders(params.headers),\n         )\n-        var job = submit.value.jsonObject\n-        if ((job[\"status\"] as? JsonPrimitive)?.contentOrNull == \"failed\") {\n+        val submitted = RevaiJobSnapshotDecoder.decode(submit.value)\n+        if (submitted.state is RevaiJobState.Failed) {\n             throw NoTranscriptGeneratedError(\"Failed to submit transcription job to Rev.ai\")\n         }\n-        val jobId = (job[\"id\"] as? JsonPrimitive)?.contentOrNull\n+        val jobId = submitted.id\n             ?: throw InvalidResponseDataError(submit.value, \"Rev.ai transcription job response is missing id\")\n+        if (submitted.state is RevaiJobState.Unknown) submitted.throwInvalidStatus()\n \n-        repeat(settings.maxPollAttempts.coerceAtLeast(1)) { attempt ->\n-            params.abortSignal.throwIfAborted()\n-            val status = (job[\"status\"] as? JsonPrimitive)?.contentOrNull\n-            if (status == \"transcribed\") return@repeat\n-            if (attempt > 0 || status != \"transcribed\") {\n-                val poll = revaiGetJson(\n-                    url = \"$REVAI_BASE_URL/speechtotext/v1/jobs/$jobId\",\n-                    headers = settings.revaiHeaders(params.headers),\n-                    abortSignal = params.abortSignal,\n-                )\n-                job = poll.value.jsonObject\n-                when ((job[\"status\"] as? JsonPrimitive)?.contentOrNull) {\n-                    \"transcribed\" -> return@repeat\n-                    \"failed\" -> throw NoTranscriptGeneratedError(\"Rev.ai transcription job failed\")\n-                }\n-            }\n-            if ((job[\"status\"] as? JsonPrimitive)?.contentOrNull != \"transcribed\" && settings.pollingIntervalMillis > 0 && attempt < settings.maxPollAttempts - 1) {\n-                delay(settings.pollingIntervalMillis)\n-            }\n-        }\n-        if ((job[\"status\"] as? JsonPrimitive)?.contentOrNull != \"transcribed\") {\n-            throw NoTranscriptGeneratedError(\"Rev.ai transcription job polling timed out\")\n+        val terminal = if (submitted.state is RevaiJobState.InProgress) {\n+            pollRevaiJob(jobId, params)\n+        } else {\n+            submitted\n         }\n \n+        params.abortSignal.throwIfAborted()\n         val transcript = revaiGetJson(\n             url = \"$REVAI_BASE_URL/speechtotext/v1/jobs/$jobId/transcript\",\n             headers = settings.revaiHeaders(params.headers),\n@@ -456,11 +439,37 @@ private class RevaiTranscriptionModel(\n                 body = transcript.value,\n             ),\n             providerMetadata = ProviderMetadata.Raw(JsonObject(mapOf(\"revai\" to transcript.value))),\n-            language = (job[\"language\"] as? JsonPrimitive)?.contentOrNull,\n+            language = terminal.language,\n             durationInSeconds = mapped.durationInSeconds,\n         )\n     }\n \n+    private suspend fun pollRevaiJob(\n+        jobId: String,\n+        params: TranscriptionParams,\n+    ): RevaiJobSnapshot {\n+        val attempts = settings.maxPollAttempts.coerceAtLeast(1)\n+        for (attempt in 0 until attempts) {\n+            params.abortSignal.throwIfAborted()\n+            val poll = revaiGetJson(\n+                url = \"$REVAI_BASE_URL/speechtotext/v1/jobs/$jobId\",\n+                headers = settings.revaiHeaders(params.headers),\n+                abortSignal = params.abortSignal,\n+            )\n+            val snapshot = RevaiJobSnapshotDecoder.decode(poll.value)\n+            when (snapshot.state) {\n+                RevaiJobState.Transcribed -> return snapshot\n+                RevaiJobState.Failed -> throw NoTranscriptGeneratedError(\"Rev.ai transcription job failed\")\n+                RevaiJobState.InProgress -> Unit\n+                is RevaiJobState.Unknown -> snapshot.throwInvalidStatus()\n+            }\n+            if (settings.pollingIntervalMillis > 0 && attempt < attempts - 1) {\n+                delay(settings.pollingIntervalMillis)\n+            }\n+        }\n+        throw NoTranscriptGeneratedError(\"Rev.ai transcription job polling timed out\")\n+    }\n+\n     private suspend fun revaiPostMultipart(\n         url: String,\n         params: TranscriptionParams,\n@@ -510,7 +519,7 @@ private class RevaiTranscriptionModel(\n         }\n \n     private fun revaiConfigBody(params: TranscriptionParams): JsonObject {\n-        val options = revaiOptions(params.providerOptions)\n+        val options = JsonAccess.obj(params.providerOptions.toMap(), \"revai\") ?: JsonObject(emptyMap())\n         return buildJsonObject {\n             put(\"transcriber\", JsonPrimitive(modelId))\n             putRevaiOptions(options)\n@@ -588,9 +597,6 @@ private class RevaiTranscriptionModel(\n         return RevaiTranscriptMapping(text = text, segments = segments, durationInSeconds = durationInSeconds)\n     }\n \n-    private fun revaiOptions(providerOptions: ProviderOptions): JsonObject =\n-        JsonAccess.obj(providerOptions.toMap(), \"revai\") ?: JsonObject(emptyMap())\n-\n     private fun revaiErrorMessage(statusCode: Int, parsed: JsonElement?, raw: String): String {\n         val obj = parsed as? JsonObject\n         val detail = ((obj?.get(\"error\") as? JsonObject)?.get(\"message\") as? JsonPrimitive)?.contentOrNull\n@@ -600,6 +606,49 @@ private class RevaiTranscriptionModel(\n     }\n }\n \n+private data class RevaiJobSnapshot(\n+    val value: JsonElement,\n+    val state: RevaiJobState,\n+    val id: String?,\n+    val language: String?,\n+) {\n+    fun throwInvalidStatus(): Nothing =\n+        throw InvalidResponseDataError(\n+            value,\n+            \"Rev.ai transcription job response has missing, non-string, or unsupported status\",\n+        )\n+}\n+\n+private sealed interface RevaiJobState {\n+    data object Transcribed : RevaiJobState\n+    data object Failed : RevaiJobState\n+    data object InProgress : RevaiJobState\n+    data class Unknown(val raw: JsonElement?) : RevaiJobState\n+}\n+\n+private object RevaiJobSnapshotDecoder {\n+    fun decode(value: JsonElement): RevaiJobSnapshot {\n+        val job = value as? JsonObject ?: JsonObject(emptyMap())\n+        val rawStatus = job[\"status\"]\n+        val state = if (rawStatus is JsonPrimitive && rawStatus.isString) {\n+            when (rawStatus.content) {\n+                \"transcribed\" -> RevaiJobState.Transcribed\n+                \"failed\" -> RevaiJobState.Failed\n+                \"in_progress\" -> RevaiJobState.InProgress\n+                else -> RevaiJobState.Unknown(rawStatus)\n+            }\n+        } else {\n+            RevaiJobState.Unknown(rawStatus)\n+        }\n+        return RevaiJobSnapshot(\n+            value = value,\n+            state = state,\n+            id = (job[\"id\"] as? JsonPrimitive)?.contentOrNull,\n+            language = (job[\"language\"] as? JsonPrimitive)?.contentOrNull,\n+        )\n+    }\n+}\n+\n private const val REVAI_BASE_URL: String = \"https://api.rev.ai\"\n \n internal data class RevaiTranscriptMapping(\ndiff --git a/src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt b/src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt\nindex 034307e..dd79e05 100644\n--- a/src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt\n+++ b/src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt\n@@ -1,8 +1,10 @@\n package ai.torad.aisdk\n+\n import ai.torad.aisdk.providers.REVAI_VERSION\n import ai.torad.aisdk.providers.Revai\n import ai.torad.aisdk.providers.RevaiProviderSettings\n import io.ktor.http.HttpHeaders\n+import kotlinx.coroutines.ExperimentalCoroutinesApi\n import kotlinx.coroutines.test.runTest\n import kotlinx.serialization.json.Json\n import kotlinx.serialization.json.JsonObject\n@@ -18,6 +20,7 @@ import kotlin.test.assertEquals\n import kotlin.test.assertFailsWith\n import kotlin.test.assertTrue\n \n+@OptIn(ExperimentalCoroutinesApi::class)\n class RevaiProviderTest {\n     @Test\n     fun `transcription model submits multipart job polls status and maps transcript`() = runTest {\n@@ -139,6 +142,7 @@ class RevaiProviderTest {\n                 ?.get(\"value\")?.jsonPrimitive?.contentOrNull\n         )\n \n+        assertEquals(3, fixture.calls.size)\n         val submit = fixture.calls[0]\n         assertEquals(\"POST\", submit.requestMethod)\n         assertEquals(\"Bearer key\", submit.requestHeaders.headerValue(HttpHeaders.Authorization))\n@@ -158,31 +162,249 @@ class RevaiProviderTest {\n     }\n \n     @Test\n-    fun `transcription model throws when submission fails`() = runTest {\n+    fun `failed submission retains precedence over missing id`() = runTest {\n+        val fixture = revaiFixture(submitBody = \"\"\"{\"status\":\"failed\"}\"\"\")\n+        val model = revaiModel(fixture)\n+\n+        val error = assertFailsWith<NoTranscriptGeneratedError> {\n+            model.transcribe(revaiTranscriptionParams())\n+        }\n+\n+        assertTrue(error.message.orEmpty().contains(\"Failed to submit\"))\n+        assertEquals(1, fixture.calls.size)\n+        assertEquals(\"POST\", fixture.calls.single().requestMethod)\n+    }\n+\n+    @Test\n+    fun `failed submission with id stops after post`() = runTest {\n+        val fixture = revaiFixture(submitBody = \"\"\"{\"id\":\"job1\",\"status\":\"failed\"}\"\"\")\n+        val model = revaiModel(fixture)\n+\n+        val error = assertFailsWith<NoTranscriptGeneratedError> {\n+            model.transcribe(revaiTranscriptionParams())\n+        }\n+\n+        assertTrue(error.message.orEmpty().contains(\"Failed to submit\"))\n+        assertEquals(1, fixture.calls.size)\n+        assertEquals(\"POST\", fixture.calls.single().requestMethod)\n+    }\n+\n+    @Test\n+    fun `transcribed submission fetches transcript without polling status`() = runTest {\n+        val fixture = revaiFixture(\n+            submitBody = \"\"\"{\"id\":\"job1\",\"status\":\"transcribed\",\"language\":\"en\"}\"\"\",\n+            transcriptBody = \"\"\"{\"monologues\":[]}\"\"\",\n+        )\n+        val model = revaiModel(fixture)\n+\n+        val result = model.transcribe(revaiTranscriptionParams())\n+\n+        assertEquals(\"en\", result.language)\n+        assertEquals(2, fixture.calls.size)\n+        assertEquals(\"POST\", fixture.calls[0].requestMethod)\n+        assertEquals(\"GET\", fixture.calls[1].requestMethod)\n+        assertEquals(\n+            \"https://api.rev.ai/speechtotext/v1/jobs/job1/transcript\",\n+            fixture.calls[1].requestUrl,\n+        )\n+    }\n+\n+    @Test\n+    fun `failed poll stops after one status request`() = runTest {\n+        val fixture = revaiFixture(\n+            submitBody = \"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\",\n+            pollBody = \"\"\"{\"id\":\"job1\",\"status\":\"failed\"}\"\"\",\n+        )\n+        val model = revaiModel(fixture)\n+\n+        val error = assertFailsWith<NoTranscriptGeneratedError> {\n+            model.transcribe(revaiTranscriptionParams())\n+        }\n+\n+        assertTrue(error.message.orEmpty().contains(\"job failed\"))\n+        assertEquals(2, fixture.calls.size)\n+        assertEquals(listOf(\"POST\", \"GET\"), fixture.calls.map { it.requestMethod })\n+    }\n+\n+    @Test\n+    fun `pending poll exhausts exact attempt bound before timeout`() = runTest {\n+        val fixture = revaiFixture(\n+            submitBody = \"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\",\n+            pollBody = \"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\",\n+        )\n+        val model = revaiModel(fixture, maxPollAttempts = 2)\n+\n+        val error = assertFailsWith<NoTranscriptGeneratedError> {\n+            model.transcribe(revaiTranscriptionParams())\n+        }\n+\n+        assertTrue(error.message.orEmpty().contains(\"timed out\"))\n+        assertEquals(3, fixture.calls.size)\n+        assertEquals(listOf(\"POST\", \"GET\", \"GET\"), fixture.calls.map { it.requestMethod })\n+    }\n+\n+    @Test\n+    fun `missing id retains precedence over malformed initial status`() = runTest {\n+        val fixture = revaiFixture(submitBody = \"\"\"{\"status\":1}\"\"\")\n+        val model = revaiModel(fixture)\n+\n+        val error = assertFailsWith<InvalidResponseDataError> {\n+            model.transcribe(revaiTranscriptionParams())\n+        }\n+\n+        assertTrue(error.message.orEmpty().contains(\"missing id\"))\n+        assertEquals(1, fixture.calls.size)\n+        assertEquals(\"POST\", fixture.calls.single().requestMethod)\n+    }\n+\n+    @Test\n+    fun `missing initial status fails without polling`() = runTest {\n+        assertInvalidInitialStatus(\"\"\"{\"id\":\"job1\"}\"\"\")\n+    }\n+\n+    @Test\n+    fun `non-string initial status fails without polling`() = runTest {\n+        assertInvalidInitialStatus(\"\"\"{\"id\":\"job1\",\"status\":1}\"\"\")\n+    }\n+\n+    @Test\n+    fun `unknown initial status fails without polling`() = runTest {\n+        assertInvalidInitialStatus(\"\"\"{\"id\":\"job1\",\"status\":\"queued\"}\"\"\")\n+    }\n+\n+    @Test\n+    fun `malformed initial status retains entire response`() = runTest {\n+        val body = \"\"\"{\"id\":\"job1\",\"status\":{\"future\":true},\"provider_field\":\"must-survive\"}\"\"\"\n+        val response = Json.parseToJsonElement(body)\n         val fixture = TestServer.createTestServer(\n             mutableMapOf(\n                 \"https://api.rev.ai/speechtotext/v1/jobs\" to UrlHandler(\n-                    UrlResponse.JsonValue(Json.parseToJsonElement(\"\"\"{\"id\":\"job1\",\"status\":\"failed\"}\"\"\")),\n+                    UrlResponse.JsonValue(response),\n                 ),\n             ),\n+        ).also { it.server.start() }\n+        val model = revaiModel(fixture)\n+\n+        val error = assertFailsWith<InvalidResponseDataError> {\n+            model.transcribe(revaiTranscriptionParams())\n+        }\n+\n+        assertEquals(response, error.data)\n+        assertEquals(1, fixture.calls.size)\n+        assertEquals(\"POST\", fixture.calls.single().requestMethod)\n+    }\n+\n+    @Test\n+    fun `malformed polled statuses retain entire response`() = runTest {\n+        val pollBodies = listOf(\n+            \"\"\"{\"id\":\"job1\",\"status\":{\"future\":true},\"provider_field\":\"must-survive\"}\"\"\",\n+            \"\"\"{\"id\":\"job1\",\"status\":null,\"provider_field\":\"must-survive\"}\"\"\",\n+            \"\"\"{\"id\":\"job1\",\"status\":[],\"provider_field\":\"must-survive\"}\"\"\",\n+            \"\"\"{\"id\":\"job1\",\"status\":true,\"provider_field\":\"must-survive\"}\"\"\",\n         )\n-        fixture.server.start()\n-        val model = Revai(\n-            fixture.httpClient(),\n-            RevaiProviderSettings {\n-                apiKey(\"key\")\n-                pollingIntervalMillis(0)\n-            },\n-        ).transcription(ModelId(\"machine\"))\n+        for (body in pollBodies) {\n+            val response = Json.parseToJsonElement(body)\n+            val fixture = TestServer.createTestServer(\n+                mutableMapOf(\n+                    \"https://api.rev.ai/speechtotext/v1/jobs\" to UrlHandler(\n+                        UrlResponse.JsonValue(\n+                            Json.parseToJsonElement(\"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\"),\n+                        ),\n+                    ),\n+                    \"https://api.rev.ai/speechtotext/v1/jobs/job1\" to UrlHandler(\n+                        UrlResponse.JsonValue(response),\n+                    ),\n+                ),\n+            ).also { it.server.start() }\n+            val model = revaiModel(fixture)\n+\n+            val error = assertFailsWith<InvalidResponseDataError> {\n+                model.transcribe(revaiTranscriptionParams())\n+            }\n \n-        val error = assertFailsWith<AiSdkException> {\n-            model.transcribe(\n-                TranscriptionParams {\n-                    audio(AudioSource(\"audio/wav\", Base64Codec.encode(byteArrayOf(1))))\n-                }\n+            assertEquals(response, error.data)\n+            assertEquals(2, fixture.calls.size)\n+            assertEquals(listOf(\"POST\", \"GET\"), fixture.calls.map { it.requestMethod })\n+            assertEquals(\n+                \"https://api.rev.ai/speechtotext/v1/jobs/job1\",\n+                fixture.calls[1].requestUrl,\n             )\n         }\n-        assertTrue(error.message.orEmpty().contains(\"Failed to submit\"))\n+    }\n+\n+    @Test\n+    fun `pending then pending then transcribed delays once and fetches transcript`() = runTest {\n+        var statusCalls = 0\n+        val fixture = TestServer.createTestServer(\n+            mutableMapOf(\n+                \"https://api.rev.ai/speechtotext/v1/jobs\" to UrlHandler(\n+                    UrlResponse.JsonValue(\n+                        Json.parseToJsonElement(\"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\"),\n+                    ),\n+                ),\n+                \"https://api.rev.ai/speechtotext/v1/jobs/job1\" to UrlHandler { _ ->\n+                    when (statusCalls++) {\n+                        0 -> UrlResponse.JsonValue(\n+                            Json.parseToJsonElement(\"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\"),\n+                        )\n+                        1 -> UrlResponse.JsonValue(\n+                            Json.parseToJsonElement(\n+                                \"\"\"{\"id\":\"job1\",\"status\":\"transcribed\",\"language\":\"en\"}\"\"\",\n+                            ),\n+                        )\n+                        else -> throw AssertionError(\"unexpected additional Rev.ai status request\")\n+                    }\n+                },\n+                \"https://api.rev.ai/speechtotext/v1/jobs/job1/transcript\" to UrlHandler(\n+                    UrlResponse.JsonValue(Json.parseToJsonElement(\"\"\"{\"monologues\":[]}\"\"\")),\n+                ),\n+            ),\n+        ).also { it.server.start() }\n+        val model = revaiModel(\n+            fixture = fixture,\n+            pollingIntervalMillis = 1,\n+            maxPollAttempts = 3,\n+        )\n+\n+        val result = model.transcribe(revaiTranscriptionParams())\n+\n+        assertEquals(\"en\", result.language)\n+        assertEquals(1L, testScheduler.currentTime)\n+        assertEquals(4, fixture.calls.size)\n+        assertEquals(listOf(\"POST\", \"GET\", \"GET\", \"GET\"), fixture.calls.map { it.requestMethod })\n+        assertEquals(\n+            listOf(\n+                \"https://api.rev.ai/speechtotext/v1/jobs\",\n+                \"https://api.rev.ai/speechtotext/v1/jobs/job1\",\n+                \"https://api.rev.ai/speechtotext/v1/jobs/job1\",\n+                \"https://api.rev.ai/speechtotext/v1/jobs/job1/transcript\",\n+            ),\n+            fixture.calls.map { it.requestUrl },\n+        )\n+        assertEquals(1, fixture.calls.count { it.requestUrl.endsWith(\"/transcript\") })\n+    }\n+\n+    @Test\n+    fun `nonpositive attempt bound still polls once`() = runTest {\n+        for (maxPollAttempts in listOf(0, -1)) {\n+            val fixture = revaiFixture(\n+                submitBody = \"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\",\n+                pollBody = \"\"\"{\"id\":\"job1\",\"status\":\"in_progress\"}\"\"\",\n+            )\n+            val model = revaiModel(\n+                fixture = fixture,\n+                pollingIntervalMillis = 0,\n+                maxPollAttempts = maxPollAttempts,\n+            )\n+\n+            val error = assertFailsWith<NoTranscriptGeneratedError> {\n+                model.transcribe(revaiTranscriptionParams())\n+            }\n+\n+            assertTrue(error.message.orEmpty().contains(\"timed out\"))\n+            assertEquals(2, fixture.calls.size)\n+            assertEquals(listOf(\"POST\", \"GET\"), fixture.calls.map { it.requestMethod })\n+        }\n     }\n \n     @Test\n@@ -194,6 +416,60 @@ class RevaiProviderTest {\n         assertFailsWith<NoSuchModelError> { provider.embeddingModel(\"embed\") }\n     }\n \n+    private fun revaiFixture(\n+        submitBody: String,\n+        pollBody: String? = null,\n+        transcriptBody: String? = null,\n+    ): CreatedTestServer {\n+        val routes = mutableMapOf(\n+            \"https://api.rev.ai/speechtotext/v1/jobs\" to UrlHandler(\n+                UrlResponse.JsonValue(Json.parseToJsonElement(submitBody)),\n+            ),\n+        )\n+        pollBody?.let {\n+            routes[\"https://api.rev.ai/speechtotext/v1/jobs/job1\"] = UrlHandler(\n+                UrlResponse.JsonValue(Json.parseToJsonElement(it)),\n+            )\n+        }\n+        transcriptBody?.let {\n+            routes[\"https://api.rev.ai/speechtotext/v1/jobs/job1/transcript\"] = UrlHandler(\n+                UrlResponse.JsonValue(Json.parseToJsonElement(it)),\n+            )\n+        }\n+        return TestServer.createTestServer(routes).also { it.server.start() }\n+    }\n+\n+    private fun revaiModel(\n+        fixture: CreatedTestServer,\n+        pollingIntervalMillis: Long = 0,\n+        maxPollAttempts: Int = 60,\n+    ): TranscriptionModel =\n+        Revai(\n+            fixture.httpClient(),\n+            RevaiProviderSettings {\n+                apiKey(\"key\")\n+                pollingIntervalMillis(pollingIntervalMillis)\n+                maxPollAttempts(maxPollAttempts)\n+            },\n+        ).transcription(ModelId(\"machine\"))\n+\n+    private fun revaiTranscriptionParams(): TranscriptionParams =\n+        TranscriptionParams {\n+            audio(AudioSource(\"audio/wav\", Base64Codec.encode(byteArrayOf(1))))\n+        }\n+\n+    private suspend fun assertInvalidInitialStatus(submitBody: String) {\n+        val fixture = revaiFixture(submitBody = submitBody)\n+        val model = revaiModel(fixture)\n+\n+        assertFailsWith<InvalidResponseDataError> {\n+            model.transcribe(revaiTranscriptionParams())\n+        }\n+\n+        assertEquals(1, fixture.calls.size)\n+        assertEquals(\"POST\", fixture.calls.single().requestMethod)\n+    }\n+\n     private fun Map<String, String>.headerValue(name: String): String? =\n         entries.firstOrNull { it.key.equals(name, ignoreCase = true) }?.value\n }\n",
  "integration_point": "Save artifact verbatim as /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I1.patch. Apply with `git -C /home/marcos/Documents/dev/opensource/aisdk-kotlin apply --check /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I1.patch && git -C /home/marcos/Documents/dev/opensource/aisdk-kotlin apply /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/I1.patch`. Changed files: /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt and /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt. Verify from /home/marcos/Documents/dev/opensource/aisdk-kotlin with `./gradlew jvmTest --tests '*Revai*' --rerun-tasks`, `./gradlew jvmTest --tests '*MediaPollRobustnessTest.revai poll GET is cancelled when abort fires in flight*' --rerun-tasks`, `./gradlew checkKotlinAbi --rerun-tasks`, `./gradlew detekt --rerun-tasks`, and `./gradlew check`.",
  "bad_example": "A Rev.ai submission returns `{\"id\":\"job1\",\"status\":\"failed\"}`. The initial state must throw NoTranscriptGeneratedError containing `Failed to submit` after exactly one POST; a guard that handles Failed only when id is absent incorrectly proceeds to a status/transcript GET and is caught by `failed submission with id stops after post`. Likewise, any malformed status response must retain the complete response on InvalidResponseDataError.data.",
  "good_example": "Submission returns `{\"id\":\"job1\",\"status\":\"in_progress\"}`; the first status GET returns `in_progress`; the second returns `{\"id\":\"job1\",\"status\":\"transcribed\",\"language\":\"en\"}`; the transcript returns `{\"monologues\":[]}`. The result has language `en`, virtual time advances exactly 1 ms, calls are POST/status GET/status GET/transcript GET, and the transcript endpoint is fetched exactly once.",
  "verification": {
    "gate_result": "Adjudication 1 — \"The artifact makes the required wave landing gate fail because RevaiTranscriptionModel.transcribe exceeds detekt NestedBlockDepth.\" Ruling honored: extracted the provider-local pollRevaiJob without suppression/config/baseline edits and inlined the semantically identical single-use revaiOptions helper to remain at the class-function threshold. Command `/home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I1/gradlew -p /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I1 detekt --rerun-tasks` exited 0: `BUILD SUCCESSFUL in 5s`, 4 tasks executed, with no NestedBlockDepth or TooManyFunctions finding. Command `.../gradlew -p ... check` exited 0: `BUILD SUCCESSFUL in 1m 1s`, 39 actionable tasks (22 executed, 2 from cache, 15 up-to-date), including detekt, checkKotlinAbi, JVM/Android/Linux tests.\nAdjudication 2 — \"The tests displaced the ordinary valid-id failed-submission case ...\" Ruling honored: retained the no-id precedence test and added the separate exact valid-id payload. The required mutation `Failed && submitted.id == null` made `... jvmTest --tests '*RevaiProviderTest.failed submission with id stops after post*' --rerun-tasks` exit 1 with `1 test completed, 1 failed` and APICallError; after byte-for-byte restoration the same command exited 0.\nAdjudication 3 — \"The malformed-status tests never assert InvalidResponseDataError.data ...\" Ruling honored: both initial and four-body polled tests compare error.data to the complete parsed body. Replacing snapshot `value = value` with `value = JsonNull` made the two-test command exit 1 with `2 tests completed, 2 failed`, both at the data assertions; after restoration it exited 0.\nFinal commands all exited 0: focused Rev.ai suite, in-flight abort test, checkKotlinAbi, detekt, and full check. `git diff --check` produced no output. The staged baseline SHA-256 remained `63fad51fdf65d1f04d068d1db82a90bf04c814187e0da95a7f411ddc32b1e762`; unstaged artifact SHA-256 is `0fa223dcfbaa1c2606c9907afe2136821135fac0df93638be60031d69fdfe80a`.",
    "ran_it": "Executed `/.../trial-opus-3-repair-I1/gradlew -p /.../trial-opus-3-repair-I1 jvmTest --tests '*Revai*' --rerun-tasks`: `BUILD SUCCESSFUL in 39s`, 7/7 tasks executed. XML count printed exactly: `TEST-ai.torad.aisdk.RevaiDefensiveParsingTest.xml: tests=1, failures=0, errors=0, skipped=0`; `TEST-ai.torad.aisdk.RevaiProviderTest.xml: tests=15, failures=0, errors=0, skipped=0`; aggregate `files=2, tests=16, failures=0, errors=0, skipped=0`. The abort command succeeded in 34s. `checkKotlinAbi --rerun-tasks` succeeded in 28s with 9/9 tasks executed after compiling JVM, linuxX64, iosX64, iosArm64, and iosSimulatorArm64. `detekt --rerun-tasks` succeeded in 5s. Full `check` succeeded in 1m 1s and ran linuxX64Test.",
    "positive_case": "Required failure and malformed-response cases passed in the final 16-test focused suite. Valid-id Failed and no-id Failed each produced NoTranscriptGeneratedError containing `Failed to submit` after one POST. The exact malformed initial object produced InvalidResponseDataError whose data equaled the complete response after one POST. Each object/null/array/boolean polled status produced data equal to its complete response after POST + one status GET and no transcript GET. The 0/-1 attempt values each produced the polling-timeout error after POST + one status GET.",
    "negative_case": "Compliant routes were not caught: initial Transcribed completed with POST + transcript GET and language `en`; InProgress→Transcribed completed with POST + one status GET + transcript GET; the delayed InProgress→InProgress→Transcribed case completed with virtual time 1 ms and exactly four calls, fetching the transcript once. Existing mapping/header assertions and in-flight abort behavior remained green.",
    "compliant_probe": "Exercised genuinely compliant shapes through HTTP: initial `transcribed`; initial `in_progress` then `transcribed`; initial `in_progress`, one more `in_progress`, then `transcribed` with positive delay; polled `failed` as the intentional terminal-error path; all-pending with 2 attempts; normalized all-pending with 0 and -1 configured attempts. Observed request counts were respectively 2, 3, 4, 2, 3, and 2 per normalized-floor iteration; final language and transcript-fetch assertions matched the plan.",
    "cannot_fail_probe": "Three independent claimed behaviors were broken and each went red, then the production file was restored byte-for-byte (SHA-256 before/after every probe: `dc1cc3d34e1e0818bad4c1e8248c481a66ea015bc0ee678a5f5d04c542730b71`). (1) Initial-failure precedence: changed the Failed guard to throw only when `submitted.id == null`; the valid-id targeted test exited 1, `1 test completed, 1 failed`, because execution proceeded to an APICallError. Restored test exited 0. (2) Complete response carriage: changed only decoder snapshot `value = value` to `value = JsonNull`; both malformed-retention tests exited 1, `2 tests completed, 2 failed`, at error.data comparisons. Restored command exited 0. (3) Immediate terminal exit/final fetch: changed `RevaiJobState.Transcribed -> return snapshot` to `-> Unit`; the delayed two-poll targeted test exited 1 when a forbidden third status request hit the route-local AssertionError. Restored targeted test exited 0. These mutations attack separate guard, data-carriage, and transition-control joints rather than neighboring spellings of one condition.",
    "expected_delta": "Premise instrument: `ast-grep run --pattern '$OBJ[\"status\"]' --lang kotlin --json=compact RevaiProvider.kt | jq 'length'`. BEFORE the repair it printed `5`; AFTER it printed `1`. The sole remaining access is the canonical decoder read at /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I1/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt:632. This matches the predicted 5→1 collapse to one raw status source of truth.",
    "real_occurrences": "/home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I1/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt:421 calls the sole provider-local poller; :447 defines it; :632 is the sole direct status access in the decoder. /home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/sandboxes/trial-opus-3-repair-I1/src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt:180 contains the valid-id Failed fixture; :292 and :325 assert complete error.data equality."
  },
  "duplication_check": "Checked the exact existing owner /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt::RevaiTranscriptionModel.transcribe and its 45-day history. The July 2 rewrite owned the five direct status reads; no prior snapshot decoder or provider-local poll helper existed in the staged baseline. Structural searches found exactly one pollRevaiJob call/definition in commonMain and no class/data class/sealed interface named PollState. /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/MediaPollRobustnessTest.kt remains the owner of in-flight abort behavior; this patch adds state-transition coverage rather than duplicating that test. No generic shared poller was introduced.",
  "known_soft_spots": "The four malformed polled shapes are table-driven, so a mutation failing the first row prevents later rows from executing in that mutated run; the final unmutated run did execute the full table, while the decoder partition (`JsonPrimitive && isString`) and complete-response mutation were separately inspected/probed. Unknown(raw) is private and the public error deliberately does not print raw status, so the exact raw carrier is structurally verified rather than directly observable; full error.data retention is the behavioral proof. Top-level non-object Rev.ai job responses are handled by the same decoder fallback but are not newly enumerated because the pinned scope names malformed status fields within job responses. The one-use revaiOptions inline is byte-for-byte expression-equivalent and the mapping/header test passed, but that mechanical gate accommodation is still a review joint worth checking when applying atop a diverged provider file."
}
