# LAW [2026-07-25]: All campaign work stays on release/v0.3.0-beta01 and preserves every pre-existing dirty-tree change; agents never commit.
# LAW [2026-07-25]: The mechanical adjudicators are focused JVM tests during each item, ./gradlew check after each wave, and bash .claude/hooks/rules/ci-gate.sh at campaign close; no bypasses.
# LAW [2026-07-25]: Public construct-types remain ABI-evolvable: @Poko or regular classes with internal constructors plus builders; no new public data classes; explicit wire values and one canonical raw representation.
# LAW [2026-07-25]: Provider-open outbound vocabularies keep custom(nonblank) escapes; inbound parsing preserves Unknown(raw) where policy is forward-compatible; closed types are reserved for SDK-owned routing and lifecycle domains.
# LAW [2026-07-25]: Public API changes require ./gradlew updateKotlinAbi plus CHANGELOG.md and INTERFACE_CONTRACT.md updates, all reconciled against the combined dirty tree.

# LAW [2026-07-25]: WORKTREES ARE FORBIDDEN for this campaign. Workflow calls carry no isolation option; every mutating or testing role uses a unique ordinary local clone under dev/campaigns/stringly-domain-types/sandboxes and never writes the source checkout.
# LAW [2026-07-26]: Because v0.3.0-beta01 is already published, public APIs and deprecations introduced by this campaign target 0.3.0-beta02; use @since/deprecation wording for beta02, and V1 bumps VERSION_NAME accordingly while the branch name remains unchanged.
[campaign]
name = "campaign"
created = "2026-07-25"
next = ["R1", "R2", "R3", "R4", "S1", "S2", "S3", "C1", "C2", "C3", "C4", "C5", "C6", "U1", "V1"]

[[items]]
id = "I1"
phase = "1"
title = "Parse Rev.ai submission and polling snapshots once so transcribed exits immediately, failed and malformed statuses fail explicitly, and only pending statuses retry"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Revai*Test.kt"]
status = "done"
verify = "./gradlew jvmTest --tests '*Revai*'"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: private snapshot/state parser, only in_progress retries, malformed/unknown fails immediately, exact request-count tests.
# [2026-07-25] ATTEST-START: tree=66de8aea4a5b71853177717127cc0bdc40cc502e at=2026-07-25T22:25:45Z
# [2026-07-25] 2026-07-25 applied repaired Opus artifact serially while preserving concurrent RFC7807 Rev.ai error parsing. Exact status-read instrument moved 5 -> 1. Serial ordinary-clone ./gradlew --no-daemon --max-workers=2 jvmTest --tests '*Revai*' PASS. Mutation value=JsonNull made both full-response retention tests fail; mutation Failed && id==null made failed-submission-with-id test fail; restored full Rev.ai gate PASS. Also repaired concurrent LiteRTLanguageModel ResponseFormat compile failure with the missing explicit import after the combined-tree test exposed it. Full repository gate remains V1 work.
# [2026-07-25] ATTEST: scoped diffstat: .../ai/torad/aisdk/providers/RevaiProvider.kt      | 117 +++++++++++++++------ | 1 file changed, 85 insertions(+), 32 deletions(-); sidecar: .claude/ledger-diffs/I1.diff; off-scope: .github/dependabot.yml, .github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/consumer-canary.yml, .github/workflows/dependabot-automerge.yml, .github/workflows/dependabot.yml, .github/workflows/dependency-review.yml, .github/workflows/dependency-submission.yml, ... 103 more

[[items]]
id = "I2"
phase = "1"
title = "Parse Replicate video prediction state once, poll only pending states, and reject unknown terminal values without changing output-authoritative image handling"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/ReplicateProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Replicate*Test.kt"]
status = "done"
verify = "./gradlew jvmTest --tests '*Replicate*'"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: video-only prediction-state parser, poll starting/processing, succeed only on succeeded, reject missing/non-string/unknown without changing Replicate image behavior.
# [2026-07-25] ATTEST-START: tree=25915d85b67954abceb7dfbbab15b7f1df6763cc at=2026-07-25T22:25:45Z
# [2026-07-25] 2026-07-25 applied repaired Opus artifact serially to source checkout. Round-1 regressions (line length and architecture budget) were repaired. Round-2 raw-preservation TEST_GAP was fixed with a second future-status case asserting exact message and InvalidResponseDataError.data. Serial ordinary-clone gate ./gradlew --no-daemon --max-workers=2 jvmTest --tests '*Replicate*' PASS; hardcoding Unknown("queued") made the focused test fail, then restored run PASS. Full repository gate remains V1 work.
# [2026-07-25] ATTEST: scoped diffstat: .../ai/torad/aisdk/providers/ReplicateProvider.kt  | 83 +++++++++++++++------- | 1 file changed, 59 insertions(+), 24 deletions(-); sidecar: .claude/ledger-diffs/I2.diff; off-scope: .github/dependabot.yml, .github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/consumer-canary.yml, .github/workflows/dependabot-automerge.yml, .github/workflows/dependabot.yml, .github/workflows/dependency-review.yml, .github/workflows/dependency-submission.yml, ... 83 more

[[items]]
id = "I3"
phase = "1"
title = "Centralize Google Interactions status parsing so synchronous finish mapping and streaming terminality share one open Known-or-Unknown representation"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/GoogleInteractionsMapping.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/GoogleInteractionsStreamState.kt", "src/commonTest/kotlin/ai/torad/aisdk/*GoogleInteractions*Test.kt"]
status = "done"
verify = "./gradlew jvmTest --tests '*GoogleInteractions*'"
# [2026-07-25] Pre-campaign overlap: GoogleInteractionsMapping.kt and GoogleInteractionsStreamState.kt already contain uncommitted GenerateId-to-injected-generator corrections. Campaign patches must preserve and compose with those exact edits.
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Implement the smallest internal-only landing in the two target implementation files plus focused tests: add one private `GoogleInteractionStatus` representation with `Completed`, `RequiresAction`, `Failed`, `Incomplete`, `Cancelled`, `InProgress`, and `Unknown(raw: String?)`; add exactly one private `parseGoogleInteractionStatus(raw: String?)`; make the existing `googleInteractionsFinishReason(String?, Boolean)` and `googleInteractionsTerminal(String?)` compatibility helpers delegate to that parsed value so `/home/…
# [2026-07-25] ATTEST-START: tree=8b3d70d9c6f7e04f09a466d92d0bb0bdd442569d at=2026-07-26T01:33:59Z
# [2026-07-25] 2026-07-25 repaired Opus artifact merged serially against newer concurrent Google Interactions work. Preserved requires_action as terminal for the poll loop (newer documented behavior), budget_exceeded mapping, full wire echo, media, injected-ID, tool, and initial-text changes. Shared private parser count 0 -> 1; mutable normalized finish assignments -> 0; exact focused GoogleInteractions gate PASS. Mutating budget_exceeded to Unknown made the status-table test fail; restored full focused gate PASS. No public API/ABI change.
# [2026-07-25] ATTEST: scoped diffstat: .../aisdk/providers/GoogleInteractionsMapping.kt   | 207 ++++++++++++++++----- | .../providers/GoogleInteractionsStreamState.kt     | 149 ++++++++++----- | 2 files changed, 269 insertions(+), 87 deletions(-); sidecar: .claude/ledger-diffs/I3.diff; off-scope: CHANGELOG.md, api/jvm/torad-aisdk.api, api/torad-aisdk.klib.api, dev/campaigns/stringly-domain-types/I3-repair-artifact.json, dev/campaigns/stringly-domain-types/I3-repair.patch, dev/campaigns/stringly-domain-types/I3-review-results.json, dev/campaigns/stringly-domain-types/adversarial-loop.js, dev/campaigns/stringly-domain-types/current-working-tree-I3.patch, ... 63 more
# [2026-07-26] Transit verification during R1 exposed two Kotlin compiler warnings in GoogleInteractionsProviderTest assertProviderExecuted: assertTrue's contract had already established google non-null, making later safe calls unnecessary. Removed only those two safe calls; next serial JVM compilation is the verification. This preserves test semantics and keeps the combined tree warning-clean.

[[items]]
id = "I4"
phase = "1"
title = "Make Fireworks malformed and unknown async-image statuses explicit and add transition coverage for polling providers that should remain one-site string dispatches"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt", "src/commonTest/kotlin/ai/torad/aisdk/*ProviderTest.kt"]
status = "done"
verify = "./gradlew jvmTest --tests '*Fireworks*' --tests '*BlackForestLabs*'"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Keep the landing private and direct. In `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt`, parse `status` once as a genuine JSON string and make the one-site policy exhaustive: Pending alone retries; Ready returns a validated `result.sample`; Error and Failed retain immediate APICallError behavior; missing/null/non-string values fail immediately as malformed; every other string, including blank, fails immediately as an explicit unknown status with…
# [2026-07-25] ATTEST-START: tree=2169edd9c817ebe7e2a956554954968488370d25 at=2026-07-26T01:33:59Z
# [2026-07-26] Final orchestrator verification: in ordinary clone dev/campaigns/stringly-domain-types/sandboxes/serial-verify-I4, ./gradlew --no-daemon --max-workers=2 jvmTest --tests '*Fireworks*' --tests '*BlackForestLabs*' completed BUILD SUCCESSFUL in 40s after strengthening the malformed-status diagnostic assertion to exact equality. Fireworks now retries only Pending, validates Ready/result.sample, preserves immediate Error/Failed APICallError behavior, and rejects malformed/unknown statuses after one poll; BFL's intentional unknown-status retry policy is locked by focused tests.
# [2026-07-26] ATTEST: scoped diffstat: .../ai/torad/aisdk/providers/FireworksFacade.kt    | 24 +++++++++++++++++++--- | 1 file changed, 21 insertions(+), 3 deletions(-); sidecar: .claude/ledger-diffs/I4.diff; off-scope: CHANGELOG.md, api/jvm/torad-aisdk.api, api/torad-aisdk.klib.api, dev/campaigns/stringly-domain-types/I3-repair-artifact.json, dev/campaigns/stringly-domain-types/I3-repair.patch, dev/campaigns/stringly-domain-types/I3-review-results.json, dev/campaigns/stringly-domain-types/I4-repair-artifact.json, dev/campaigns/stringly-domain-types/I4-repair.patch, ... 85 more

[[items]]
id = "R1"
phase = "2"
title = "Propagate the requested xAI image output format into returned base64 media types before adding a typed format API"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/XaiProvider.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/XaiMediaModels.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Xai*Test.kt"]
status = "done"
verify = "./gradlew jvmTest --tests '*Xai*'"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Before adding or changing any typed format facade, add a focused regression test in `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonTest/kotlin/ai/torad/aisdk/XaiProviderTest.kt` that sends the existing raw xAI `output_format` values `png`, `jpeg`, and `webp`, asserts request bodies retain those exact wire strings, and asserts returned inline `b64_json` files are labeled `image/png`, `image/jpeg`, and `image/webp`. Then make the smallest internal change in `/home/marcos/Documents/dev/opensource/aisdk-…
# [2026-07-26] Premise re-audit against the current combined tree found the original request-option premise factually stale: current xAI /v1/images/generations and /v1/images/edits use response_format for URL/base64 transport, expose response data[].mime_type, and do not accept the old image output_format/sync_mode/quality fields; the combined tree already filters those fields. Corrected item: for inline b64_json, propagate the same response item's nonblank mime_type into GeneratedFile.mediaType, falling back to image/png only when absent/null/blank; preserve response_format=b64_json, the current off-schema field filtering, and URL-download typing. No public API or typed format facade belongs in R1. Instrument: response MIME mismatches 2->0 for image/jpeg and image/webp while request-wire assertions remain unchanged; focused gate ./gradlew jvmTest --tests '*Xai*'. Evidence: current XaiMediaModels.kt/XaiProviderTest.kt, official xAI Images REST and generation docs, and official xai-sdk-python image request construction.
# [2026-07-26] ATTEST-START: tree=331588b47f1dc331daaf87da787955bce12877b0 at=2026-07-26T07:20:29Z
# [2026-07-26] LAND_REPAIRED after one fix-forward round. Initial implementation corrected the stale request-format premise by consuming response data[].mime_type for inline b64_json with image/png fallback. Three prosecutors accepted; the advocate found one cannot-fail gap for verbatim whitespace preservation. Planner validated it; fresh repair added a sixth spaced MIME fixture and killed a temporary trim mutant. All four raised-bar re-reviewers accepted. Orchestrator applied only R1-final.patch (4/1 production lines, 76 test lines) over the unchanged combined baseline. Final serial source-tree gate ./gradlew --no-daemon --max-workers=2 jvmTest --tests '*Xai*' completed BUILD SUCCESSFUL in 25s. Durable artifact: R1-final.patch; reviews: R1-review-results.json. No public API/ABI/docs change.
# [2026-07-26] ATTEST: scoped diffstat: src/commonMain/kotlin/ai/torad/aisdk/providers/XaiMediaModels.kt | 5 ++++- | 1 file changed, 4 insertions(+), 1 deletion(-); sidecar: .claude/ledger-diffs/R1.diff; off-scope: dev/campaigns/stringly-domain-types/R1-final.patch, dev/campaigns/stringly-domain-types/R1-review-results.json, dev/campaigns/stringly-domain-types/adversarial-loop.js, dev/campaigns/stringly-domain-types/sandboxes/serial-verify-R1, dev/campaigns/stringly-domain-types/sandboxes/wave-R1-20260726-implement-R1, dev/campaigns/stringly-domain-types/sandboxes/wave-R1-20260726-plan-R1, dev/campaigns/stringly-domain-types/sandboxes/wave-R1-20260726-repair-R1, dev/campaigns/stringly-domain-types/sandboxes/wave-R1-20260726-review-r1-advocate-R1, ... 8 more

[[items]]
id = "R2"
phase = "2"
title = "Define and implement an honest Voyage compressed embedding result representation preserving dtype and dimensions before exposing binary output as supported"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/VoyageProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Voyage*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "in_flight"
verify = "./gradlew --no-daemon --max-workers=2 jvmTest --tests '*Voyage*' && ./gradlew --no-daemon --max-workers=2 checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Smallest useful landing: keep `EmbeddingModelResult.embeddings` and its ABI unchanged, and make Voyage populate its existing `providerMetadata` seam with an explicit representation descriptor derived from the request actually sent and the observed response: raw/effective `outputDtype`; packing state (`none`, `bit-packed`, or `unknown` for a future custom dtype); logical/original dimension; and stored elements per embedding. Do not duplicate the packed arrays into metadata because `/home/marcos/Documents/dev/opensou…
# [2026-07-26] Current-tree re-audit: the providerMetadata seam remains correct, but descriptor-only R2 became incomplete when the combined tree began forwarding encoding_format: documented base64 responses are strings and currently collapse to empty embeddings. Pragmatic corrected landing keeps public EmbeddingModelResult/ABI unchanged and emits voyage.embeddingRepresentation = {rawOutputDtype: String|null, effectiveOutputDtype: String, packing: none|bit-packed|unknown, logicalDimension: Int|null, storedElementCounts: Int[]}. Decode documented base64 privately into List<Float> for float32, signed int8 (int8/binary), and unsigned uint8 (uint8/ubinary); preserve raw base64 in response.body; custom dtype plus base64 fails explicitly, while custom JSON arrays retain raw dtype/unknown packing. Preserve compatibility: do not newly reject row-count, empty, unequal, or requested-dimension mismatches; record every row length and infer dimension only from uniform nonempty known rows when none was requested. Minimum real HTTP coverage: five known JSON-array dtypes, absent/default float, three base64 storage classes with captured encoding_format, custom JSON array, custom+base64 failure, irregular/mismatch and empty data metadata. Preserve live 1000 batch limit and encoding_format forwarding. Strict shape/dimension enforcement and metadata aggregation are follow-ups. Update CHANGELOG/INTERFACE_CONTRACT; ABI dumps remain descriptor-identical and checkKotlinAbi must pass.
# [2026-07-26] ATTEST-START: tree=4f418b5031d333243510c421ccf86ca08f1c568c at=2026-07-26T18:34:16Z
# [2026-07-26] Frozen dispatch baseline tuple: base commit 0421def3c4e4f36193eac8e6e2e2f9cba8ffd5d6; canonical staged patch wave-R2-baseline.patch sha256 2e3e24ddbba9451f827c93fcec60e03f786e895b9f4d3fe717a6a0a91301d7f6; complete metadata/hashes in wave-R2-baseline.json. The canonical patch includes the two otherwise-untracked I4 polling test files as staged new-file context. Fresh ordinary-clone apply+git add -A reproduced the exact patch hash, closing the prior invisible-untracked baseline gap.
# [2026-07-26] VERIFY-EDITED: verify = "./gradlew jvmTest --tests '*Voyage*' && ./gradlew checkKotlinAbi" -> verify = "./gradlew --no-daemon --max-workers=2 jvmTest --tests '*Voyage*' && ./gradlew --no-daemon --max-workers=2 checkKotlinAbi" (by=orchestrator implied; no flag)

[[items]]
id = "R3"
phase = "2"
title = "Repair or deprecate Groq transcription responseFormat based on actual multipart transport and decoder capabilities rather than builder-only behavior"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/GroqFacade.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/OpenAICompatibleModels.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Groq*Test.kt", "src/commonTest/kotlin/ai/torad/aisdk/ProviderModelOptionsBuilderTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Groq*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Choose deprecation, not implementation, for this wave. Add WARNING deprecations with an explicit “ignored; Groq transcription currently always requests JSON” message to `GroqTranscriptionModelOptions.responseFormat` and `GroqTranscriptionModelOptionsBuilder.responseFormat` in /home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/GroqFacade.kt; preserve both ABI symbols as compatibility bridges. Prevent the deprecated serialized camel-case key from leaking as an extra mult…
# [2026-07-26] FENCE-EDITED: files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/GroqFacade.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/**Transcription*.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Groq*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"] -> files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/GroqFacade.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/OpenAICompatibleModels.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Groq*Test.kt", "src/commonTest/kotlin/ai/torad/aisdk/ProviderModelOptionsBuilderTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"] (by=orchestrator implied; no flag)
# [2026-07-26] Current-tree re-audit: core premise remains sound, but the transport fence was incomplete and is now amended. Groq currently documents json, verbose_json, and text; the shared decoder always requests/decodes JSON. Preserve forced response_format=json, filter both response_format and serialized responseFormat from multipart passthrough, and deprecate the public property+builder at WARNING while preserving ABI/serialization. Because v0.3.0-beta01 already contains these symbols and HEAD is 53 commits after that tag, the deprecation message must identify the next shipping release (0.3.0-beta02 if that remains the next release), not falsely claim beta01; no ReplaceWith. Required real Groq-named HTTP test asserts one canonical JSON field, no camel-case leak, and successful JSON decode; builder compatibility remains tested with narrow suppression. ABI symbols stay present; docs and regenerated/reviewed dumps are required.
# [2026-07-26] Deprecation wording pin: until VERSION_NAME is deliberately advanced, use truthful versioned wording such as 'Deprecated after 0.3.0-beta01: ignored; Groq transcription always requests JSON. Omit this option.' This satisfies the version rule without guessing beta02 or falsely dating the annotation to the already-tagged beta01. V1 may replace it with the actual next release number if the version is settled there.

[[items]]
id = "R4"
phase = "2"
title = "Repair or deprecate LMNT format and define precedence with generic speech responseFormat so no dead public option gains a typed facade"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/LMNTProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*LMNT*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*LMNT*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Choose implementation, not deprecation. Smallest useful landing: make the already-public LMNT `format` option a compatibility fallback for the generic speech `responseFormat`, with one explicit rule: a non-null generic value wins; otherwise nested `lmnt.format` wins; otherwise use `mp3`. Validate the selected value through the existing five-value `lmntResponseFormat` path, so an explicitly supplied unsupported winner still emits the existing warning and falls back to `mp3` rather than silently consulting the lower-…
# [2026-07-26] Current-tree re-audit: the dead nested format diagnosis remains sound, but the five-value/response_format premise is stale. Preserve concurrent current-API corrections (wire key format, blizzard/leah defaults, binary parsing, removal of unsupported body fields). Resolve generic responseFormat > nested lmnt.format > mp3; validate only the selected winner and never fall through after an unsupported higher-priority value. Canonical current values: aac/mp3/ulaw/wav/webm/pcm_s16le/pcm_f32le; normalize legacy mulaw->ulaw and raw->pcm_f32le with warnings. Fix lmnt-version default from invalid 2024-10-01 to 1.2 and enforce case-insensitive per-call > provider > default single-header precedence. Preserve server Content-Type first; correct local fallbacks, notably ulaw->audio/wav. Do not restore response_format or change public descriptors. Related live 1.2 honesty: unsupported outbound seed must not ship; nested ignored speed must warn. Accept header and dead model deprecation remain follow-up unless implementation evidence makes them necessary. Required HTTP tables cover source precedence, canonical/legacy/unknown formats, MIME fallback/header precedence, header casing/precedence, and preserved current defaults/unsupported-field warnings; docs update, ABI descriptors unchanged.

[[items]]
id = "S1"
phase = "3"
title = "Add closed typed Quiver generate/vectorize routing with exact wire values, one canonical raw representation, a deprecated compatibility bridge, and unknown rejection"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/QuiverAIProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Quiver*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Quiver*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Smallest useful landing: introduce public `QuiverAIOperation` as a two-entry enum (or equivalently small closed type, but the enum is the least code) with `Generate("generate")` and `Vectorize("vectorize")` and a public exact `wireValue`. Keep `QuiverAIImageModelOptions.operation: String?` and the builder’s single private `String?` storage unchanged. Add non-null `operation(QuiverAIOperation)` that stores `value.wireValue`; retain `operation(String?)`, annotate it deprecated with a versioned message, and keep it as…
# [2026-07-26] Current-tree re-audit: premise remains sound as a closed SDK endpoint selector, but refine the public shape. Add QuiverAIOperation Generate/Vectorize with explicit internal selectorValue (not public wireValue: operation is never sent to Quiver), plus a non-null typed builder setter canonicalizing into the retained raw String property/slot. Retain and version-deprecate raw operation(String?); missing/null defaults Generate; blank/unknown raw rejects before transport; classify once and dispatch exhaustively. Exact endpoints are /v1/svgs/generations and /v1/svgs/vectorizations; operation is omitted. Real request tests must prove typed/raw parity, route-specific fields, unknown zero-call rejection, and that vectorization does not send undocumented n. Preserve concurrent attributes/viewBox ABI edits. Do not use current object-shaped credits or response-attributes tests as the routing oracle: official credits is numeric and response attributes lack authoritative support; those are adjacent corrections, not grounds to force S1 wider. Same-name typed/raw overload has Java bare-null source ambiguity; V1 must review whether a distinct typed setter name is preferable before final API freeze.

[[items]]
id = "S2"
phase = "3"
title = "Add closed typed Luma reference routing for image/style/character/modify_image while deciding no-file selector and reserved-key collision behavior explicitly"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/LumaProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Luma*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Luma*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Add a public closed `LumaReferenceType` with exactly four cases and an explicit case-to-wire mapping for `image`, `style`, `character`, and `modify_image`. Add `referenceType(LumaReferenceType)` to `LumaImageModelOptionsBuilder`; keep `referenceType(String?)` and `LumaImageModelOptions.referenceType: String?` as the single canonical raw representation, deprecating only the raw setter with a versioned migration message. Do not add a nullable typed overload. Validate the canonical raw selector before the no-files ear…
# [2026-07-26] Current-tree re-audit: typed selector remains warranted but old wire/collision premise is stale. image/style/character/modify_image are SDK-local selector tokens; current provider fields are image_ref/style_ref/character_ref/modify_image_ref and referenceType is never sent. Add LumaReferenceType with explicit internal selector mapping and a typed setter canonicalizing into retained raw String state; version-deprecate raw setter, avoid nullable same-name overload ambiguity. Validate selector before no-files early return; missing/null defaults image; known selector with zero files remains valid; blank/unknown rejects before transport. Write passthrough first then SDK-owned prompt/model/aspect_ratio/generated *_ref fields so core values win while unrelated inactive keys survive. Preserve current *_ref corrections. Tests cover four typed/raw routes, zero-file validation, updated reserved-key collisions, image/character/modify boundaries, and leave style uncapped absent evidence. Public enum+setter require ABI/docs updates.

[[items]]
id = "S3"
phase = "3"
title = "Add closed typed xAI video operations with exact endpoint routing, operation-specific validation, deprecated raw compatibility, and unknown legacy rejection"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/XaiProvider.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/XaiMediaModels.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Xai*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Xai*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Add the smallest closed construction and runtime classifier: a public four-value `XaiVideoOperation` enum (Generate, Edit, Extend, Reference), a distinctly named `operation(XaiVideoOperation?)` builder method, and an internal exact mapping to `generate-video`, `edit-video`, `extend-video`, and the established `reference-to-video`. Keep the existing `mode(String?)` method as a deprecated source/binary-compatible bridge; do not remove the existing serialized/raw `mode` property. Classify once before warnings, body co…
# [2026-07-26] Current-tree re-audit found the old four-token premise factually wrong: explicit xAI mode has only edit-video, extend-video, reference-to-video; ordinary generation is absent mode, never generate-video. Correct item: public three-entry XaiVideoMode with explicit internal selector values and distinctly named operation(XaiVideoMode?) writing the retained mode String slot; null clears explicit mode. Retain/version-deprecate mode(String?). Classify once: recognized explicit mode wins; absent plus nonblank videoUrl infers Edit; else references infer Reference; else Generate; unknown/blank explicit rejects before HTTP. Validate Edit/Extend nonblank videoUrl and Reference nonempty/all-nonblank. Exact routes: generate+reference /videos/generations, edit /videos/edits, extend /videos/extensions; mode never sent. Fix current request leak so generic image is omitted for edit/extend; edit omits duration/aspect/resolution, extend keeps duration but omits aspect/resolution. Preserve concurrent 1080p and expired fixes. Tests cover all routes, inference/precedence, raw parity, request omission, and zero-call invalid inputs. ABI/docs must distinguish three explicit modes from four effective routes.

[[items]]
id = "C1"
phase = "4"
title = "Add a Deepgram provider-local algebraic output model that owns only valid encoding-container-rate tuples while preserving generic raw responseFormat compatibility"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/DeepgramSpeechModel.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Deepgram*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Deepgram*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Add one provider-local sealed `DeepgramSpeechOutput` in `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/DeepgramSpeechModel.kt` with six consumer-facing families: Mp3(optional allowed bitrate), Aac(optional in-range bitrate), Flac(optional allowed sample rate), Opus(optional in-range bitrate), Wav(Linear16/MuLaw/ALaw plus compatible optional sample rate), and Raw(Linear16/MuLaw/ALaw plus compatible optional sample rate). Parameterized public variants are `@Poko` cl…
# [2026-07-26] Current-tree re-audit: premise remains SOUND with corrected authoritative constraints. Keep a six-family Deepgram algebra where each variant emits encoding/container and only legal dependent fields; Raw means documented uncontainerized PCM-family output (container=none), not an arbitrary escape. Preserve arbitrary/future values through existing raw setters/ProviderOptions.Raw and preserve generic responseFormat > Deepgram provider options precedence. Exact discrete rates: Linear16 8000/16000/24000/32000/48000; Flac 8000/16000/22050/32000/48000 (not 24000); MuLaw/ALaw 8000/16000. MP3 bitrate 32000|48000; AAC 4000..192000; Opus 4000..650000. Each typed variant emits encoding explicitly because official pages conflict on omitted-parameter defaults; do not encode a public default variant. Preserve current speed forwarding and invent no speed range. Request tests include near-miss invalid FLAC 24000 and Linear16 22050 plus precedence/raw-bridge parity. Public construct type follows internal-constructor builder/DSL ABI doctrine; ABI/docs update required.

[[items]]
id = "C2"
phase = "4"
title = "Add Voyage input and output provider-open value types with named constants and custom escapes only after compressed output representation is honest"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/VoyageProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Voyage*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Voyage*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit OVER_PINNED; full evidence in premise-results.json. Corrected scope: Split the landing at the actual dependency. C2a can land now: add one provider-open `VoyageEmbeddingInputType` value class with `Query`, `Document`, and `custom(nonblank)`; add a non-null typed `inputType(VoyageEmbeddingInputType)` builder overload that stores the exact `wireValue` in the existing canonical `String?`; retain `inputType(String?)` unchanged as the raw/null bridge. C2b remains gated on verified R2 artifacts, not merely a status edit: only after Voyage results explicitly preserve dtype and original dim…
# [2026-07-26] Current-tree re-audit: design remains sound but C2 is wholly BLOCKED on corrected R2; do not land even input typing before verified base64 normalization/representation metadata. After R2, add provider-open boxed value classes VoyageEmbeddingInputType(Query, Document, custom(nonblank)) and VoyageEmbeddingOutputDtype(Float, Int8, UInt8, Binary, UBinary, custom(nonblank)), each with explicit wireValue and @JvmExposeBoxed following repo convention. Add only non-null typed builder overloads writing the retained canonical String slots; preserve raw String? properties/setters and ProviderOptions.Raw, with ordinary last-call-wins. Typed custom preserves exact nonblank strings; no model/dimension/encoding capability matrix. Real request tests prove every named/custom value, raw parity, both precedence directions, null omission/default behavior, and pair output requests with R2-honest metadata. Public additions require both ABI dumps and docs; no public data class.

[[items]]
id = "C3"
phase = "4"
title = "Add Cohere provider-open embedding input values and a constrained thinkingEnabled budget operation without independently mutable type and budget sources"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/CohereProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*Cohere*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Cohere*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit OVER_PINNED; full evidence in premise-results.json. Corrected scope: Add the smallest additive preferred API, without claiming removal of every raw escape: (1) a public single-raw-value `CohereEmbeddingInputType` with four text-safe named constants (`search_document`, `search_query`, `classification`, `clustering`) plus nonblank `custom(value)`; (2) a non-null typed `inputType(CohereEmbeddingInputType)` overload that writes `value` into the existing `String?` backing property, while retaining `inputType(String?)` as the raw compatibility bridge and making last builder call the expli…
# [2026-07-26] Current-tree re-audit: original globally-unrepresentable-state claim is OVER_PINNED, but additive typed scope is sound and independent of R2. Add boxed provider-open CohereEmbeddingInputType with text-safe named values SearchDocument/SearchQuery/Classification/Clustering plus custom(nonblank); do not name Image because this SDK sends text-only inputs. Typed overload writes existing String slot; raw property/setter remains unrestricted and last-call-wins. Add atomic operations on existing CohereThinkingOptionsBuilder, not a new type: thinkingEnabled(budgetTokens>=1) sets enabled+budget; thinkingDisabled() sets disabled and clears budget. Preserve raw type/tokenBudget setters and document later-call precedence; raw escapes can still construct broader states. Do not claim Cohere forbids disabled+budget or invent model matrices. Real HTTP tests cover four named/custom input values, blank zero-call failure, current search_query default, raw parity/precedence, enabled/disabled mapping, positive-budget validation, atomic normalization, and later raw override while tolerating concurrent extra warnings. Additive ABI/docs only; preserve current Cohere wire/stream edits.

[[items]]
id = "C4"
phase = "4"
title = "Add AssemblyAI summary model and summary type typed APIs with explicit wire values and coherence validation while preserving raw bridges"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/AssemblyAIProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*AssemblyAI*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*AssemblyAI*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit OVER_PINNED; full evidence in premise-results.json. Corrected scope: Add the smallest coherent typed path rather than independent setters that can construct invalid pairs: two public explicit-wire enums, `AssemblyAISummaryModel` with all 3 currently documented known values and `AssemblyAISummaryType` with all 5, plus one additive builder operation such as `summary(model, type)`. That operation validates the 10 documented pairs atomically, enables `summarization`, establishes the required `punctuate`/`formatText` state, and rejects an explicitly enabled `autoChapters`; builder-final …
# [2026-07-26] Current-tree re-audit supersedes the proposed closed typed summary enums: AssemblyAI transcript summarization is now upstream-deprecated, Universal-2-only, and the provider-owned values are open; adding a brand-new preferred typed API would enlarge ABI around a dying feature. Corrected useful landing: add no new typed summary type. Preserve existing raw summarization/summaryModel/summaryType fields and wire mapping as compatibility API, add beta02 WARNING deprecations explaining the upstream deprecation/Universal-2 restriction (no ReplaceWith), and add a real serialized-options request test using ModelId(universal-2) that proves speech_models=[universal-2], summarization=true, exact summary_model/summary_type, punctuate=true, format_text=true, and no auto_chapters. Do not restore speech_model or validate arbitrary raw values/pairs. Update docs/ABI annotations while preserving descriptors. Known current values/pairs may be documented but must not become closed enums.

[[items]]
id = "C5"
phase = "4"
title = "Add typed Open Responses allowed-tools mode for the locally supported auto and required values with exact primitive wire parity"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/OpenResponsesProvider.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/OpenResponsesRequestMapping.kt", "src/commonTest/kotlin/ai/torad/aisdk/OpenResponsesProviderTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*OpenResponsesProviderTest' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Smallest useful landing: add a public explicit-wire `OpenResponsesAllowedToolsMode` open value type with named `Auto` (`"auto"`) and `Required` (`"required"`) values and `custom(nonblank)`; add one non-null typed builder entry that writes its `wireValue` into the existing single `String?` backing field; retain the existing raw `mode(String?)` bridge and public `mode: String?` property unchanged. Do not introduce a second independently mutable typed field and do not replace serializer-visible storage. The mapper sho…
# [2026-07-26] Current-tree re-audit confirms C5 SOUND with no mapper edit needed. Add boxed provider-open OpenResponsesAllowedToolsMode with explicit wireValue, named Auto/Required, and custom(nonblank) preserving exact input; add one non-null typed mode overload writing the retained String? slot. Keep raw getter/setter undeprecated for null clearing/compatible endpoints; ordinary last-call-wins. Existing mapper remains canonical: nonempty names emit primitive nested mode, null defaults auto, empty names suppress object, allowed tools override outer ToolChoice. Direct OpenResponses HTTP tests cover named/custom, blank zero-call failure, raw parity, null default, empty suppression, outer-choice precedence, and both setter orders; assert primitive string, not wrapper serialization. Preserve concurrent stream/tool-choice changes. Additive beta02 ABI/docs only.

[[items]]
id = "C6"
phase = "4"
title = "Add constrained Fireworks thinking configuration, Rev.ai diarization known values with custom escape, and Black Forest Labs output-format known values only where request tests prove behavior"
files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/BlackForestLabsProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/FireworksProviderTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsProviderTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsPollingStatusTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests '*Fireworks*' --tests '*Revai*' --tests '*BlackForestLabs*' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit SOUND; full evidence in premise-results.json. Corrected scope: Land only three additive adapters and their selected request proofs. 1. In `/home/marcos/Documents/dev/opensource/aisdk-kotlin/src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt`, add `thinkingEnabled(budgetTokens: Int)` to `FireworksLanguageModelOptionsBuilder`. It must reject budgets below 1024 and atomically set the existing primitive object to `type="enabled"` plus that budget. Preserve the existing raw `thinking(...)`, `type(...)`, and `budgetTokens(...)` paths without new validation so legacy/c…
# [2026-07-26] FENCE-EDITED: files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/BlackForestLabsProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/*ProviderTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"] -> files = ["src/commonMain/kotlin/ai/torad/aisdk/providers/FireworksFacade.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/RevaiProvider.kt", "src/commonMain/kotlin/ai/torad/aisdk/providers/BlackForestLabsProvider.kt", "src/commonTest/kotlin/ai/torad/aisdk/FireworksProviderTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/RevaiProviderTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsProviderTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/FireworksPollingStatusTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/BlackForestLabsPollingStatusTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"] (by=orchestrator implied; no flag)
# [2026-07-26] Current-tree provider-by-provider correction: retain exactly three independent adapters. Fireworks: add only thinkingEnabled(budgetTokens>=1024), atomically setting existing enabled+budget fields; keep raw thinking/type/budget permissive and last-call-wins, including legacy 256. Rev.ai: provider-open boxed RevaiDiarizationType Standard/Premium/custom(nonblank) plus camel-case typed setter writing retained diarization_type String slot; raw null/values remain. BFL: current known output formats are Jpeg/Png/Webp (old two-value inventory stale), plus custom(nonblank), with typed overload writing retained outputFormat String slot; no model matrix/default claim. Real selected request tests prove exact primitive mappings, raw parity, both setter orders, blank/boundary zero-call failures, and null omission. Preserve I1/I4 production behavior and include both untracked polling tests in the staged baseline/final inventory. LMNT remains solely R4; Hume is outside the full-sweep C6 scope and no Hume output-format source is added. Public additions target beta02 and require ABI/docs.

[[items]]
id = "U1"
phase = "5"
title = "Replace public UI tool lifecycle enum-plus-nullable bags with focused typed state variants while preserving the flat serialized wire shape and all approval, renderer, persistence, and transition behavior"
files = ["src/commonMain/kotlin/ai/torad/aisdk/ui/ToolCallState.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/UIMessagePart.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/InferAgentMessage.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/MessageStreamReader.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/ConvertToModelMessages.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/Chat.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/Streams.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/UIToolInvocationState.kt", "src/commonTest/kotlin/ai/torad/aisdk/**/*UI*Test.kt", "src/commonTest/kotlin/ai/torad/aisdk/**/*Tool*Test.kt", "src/commonTest/kotlin/ai/torad/aisdk/MessagePartsTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/MessageStreamReaderCollisionTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/ApprovalIdTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/ConvertToModelMessagesTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/ChatApprovalResponseTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/StructuredUiPokoTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"]
status = "todo"
verify = "./gradlew jvmTest --tests 'ai.torad.aisdk.UIMessagePartWireFormatTest' --tests 'ai.torad.aisdk.UIMessageShapeTest' --tests 'ai.torad.aisdk.MessagePartsTest' --tests 'ai.torad.aisdk.MessageStreamReaderCollisionTest' --tests 'ai.torad.aisdk.ApprovalIdTest' --tests 'ai.torad.aisdk.ConvertToModelMessagesTest' --tests 'ai.torad.aisdk.ChatApprovalResponseTest' --tests 'ai.torad.aisdk.ToolApprovalDenialTest' --tests 'ai.torad.aisdk.ToolOccurrenceContractTest' --tests 'ai.torad.aisdk.StructuredUiPokoTest' --tests 'ai.torad.aisdk.UIToolInvocationStateTest' --tests 'ai.torad.aisdk.ToolPartHandlerRegistryTest' --tests 'ai.torad.aisdk.FullPortFeatureParityTest' --tests 'ai.torad.aisdk.StreamingDocSnippetTest' --tests 'ai.torad.aisdk.ToUIMessageStreamTest' && ./gradlew checkKotlinAbi"
# [2026-07-25] Premise audit OVER_PINNED; full evidence in premise-results.json. Corrected scope: Widen U1 from three declarations to the actual lifecycle seam. Introduce one focused public sealed lifecycle model with `@Poko` value variants and map both serialized UI part kinds plus generic `UIToolInvocation<TInput,TOutput>` onto it. Minimum defensible variants: `InputStreaming(partialInput?)`; `InputAvailable(input)`; `ApprovalRequested(input, raw approvalId?, signature?)`; persisted `ApprovalResponded` with the same replay-required data; `OutputAvailable(output, input?, preliminary)`; `OutputError(message, in…
# [2026-07-26] FENCE-EDITED: files = ["src/commonMain/kotlin/ai/torad/aisdk/ui/**", "src/commonTest/kotlin/ai/torad/aisdk/**/*UI*Test.kt", "src/commonTest/kotlin/ai/torad/aisdk/**/*Tool*Test.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"] -> files = ["src/commonMain/kotlin/ai/torad/aisdk/ui/ToolCallState.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/UIMessagePart.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/InferAgentMessage.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/MessageStreamReader.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/ConvertToModelMessages.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/Chat.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/Streams.kt", "src/commonMain/kotlin/ai/torad/aisdk/ui/UIToolInvocationState.kt", "src/commonTest/kotlin/ai/torad/aisdk/**/*UI*Test.kt", "src/commonTest/kotlin/ai/torad/aisdk/**/*Tool*Test.kt", "src/commonTest/kotlin/ai/torad/aisdk/MessagePartsTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/MessageStreamReaderCollisionTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/ApprovalIdTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/ConvertToModelMessagesTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/ChatApprovalResponseTest.kt", "src/commonTest/kotlin/ai/torad/aisdk/StructuredUiPokoTest.kt", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "api/**"] (by=orchestrator implied; no flag)
# [2026-07-26] VERIFY-EDITED: verify = "./gradlew jvmTest --tests '*UI*' --tests '*Tool*' && ./gradlew checkKotlinAbi" -> verify = "./gradlew jvmTest --tests 'ai.torad.aisdk.UIMessagePartWireFormatTest' --tests 'ai.torad.aisdk.UIMessageShapeTest' --tests 'ai.torad.aisdk.MessagePartsTest' --tests 'ai.torad.aisdk.MessageStreamReaderCollisionTest' --tests 'ai.torad.aisdk.ApprovalIdTest' --tests 'ai.torad.aisdk.ConvertToModelMessagesTest' --tests 'ai.torad.aisdk.ChatApprovalResponseTest' --tests 'ai.torad.aisdk.ToolApprovalDenialTest' --tests 'ai.torad.aisdk.ToolOccurrenceContractTest' --tests 'ai.torad.aisdk.StructuredUiPokoTest' --tests 'ai.torad.aisdk.UIToolInvocationStateTest' --tests 'ai.torad.aisdk.ToolPartHandlerRegistryTest' --tests 'ai.torad.aisdk.FullPortFeatureParityTest' --tests 'ai.torad.aisdk.StreamingDocSnippetTest' --tests 'ai.torad.aisdk.ToUIMessageStreamTest' && ./gradlew checkKotlinAbi" (by=orchestrator implied; no flag)
# [2026-07-26] Current-tree re-audit confirms U1 must migrate the full lifecycle seam, not only three bags. Add public generic sealed UIToolInvocationState with @Poko internal-constructor variants/factories: InputStreaming(partialInput?), InputAvailable(required input), ApprovalRequested/ApprovalResponded(required input, nullable approvalId/signature), OutputAvailable(required output, optional input, preliminary, optional approval trail/reason compatibility), OutputError(required message, optional input/returned output/approval trail), OutputDenied(optional input/returned output/approvalId/reason/signature). Dynamic tools support only input/output/error/denied states and reject approval variants/metadata. Preserve valid exceptions: approvalId may fall back to toolCallId; host output may lack input; error/denial may retain returned payload; terminal approval metadata persists. ToolUI/DynamicToolUI keep generated serializers, old constructors/getters, and exact flat JSON as deprecated beta02 validating projections over one lifecycle source; do not introduce nested lifecycle wire or custom serializer if it removes dumped serializer ABI. UIToolInvocation keeps identity equality and legacy constructor/payload/metadata projections, all deprecated but present. Migrate MessageStreamReader, ConvertToModelMessages, Chat, Streams, and renderer mapping to exhaustive variants; preserve contextual user approval-response interpretation, duplicate occurrences, late-delta resistance, preliminary replacement, and denial progression. Existing PascalCase state strings gain explicit SerialName. Valid old JSON round-trips byte-shape-equivalent; malformed bags intentionally fail. New public APIs/deprecations target 0.3.0-beta02. If staged, state+wire and lifecycle-consumer stages are both required before done; final ABI review must prove old constructors/getters/generated serializers remain. Fence and focused gate widened to actual seam/classes.

[[items]]
id = "V1"
phase = "V"
title = "Reconcile every public API and behavior change, preserve explicit keep-as-String boundaries, review the combined diff, regenerate ABI dumps, and pass full repository verification"
files = ["src/**", "api/**", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "gradle.properties"]
status = "todo"
verify = "./gradlew updateKotlinAbi && ./gradlew check && bash .claude/hooks/rules/ci-gate.sh"
# [2026-07-25] Baseline on 2026-07-25: bash .claude/hooks/rules/ci-gate.sh PASS and ./gradlew check BUILD SUCCESSFUL on the combined pre-campaign dirty tree. Shared facts verified at build.gradle.kts:48-93 (KMP explicit API + ABI), build.gradle.kts:148-179 (source sets), and .claude/hooks/rules/ci-gate.sh:49-159 (enforced gate).
# [2026-07-25] Campaign CLI vendored 2026-07-25 from torad-fleet/torad-fleet-main dev/campaigns/manifest.py; selftest passed. Trace-hydration leg skipped mechanically because this repo has no scripts/hydrate-traces.py; portable ledger operations passed.
# [2026-07-25] Premise audit BLOCKED_ON_PRECONDITION; full evidence in premise-results.json. Corrected scope: Do not implement V1 now. Once all 18 predecessors are `done` or `verified`, pin `2ad450a` as the reconciliation base and make V1 one closeout landing only: (1) review the complete `2ad450a..worktree` delta under the four target scopes; (2) compare every changed direct String-typed declaration against the binding keep decisions in `/home/marcos/Documents/dev/opensource/aisdk-kotlin/dev/campaigns/stringly-domain-types/full-sweep.md:723-827`, preserving raw protocol, open-provider, identifier, MIME, diagnostic, and co…
# [2026-07-26] FENCE-EDITED: files = ["src/**", "api/**", "CHANGELOG.md", "INTERFACE_CONTRACT.md"] -> files = ["src/**", "api/**", "CHANGELOG.md", "INTERFACE_CONTRACT.md", "gradle.properties"] (by=orchestrator implied; no flag)
# [2026-07-26] Dependency audit correction: final reconciliation baseline is current release branch plus per-item frozen baseline tuples, not only historical 2ad450a. V1 must inspect git status as well as diff so untracked source cannot disappear; update VERSION_NAME to 0.3.0-beta02 while remaining on release/v0.3.0-beta01; normalize new @since/deprecation wording to beta02; regenerate ABI once after all source decisions; review campaign delta and broad 2ad450a..final release delta separately. Preserve the two untracked I4 polling tests in every relevant staged baseline and final source inventory.
